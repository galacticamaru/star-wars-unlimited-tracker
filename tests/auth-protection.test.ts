import { describe, it } from "vitest";

describe("Auth Protection", () => {
    it.todo("redirects unauthenticated users from /collection to /login");
    it.todo("redirects unauthenticated users from /decks to /login");
    it.todo("allows authenticated users to access /collection");
});
