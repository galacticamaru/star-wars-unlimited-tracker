import { describe, it } from "vitest";

describe("First-User Migration Hook", () => {
    it.todo("migrates legacy data (userId=1) to the first registered user");
    it.todo("does not migrate data for subsequent user registrations");
    it.todo("reassigns userCollections correctly");
    it.todo("reassigns decks correctly");
});
