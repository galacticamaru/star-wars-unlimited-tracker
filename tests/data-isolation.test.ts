import { describe, it } from "vitest";

describe("Data Isolation", () => {
    it.todo("only returns collection items belonging to the current user");
    it.todo("only returns decks belonging to the current user");
    it.todo("prevents users from updating other users' collection items");
    it.todo("prevents users from updating other users' decks");
});
