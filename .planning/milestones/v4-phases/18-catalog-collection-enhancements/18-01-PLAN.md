---
phase: 18-catalog-collection-enhancements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/starter-decks.ts
  - src/db/queries/collection.ts
  - src/app/api/collection/starter-deck/route.ts
  - src/app/collection/page.tsx
  - tests/starter-deck-api.test.ts
autonomous: true
requirements: [REQ-CAT-04]
must_haves:
  truths:
    - "User can trigger a single action to add all cards from a starter deck"
    - "After quick-add, the user receives confirmation of how many cards were added"
    - "Quantities are incremented on top of existing counts, not overwritten"
  artifacts:
    - path: "src/data/starter-decks.ts"
      provides: "Starter deck definitions"
    - path: "src/db/queries/collection.ts"
      provides: "incrementVariantCount query"
    - path: "src/app/api/collection/starter-deck/route.ts"
      provides: "POST endpoint for quick-add"
  key_links:
    - from: "src/app/collection/page.tsx"
      to: "/api/collection/starter-deck"
      via: "POST request"
---

<objective>
Provide a single-click Quick-Add for official Star Wars: Unlimited starter decks on the collection page.

Purpose: Reduce friction for new players adding known starter sets by automating the increment of card counts for all cards in the pre-constructed deck.
Output: Working starter deck importer with static data, safe DB increment logic, and UI integration.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-catalog-collection-enhancements/18-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Starter Deck API & Database</name>
  <files>src/data/starter-decks.ts, src/db/queries/collection.ts, src/app/api/collection/starter-deck/route.ts, tests/starter-deck-api.test.ts</files>
  <behavior>
    - Test 1: POST /api/collection/starter-deck validates deckId and increments `user_printing_collections` for Normal variants.
    - Test 2: `recomputeTotal` is called for all affected definitions, updating `user_collections`.
    - Test 3: Increments are additive (adding same deck twice doubles counts).
  </behavior>
  <action>
    1. Create `src/data/starter-decks.ts` with the static lists provided in 18-RESEARCH.md.
    2. Add `incrementVariantCount(cardPrintingId: number, qtyToAdd: number, userId: number)` to `src/db/queries/collection.ts` using `sql`${userPrintingCollections.count} + ${qtyToAdd}`` on conflict (D-13, Pitfall 1).
    3. Implement `POST /api/collection/starter-deck` that:
       - Validates `deckId` against `starterDecks` list.
       - Queries `card_printings` for matching `setCode` + `collectorNumber` + `variantType='Normal'` for each card in deck.
       - Calls `incrementVariantCount` for each resolved printing.
       - Calls `recomputeTotal` for distinct `cardDefinitionId`s affected.
       - Returns `{ cardsAdded }`.
       - Enforce auth using existing patterns (D-14).
  </action>
  <verify>
    <automated>npx vitest run tests/starter-deck-api.test.ts</automated>
  </verify>
  <done>API successfully increments counts and total collections without overwriting existing data.</done>
</task>

<task type="auto">
  <name>Task 2: Starter Deck UI Integration</name>
  <files>src/app/collection/page.tsx</files>
  <action>
    Add a "Quick-Add Starter Deck" section to the Collection page (D-08).
    - Match existing UI pattern for CSV import.
    - Component: A `<select>` containing names from `starterDecks` and an "Add to Collection" button (D-09).
    - Logic: On click, send POST request. On success, show inline message: "Added {N} cards from {Deck Name} to your collection." (D-10).
    - Call `router.refresh()` to update collection data shown on page.
  </action>
  <verify>
    <automated>npm run lint -- src/app/collection/page.tsx</automated>
  </verify>
  <done>UI allows selecting a starter deck, sends the POST request, and displays success message inline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | Untrusted input (deckId) crosses to server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | starter-deck route | mitigate | Validate deckId against the known static list before processing; enforce session.user.id |
</threat_model>

<verification>
1. Starter deck added via UI successfully increments variants in DB.
2. Multiple additions result in cumulative counts (idempotent-safe increment).
3. API requires authenticated session.
</verification>

<success_criteria>
- `POST /api/collection/starter-deck` works reliably and increments existing counts.
- `src/data/starter-decks.ts` contains all SOR, SHD, and TWI starters.
- Collection page UI allows easy deck addition with clear feedback.
</success_criteria>

<output>
After completion, create `.planning/phases/18-catalog-collection-enhancements/18-01-SUMMARY.md`
</output>