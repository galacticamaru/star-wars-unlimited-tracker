---
phase: 18-catalog-collection-enhancements
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/queries/catalog.ts
  - src/app/cards/page.tsx
  - src/components/catalog/catalog-client.tsx
  - src/components/catalog/card-grid.tsx
  - src/components/catalog/card-item.tsx
  - tests/catalog-variant.test.ts
autonomous: true
requirements: [REQ-COLLECT-08]
must_haves:
  truths:
    - "A catalog card tile displays the art of whichever variant the user owns the most copies of"
    - "Tie-breaking precedence (Showcase > Hyperspace Foil > Hyperspace > Foil > Normal) is respected"
    - "Logged-out users see Normal art by default"
  artifacts:
    - path: "src/db/queries/catalog.ts"
      provides: "getPrintingArtMap query"
    - path: "src/components/catalog/card-grid.tsx"
      provides: "Best variant art selection logic"
  key_links:
    - from: "src/app/cards/page.tsx"
      to: "src/components/catalog/catalog-client.tsx"
      via: "printingArtMap prop"
---

<objective>
Enhance the visual catalog to accurately reflect the user's specific collection by dynamically showing the art of the highest-owned variant.

Purpose: Make the card catalog more personalized and visually rewarding for collectors (e.g., seeing their rare Showcase art in the main list).
Output: Working variant art resolution logic in the catalog component tree.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-catalog-collection-enhancements/18-RESEARCH.md

<interfaces>
From src/app/api/collection/collection-shape.ts:
```typescript
export interface CollectionMap {
  [cardDefinitionId: number]: {
    total: number;
    variants: Record<number, number>; // cardPrintingId -> count
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Printing Art Data Layer</name>
  <files>src/db/queries/catalog.ts, src/app/cards/page.tsx, tests/catalog-variant.test.ts</files>
  <behavior>
    - Test 1: `getPrintingArtMap` returns a map keyed by `cardPrintingId` containing `variantType` and `frontArtUrl`.
    - Test 2: `tests/catalog-variant.test.ts` (unit tests for precedence logic) pass.
  </behavior>
  <action>
    1. In `src/db/queries/catalog.ts`, create `getPrintingArtMap()` that selects `id`, `variantType`, and `frontArtUrl` from `card_printings` and reduces them into a `Record<number, { variantType: string; frontArtUrl: string | null }>`.
    2. In `src/app/cards/page.tsx`, await `getPrintingArtMap()` and pass it to `<CatalogClient />` (D-01).
    3. Initialize `tests/catalog-variant.test.ts` with precedence tests using the mapping logic from 18-RESEARCH.md Pattern 1.
  </action>
  <verify>
    <automated>npx vitest run tests/catalog-variant.test.ts</automated>
  </verify>
  <done>Backend correctly provides art mapping and precedence logic is verified.</done>
</task>

<task type="auto">
  <name>Task 2: UI Art Resolution Logic</name>
  <files>src/components/catalog/catalog-client.tsx, src/components/catalog/card-grid.tsx, src/components/catalog/card-item.tsx</files>
  <action>
    1. Update `CatalogClient` props to accept `printingArtMap`. Thread it to `CardGrid`.
    2. In `CardGrid`, implement the logic for selecting `bestVariantArtUrl` for each card (D-02, D-04):
       - Iterate over `collection[card.id].variants`.
       - Find variant with highest count > 0.
       - Tie-break using: Showcase(5) > Hyperspace Foil(4) > Hyperspace(3) > Foil(2) > Normal(1).
       - Look up `frontArtUrl` in `printingArtMap`.
    3. Update `CardItem` to accept an optional `frontArtUrl` prop. If provided, use it for the main image instead of the default normal art.
    4. Ensure logged-out users still see Normal art (D-03).
  </action>
  <verify>
    <automated>npm run lint -- src/components/catalog/card-grid.tsx</automated>
  </verify>
  <done>Catalog components dynamically resolve and display the best variant art owned by the user.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None. This plan only enhances data display based on existing collection access.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02 | Information Disclosure | printingArtMap | accept | Art URLs and variant types are public catalog data. |
</threat_model>

<verification>
1. Catalog card tiles show Hyperspace art when a user owns more Hyperspace variants than Normal variants.
2. Precedence logic correctly selects Showcase over other variants if counts are equal.
3. Logged out users see standard card art.
</verification>

<success_criteria>
- `CardGrid` efficiently resolves the highest-owned variant art for each card.
- `printingArtMap` is fetched once and passed down via RSC.
- Catalog UI remains performant with the new resolution logic.
</success_criteria>

<output>
After completion, create `.planning/phases/18-catalog-collection-enhancements/18-02-SUMMARY.md`
</output>