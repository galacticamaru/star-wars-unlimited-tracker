---
phase: 17-variant-collection-tracking
plan: "08"
type: execute
wave: 6
depends_on:
  - "17-07"
files_modified: []
autonomous: false
requirements:
  - REQ-COLLECT-06
  - REQ-COLLECT-07

must_haves:
  truths:
    - "Card detail page shows all same-set variant rows with owned counts"
    - "Increment and decrement persist across page reload"
    - "Total line reflects the sum of variant counts correctly"
    - "Catalog and deck builder owned-count overlays still work correctly after GET shape change"
  artifacts:
    - path: "src/components/catalog/variant-collection-section.tsx"
      provides: "UI verified by user"
    - path: "src/app/cards/[set-code]/[card-number]/page.tsx"
      provides: "Card detail page verified end-to-end by user"
  key_links: []
---

<objective>
Human verification checkpoint. Confirm the card detail page renders correctly with per-variant collection controls, that mutations persist, that the total line is accurate, and that existing catalog/deck-builder owned-count functionality still works with the updated GET /api/collection response shape.

Purpose: Visual and functional verification that cannot be automated — requires live DB + auth + browser interaction. All automated tests (TypeScript, Vitest) have already passed by this point.

Output: User approves all success criteria or reports issues for gap closure.
</objective>

<execution_context>
@C:\Users\alan_\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\alan_\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/17-variant-collection-tracking/17-CONTEXT.md
@.planning/phases/17-variant-collection-tracking/17-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify card detail page variant collection section</name>

  <what-built>
    - New VariantCollectionSection component on the card detail page
    - Per-variant +/- controls (one row per printing in the same set)
    - Read-only Total line below the variant rows
    - POST /api/collection/variants mutation endpoint
    - GET /api/collection returns enriched { total, variants } shape
    - All three consumers (CatalogClient, WantListTab, filter-cards) updated to read .total
  </what-built>

  <how-to-verify>
**Step 1: Start the development server**
```
npm run dev
```

**Step 2: Navigate to a card detail page for a card that has multiple variants in the same set**
Example URL pattern: `http://localhost:3000/cards/SOR/059`
(Replace SOR/059 with any card that has Hyperspace or Foil variants)

**Step 3: Verify the "Your Collection" section**
- [ ] The section appears below the card image (where CollectionControls used to be)
- [ ] The section label reads "YOUR COLLECTION" (uppercase)
- [ ] Multiple rows appear — one per variant type (e.g., "Normal", "Hyperspace", "Showcase")
- [ ] Each row has: variant type label (left), Minus button, count display, Plus button, status text (right)
- [ ] Minus button is DISABLED on rows where count is 0
- [ ] Status shows "Owned" (bold primary color) when count > 0, "Not owned" (muted) when count is 0
- [ ] A "Total: X copies" line appears BELOW the variant rows

**Step 4: Test increment**
- Click + on the "Normal" variant row
- [ ] Count increases by 1 immediately (optimistic update — no page reload)
- [ ] Total line increases by 1
- Reload the page
- [ ] Count persists (was saved to DB)

**Step 5: Test decrement**
- Click - on a row with count > 0
- [ ] Count decreases by 1 immediately
- [ ] Minus button becomes disabled when count reaches 0

**Step 6: Verify catalog still shows owned-count overlays correctly**
Navigate to `http://localhost:3000/cards`
- [ ] Cards you own still show a count badge/overlay
- [ ] The owned-only filter (if applied) still shows only owned cards
- No console errors about `[object Object]` or NaN

**Step 7: Verify WantListTab still calculates shortfall correctly**
Open any deck, go to the Want List tab
- [ ] Shortfall counts look correct (not NaN, not [object Object])

**Step 8: Verify a card with only ONE variant (no Hyperspace/Showcase)**
Navigate to a base card or a common card that only has Normal variant
- [ ] The section shows exactly one row (Normal)
- [ ] Total line still appears with the single count
  </how-to-verify>

  <resume-signal>
    Type "approved" if all checks pass, or describe any issues found (e.g., "Normal count not persisting", "Catalog shows [object Object]").
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ App | Manual verification through authenticated browser session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-08-01 | Information Disclosure | Manual verification session | accept | User is verifying their own collection data. No cross-user data exposure during verification. |
</threat_model>

<verification>
Human confirms all 8 verification steps pass.
</verification>

<success_criteria>
1. Card detail page lists each same-set variant with current owned count
2. User can increment count per variant — persists after page reload
3. User can decrement count per variant (disabled at 0)
4. Total line shows correct sum
5. Catalog owned-count overlays still work (no regression)
6. WantListTab shortfall counts still correct (no regression)
</success_criteria>

<output>
After completion, create `.planning/phases/17-variant-collection-tracking/17-08-SUMMARY.md`
</output>
