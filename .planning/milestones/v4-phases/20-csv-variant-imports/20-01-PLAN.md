---
phase: 20-csv-variant-imports
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/collection/normalize.ts
  - src/lib/collection/normalize.test.ts
  - src/app/api/collection/import/route.ts
autonomous: true
requirements:
  - REQ-COLLECT-09
must_haves:
  truths:
    - CSV imports now process Hyperspace and F-Hyperspace columns.
    - Normalizer handles duplicate row counts via aggregation before emitting the array.
    - API correctly looks up printingId by joining cardDefinitions and cardPrintings.
  artifacts:
    - path: src/lib/collection/normalize.ts
      provides: "Aggregated array of { swudbId, variantType, count }"
    - path: src/app/api/collection/import/route.ts
      provides: "Batch lookup via swudbId and variantType"
  key_links:
    - from: src/lib/collection/normalize.ts
      to: src/app/api/collection/import/route.ts
      via: JSON array payload
    - from: src/app/api/collection/import/route.ts
      to: "cardDefinitions & cardPrintings"
      via: "innerJoin on cardDefinitionId"
---

<objective>
Update CSV imports to support all four variant types: Normal, Foil, Hyperspace, and Hyperspace Foil.
Purpose: Shift the burden of Hyperspace printing resolution from the client (which cannot know the arbitrary collectorNumber) to the server (which can query by swudbId + variantType).
Output: Updated normalizer and API route.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-csv-variant-imports/20-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update normalizeRedditCsv to emit variant objects</name>
  <files>src/lib/collection/normalize.ts, src/lib/collection/normalize.test.ts</files>
  <behavior>
    - Parses Normal, Foil, Hyperspace, and Hyperspace Foil columns.
    - F-Hyperspace maps to "Hyperspace Foil".
    - Standard and Non-Foil columns are MAX'd together into "Normal".
    - Aggregates duplicate rows (same swudbId and variantType) by summing them before mapping to the final array to avoid multiple upsert operations for the same variant.
    - Returns an array of `{ swudbId: string, variantType: string, count: number }` instead of a Record.
  </behavior>
  <action>
    1. Update the normalizer logic to iterate over rows, compute `swudbId` (`${setCode}-${num}`), and build an intermediate `Record<string, number>` keyed by `${swudbId}|${variantType}` to aggregate counts.
    2. Map "Standard" or "Non-Foil" to "Normal", "Foil" to "Foil", "Hyperspace" to "Hyperspace", and "F-Hyperspace" to "Hyperspace Foil".
    3. Return the mapped array.
    4. Update all tests in `normalize.test.ts` to expect arrays of objects rather than key-value records. Add tests specifically for Hyperspace and F-Hyperspace columns.
  </action>
  <verify>
    <automated>npm test src/lib/collection/normalize.test.ts</automated>
  </verify>
  <done>Normalizer tests pass and emit the expected array of objects for all four variants.</done>
</task>

<task type="auto">
  <name>Task 2: Update import API route for array payload and JOIN lookup</name>
  <files>src/app/api/collection/import/route.ts</files>
  <action>
    1. Change payload validation to expect a JSON array.
    2. Extract all distinct `swudbId` values from the payload to use for chunked querying.
    3. Update the mapping logic to query `cardPrintings` and `innerJoin` `cardDefinitions` (where `cardDefinitions.id = cardPrintings.cardDefinitionId`).
    4. Filter using `inArray(cardDefinitions.swudbId, chunk)`.
    5. Build the `mapping` dictionary keyed by `${row.swudbId}|${row.variantType}` pointing to `{ printingId, cardDefinitionId }`.
    6. Iterate over the input array, look up the `printingId`, enforce `Math.max(0, count)`, and call `upsertVariantCount`.
    7. Track affected `cardDefinitionId`s and call `recomputeTotal` as before.
    8. Limit `swudbIds.length` to 2000 (instead of `collectorNumbers.length`).
  </action>
  <verify>
    <automated>npm run check-types -- src/app/api/collection/import/route.ts</automated>
  </verify>
  <done>API route is fully typed, handles the array payload, and resolves printingId via join.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | User uploads arbitrary JSON mapping (via CSV) representing their collection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01-01 | Denial of Service | `POST /api/collection/import` | mitigate | Cap unique `swudbId` queries to 2000 to prevent large IN clause database crashes. Chunk queries to 500 at a time. |
| T-20-01-02 | Tampering | `POST /api/collection/import` | mitigate | Force `Math.max(0, count)` to ensure malicious payloads cannot inject negative counts into the database. |
| T-20-01-03 | Information Disclosure | `POST /api/collection/import` | mitigate | Verify session exists and map upserts explicitly to `session.user.id`. |
</threat_model>

<verification>
Ensure the CSV upload UI still triggers the API successfully with a sample CSV that has Hyperspace variants.
</verification>

<success_criteria>
The codebase supports normalizing and importing all four variant types natively via the CSV interface, with proper lookup on the backend.
</success_criteria>

<output>
After completion, create `.planning/phases/20-csv-variant-imports/20-01-SUMMARY.md`
</output>