---
phase: 15.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["src/components/decks/deck-builder.tsx"]
autonomous: true
requirements: []
must_haves:
  truths:
    - "Deck list card rows in all sections are ordered by cost ascending, then alphabetically by name"
  artifacts:
    - path: "src/components/decks/deck-builder.tsx"
      provides: "Sorted mainDeck and sideboard arrays"
  key_links: []
---

<objective>
Order each cardRow in a decklist by cost ascending.

Purpose: Makes it easier to read the deck's cost curve at a glance when viewing the decklist grouped by type.
Output: Updated `deck-builder.tsx` sorting the `mainDeck` and `sideboard` arrays.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sort mainDeck and sideboard by cost</name>
  <files>src/components/decks/deck-builder.tsx</files>
  <action>
    Locate the `useMemo` blocks for `mainDeck` and `sideboard` in `src/components/decks/deck-builder.tsx`.
    Add a `.sort()` call to the end of the chain (after the final filter) for both `mainDeck` and `sideboard`.
    The sort function should be:
    ```typescript
    .sort((a, b) => {
      const costA = a.card.cost ?? 0;
      const costB = b.card.cost ?? 0;
      if (costA !== costB) return costA - costB;
      return a.card.name.localeCompare(b.card.name);
    })
    ```
    This ensures that when `groupDeckCards` processes `mainDeck`, it preserves the cost order within each type group, and `sideboard` is inherently ordered.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    Both mainDeck and sideboard arrays are sorted by cost ascending, falling back to alphabetical name sorting.
  </done>
</task>

</tasks>