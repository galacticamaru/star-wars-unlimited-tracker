# Phase 16.1: Reorder Deck Tabs - Context

**Gathered:** 2026-05-15
**Status:** Ready for planning

<domain>
## Phase Boundary

Reorder the three tabs in the deck builder and change the default active tab. No new tabs, no new behavior — purely tab order and initial state.

</domain>

<decisions>
## Implementation Decisions

### Tab Order
- **D-01:** New tab order (left to right): **Deck List → Add Cards → Want List**
- **D-02:** Current order is Add Cards → Deck List → Want List; the two leftmost tabs swap positions

### Default Tab
- **D-03:** Default view changes from `'catalog'` to `'editor'` — Deck List is the first tab the user sees when opening a deck
- **D-04:** The existing `useState<'editor' | 'catalog' | 'want-list'>('catalog')` initial value changes to `'editor'`

### Claude's Discretion
- No other behavior changes. All tab button styling, active/inactive states, and view rendering logic remain identical.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Primary Implementation Target
- `src/components/decks/deck-builder.tsx` — The only file that changes. Tab buttons are at lines ~318–343 (the `<div className="flex bg-slate-100 rounded-lg p-1">` block). Default state is at line ~128.

### No External Specs
No external specs — requirements fully captured in decisions above.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- Three tab buttons already exist with identical styling pattern (variant + className based on active view). The reorder is a pure JSX rearrangement — no new props, no new state.

### Established Patterns
- Active tab: `variant="secondary"` + `className="bg-white shadow-sm"`
- Inactive tab: `variant="ghost"` + empty className
- All three views (`'editor'`, `'catalog'`, `'want-list'`) are already fully implemented

### Integration Points
- Line ~128: `useState<'editor' | 'catalog' | 'want-list'>('catalog')` → change default to `'editor'`
- Lines ~318–343: JSX tab button block → move Deck List button before Add Cards button
- No other files need changes

</code_context>

<specifics>
## Specific Ideas

User stated the exact desired order verbatim: "Deck List → Add Cards → Want List. The default tab is Deck List." No ambiguity.

</specifics>

<deferred>
## Deferred Ideas

None — discussion stayed within phase scope.

</deferred>

---

*Phase: 16.1-reorder-deck-tabs*
*Context gathered: 2026-05-15*
