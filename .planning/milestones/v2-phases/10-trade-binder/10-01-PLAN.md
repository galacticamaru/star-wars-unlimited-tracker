---
requirements: [TRADE-01, TRADE-02, TRADE-03, TRADE-05]
must_haves:
  truths:
    - "Database schema supports trade quantities, exclusions, and manual wants."
    - "Better Auth resolves human-readable usernames for binder URLs."
    - "Looking For logic correctly merges auto-shortfalls and manual overrides."
  artifacts:
    - path: "src/db/schema.ts"
      provides: "Schema definitions for trade binder features"
      min_lines: 300
    - path: "src/lib/auth.ts"
      provides: "Better Auth configuration with username plugin"
      min_lines: 50
    - path: "src/lib/binder-logic.ts"
      provides: "Composite logic for Looking For list"
      min_lines: 40
  key_links: []
---

# Phase 10-01: Trade Binder - Infrastructure & Logic

This plan sets up the foundational data structures and core business logic for the trade binder.

## Tasks

<task id="10.1">
<files>
- src/db/schema.ts
</files>
<action>
Extend the database schema to support trade binder features:
1. Update `user` table with `username` and `displayUsername` columns (Better Auth plugin requirements).
2. Add `tradeQuantity` (integer, default 0) to `user_collections`.
3. Create `trade_exclusions` table (userId, cardDefinitionId).
4. Create `trade_manual_wants` table (userId, cardDefinitionId, quantity).
</action>
<verify>
<automated>
npx drizzle-kit generate && npx drizzle-kit push
</automated>
</verify>
<done>Tables exist in DB and schema is synced.</done>
</task>

<task id="10.2">
<files>
- src/lib/auth.ts
- tests/auth-config.test.ts
</files>
<action>
Configure Better Auth with the `username` plugin. Ensure uniqueness and normalization are handled by the library.
Write a test to verify the plugin is active.
</action>
<verify>
<automated>
npm test tests/auth-config.test.ts
</automated>
</verify>
<done>Better Auth configured for human-readable usernames.</done>
</task>

<task id="10.3">
<files>
- src/lib/binder-logic.ts
- src/lib/binder-logic.test.ts
</files>
<action>
Implement the core logic for the composite "Looking For" list.
Logic: `Math.max(autoShortfall, manualQuantity) - exclusions - currentInventory`.
Include unit tests to verify merging behavior and precedence.
</action>
<verify>
<automated>
npm test src/lib/binder-logic.test.ts
</automated>
</verify>
<done>Logic is robust and tested.</done>
</task>
