---
phase: 06-auth-multi-user
plan: 04
type: execute
wave: 4
depends_on: [06-01, 06-02, 06-03]
files_modified: [src/proxy.ts, src/lib/auth.ts, src/components/nav-bar.tsx, src/components/catalog/catalog-client.tsx, src/components/catalog/card-item.tsx, tests/auth-protection.test.ts, tests/data-isolation.test.ts, tests/migration-hook.test.ts]
autonomous: true
requirements: [AUTH-03, AUTH-04, AUTH-07]
must_haves:
  truths:
    - "Unauthenticated users are redirected to /login when accessing /collection or /decks"
    - "NavBar shows 'Sign In' when logged out and User Avatar when logged in"
    - "First registered user inherits data from userId=1"
    - "Clicking +/- on catalog while logged out redirects to /login"
    - "All Vitest auth and isolation tests pass"
  artifacts:
    - path: "src/proxy.ts"
      provides: "Route protection middleware"
  key_links:
    - from: "src/proxy.ts"
      to: "src/app/(auth)/login/page.tsx"
      via: "NextResponse.redirect"
---

<objective>
Implement route protection, data migration for the first user, and final UI integrations. Complete and run the test suites to verify full system integrity.

Purpose: Finalize the auth wall and ensure a smooth transition for v1 users with verified data isolation.
Output: Fully protected application with user-specific navigation and interaction logic, backed by passing tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-auth-multi-user/06-01-SUMMARY.md
@.planning/phases/06-auth-multi-user/06-02-SUMMARY.md
@.planning/phases/06-auth-multi-user/06-03-SUMMARY.md
@src/lib/auth.ts
@src/components/nav-bar.tsx
@src/components/catalog/catalog-client.tsx
@src/components/catalog/card-item.tsx
@tests/auth-protection.test.ts
@tests/data-isolation.test.ts
@tests/migration-hook.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Route Protection Middleware</name>
  <files>src/proxy.ts, tests/auth-protection.test.ts</files>
  <action>
    1. Create `src/proxy.ts` (Next.js 16 convention) to protect `/collection` and `/decks` routes.
    2. Use `getSessionCookie` from `better-auth/cookies` to check for an active session.
    3. Redirect unauthenticated requests to `/login` (per D-02).
    4. Allow public access to `/` and `/cards/*` (per D-01).
    5. Implement tests in `tests/auth-protection.test.ts` to verify these redirects.
  </action>
  <verify>
    <automated>npx vitest run tests/auth-protection.test.ts</automated>
  </verify>
  <done>Middleware created and protects private routes; tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Implement v1 Data Migration Hook</name>
  <files>src/lib/auth.ts, tests/migration-hook.test.ts, tests/data-isolation.test.ts</files>
  <action>
    1. Add `databaseHooks` to the `betterAuth` configuration in `src/lib/auth.ts`.
    2. In the `user.create.after` hook, check if the new user is the first one in the `user` table.
    3. If they are the first user, update all `user_collections` and `decks` rows where `user_id = 1` to the new user's ID (per D-11).
    4. Implement tests in `tests/migration-hook.test.ts` for the migration logic.
    5. Implement tests in `tests/data-isolation.test.ts` to verify data is correctly filtered by userId in queries.
  </action>
  <verify>
    <automated>npx vitest run tests/migration-hook.test.ts tests/data-isolation.test.ts</automated>
  </verify>
  <done>First-user migration logic implemented and verified with tests.</done>
</task>

<task type="auto">
  <name>Task 3: Update NavBar and Catalog UI for Auth State</name>
  <files>src/components/nav-bar.tsx, src/components/catalog/catalog-client.tsx, src/components/catalog/card-item.tsx</files>
  <action>
    1. Update `NavBar` to use `authClient.useSession()`.
    2. Show "Sign In" link when logged out, and a User Avatar/Sign Out dropdown when logged in (per D-08, D-09, D-10).
    3. Update `CatalogClient` to fetch the session and pass `isAuthenticated` down to `CardItem`.
    4. Update `CardItem` +/- buttons: if `!isAuthenticated`, call `router.push('/login')` instead of the API (per D-03).
  </action>
  <verify>
    <automated>npm run build --no-lint</automated>
  </verify>
  <done>UI reflects authentication state and redirects unauthenticated interactions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Server (Middleware) | Middleware must reliably identify session state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Information Disclosure | Private Routes | mitigate | Middleware (proxy.ts) ensures no private data is served without a session. |
| T-06-07 | Tampering | Migration Hook | mitigate | migration logic only runs ONCE for the first user creation. |
</threat_model>

<verification>
1. Run all vitest tests: `npx vitest run`.
2. Manually test logout and then trying to visit `/collection` (should redirect).
3. Manually test clicking +/- in the catalog while logged out (should redirect).
4. Verify NavBar changes when logged in.
</verification>

<success_criteria>
The application is fully secured, and all multi-user requirements are verified by automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/06-auth-multi-user/06-04-SUMMARY.md`
</output>
