---
phase: 06-auth-multi-user
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified: [src/db/queries/decks.ts, src/db/queries/card-detail.ts, src/db/queries/catalog.test.ts, src/db/queries/catalog.ts, src/db/queries/collection.ts]
autonomous: true
requirements: [AUTH-04]
must_haves:
  truths:
    - "All collection and deck queries filter by userId"
  artifacts:
    - path: "src/db/queries/collection.ts"
      provides: "User-isolated collection queries"
---

<objective>
Refactor all database queries to ensure strict data isolation per user. Every query that touches user-owned data must require a userId parameter.

Purpose: Prevent cross-user data leaks at the database query level.
Output: Refactored query functions that are multi-user aware.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-auth-multi-user/06-01-SUMMARY.md
@src/lib/auth.ts
@src/db/queries/catalog.ts
@src/db/queries/collection.ts
@src/db/queries/decks.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor database queries for multi-user isolation</name>
  <files>src/db/queries/decks.ts, src/db/queries/card-detail.ts, src/db/queries/catalog.test.ts, src/db/queries/catalog.ts, src/db/queries/collection.ts</files>
  <behavior>
    - Ensure all query functions (e.g., `getUserCollection`, `getUserDecks`) accept a `userId: number` parameter.
    - Remove all hardcoded `userId = 1` or `.default(1)` assumptions.
    - Add `and(eq(table.userId, userId), ...)` filters to all relevant Drizzle queries.
    - Update `catalog.ts` to join with `userCollections` on both `cardDefinitionId` AND `userId`.
    - Update `catalog.test.ts` to pass a valid `userId` to query calls.
  </behavior>
  <action>
    Modify the listed query files to add the `userId` parameter and apply the filter in every database call involving user-owned tables (`user_collections`, `decks`).
  </action>
  <verify>
    <automated>npm test src/db/queries/catalog.test.ts</automated>
  </verify>
  <done>Queries are isolated by userId and tests pass (after updating test calls with a userId).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API Route → DB Query | userId parameter must be trusted (comes from session) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Information Disclosure | DB Queries | mitigate | Strict `userId` filtering on every query involving user-owned data. |
</threat_model>

<verification>
1. Run updated tests for catalog queries.
</verification>

<success_criteria>
All user-specific queries are refactored to require a userId.
</success_criteria>

<output>
After completion, create `.planning/phases/06-auth-multi-user/06-02-SUMMARY.md`
</output>
