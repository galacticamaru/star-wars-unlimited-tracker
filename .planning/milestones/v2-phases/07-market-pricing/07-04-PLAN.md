---
phase: 07-market-pricing
plan: 04
type: execute
wave: 4
depends_on: [07-03]
files_modified: [src/db/queries/decks.ts, src/components/decks/deck-sidebar.tsx, src/components/decks/want-list-tab.tsx]
autonomous: true
requirements: [MARKET-02, MARKET-03]

must_haves:
  truths:
    - "User can see the total estimated value of a deck in the sidebar"
    - "User can see the total estimated cost to complete their want list"
  artifacts:
    - path: "src/components/decks/deck-sidebar.tsx"
      provides: "Deck valuation display"
    - path: "src/components/decks/want-list-tab.tsx"
      provides: "Want list completion cost summary"
  key_links:
    - from: "src/components/decks/deck-sidebar.tsx"
      to: "src/db/queries/decks.ts"
      via: "price data in query results"
---

<objective>
Calculate and display financial summaries for decks and want lists, helping users understand the value of their builds and the cost of missing cards.

Purpose: Provide financial context for collection and deck building.
Output: Total deck cost and estimated want list completion cost.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-market-pricing/07-03-SUMMARY.md
@src/db/queries/decks.ts
@src/components/decks/deck-sidebar.tsx
@src/components/decks/want-list-tab.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update deck and want list queries</name>
  <files>src/db/queries/decks.ts</files>
  <action>
    Modify `src/db/queries/decks.ts` (and related collection/want list queries) to include `priceEur` and `priceUsd` from the `card_definitions` table in the results.
    - Ensure `getDeckById` and any want list calculation functions return pricing data for each card.
  </action>
  <verify>
    <automated>grep -i "price" src/db/queries/decks.ts</automated>
  </verify>
  <done>Database queries updated to provide pricing information for decks and lists.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Total Deck Cost in Sidebar</name>
  <files>src/components/decks/deck-sidebar.tsx</files>
  <action>
    Update `src/components/decks/deck-sidebar.tsx`:
    - Access current currency from `CurrencyContext`.
    - Calculate the sum of prices for all cards in the deck (Main + Sideboard).
    - Display "Estimated Value" with the total formatted in the selected currency.
    - Style as a prominent sidebar metric below card counts.
  </action>
  <verify>
    <automated>grep -i "estimated value" src/components/decks/deck-sidebar.tsx</automated>
  </verify>
  <done>Deck builder now shows the total market value of the current deck.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Estimated Cost to Complete in Want List</name>
  <files>src/components/decks/want-list-tab.tsx</files>
  <action>
    Update `src/components/decks/want-list-tab.tsx`:
    - Access current currency from `CurrencyContext`.
    - Calculate the sum of prices for the shortfall (Quantity Needed - Quantity Owned).
    - Display "Estimated Cost to Complete" in a sticky footer or summary bar at the bottom of the list.
    - Use high-contrast styling as per UI-SPEC.
  </action>
  <verify>
    <automated>grep -i "cost to complete" src/components/decks/want-list-tab.tsx</automated>
  </verify>
  <done>Want list now highlights the financial investment required to complete decks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend → UI | Financial calculations performed on the client using DB data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Information Disclosure | Deck Pricing | accept | Pricing info is public knowledge; showing it in the context of a user's deck is a intended feature |
</threat_model>

<verification>
- Changing the currency updates both the deck total and want list summary.
- Shortfall calculations correctly account for owned card counts.
</verification>

<success_criteria>
Users can accurately estimate the market value of their decks and the cost to acquire missing cards.
</success_criteria>

<output>
After completion, create `.planning/phases/07-market-pricing/07-04-SUMMARY.md`
</output>
