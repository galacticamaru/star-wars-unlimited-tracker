---
phase: 07-market-pricing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/db/schema.ts, src/lib/sync/prices.ts, .env.example]
autonomous: true
requirements: [MARKET-01, MARKET-04]

must_haves:
  truths:
    - "Database supports storing EUR/USD prices and update timestamps"
    - "API client can fetch prices for a specific set from pokewallet.io"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "Price columns in card_definitions"
    - path: "src/lib/sync/prices.ts"
      provides: "PokéWallet API client"
  key_links:
    - from: "src/lib/sync/prices.ts"
      to: "https://api.pokewallet.io"
      via: "fetch with POKEMON_API_KEY"
---

<objective>
Update the database schema to support pricing data and implement the infrastructure for communicating with the PokéWallet API.

Purpose: Foundation for market pricing integration.
Output: Schema update and functional API client.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-market-pricing/07-CONTEXT.md
@.planning/phases/07-market-pricing/07-RESEARCH.md
@src/db/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update database schema</name>
  <files>src/db/schema.ts</files>
  <action>
    Add the following columns to the `card_definitions` table:
    - `priceEur`: integer (to store cents)
    - `priceUsd`: integer (to store cents)
    - `pricesUpdatedAt`: timestamp

    Run `npx drizzle-kit generate` and `npx drizzle-kit migrate` (or use the appropriate project command) to apply changes to the local and Neon databases.
  </action>
  <verify>
    <automated>npx drizzle-kit check</automated>
  </verify>
  <done>Database schema updated and migrated with price columns.</done>
</task>

<task type="auto">
  <name>Task 2: Implement PokéWallet API client</name>
  <files>src/lib/sync/prices.ts, .env.example</files>
  <action>
    Create `src/lib/sync/prices.ts` containing:
    - `fetchSetPrices(setCode: string)`: Calls `https://api.pokewallet.io/prices/{setCode}` (or the RapidAPI equivalent if configured) using `POKEMON_API_KEY`.
    - Handles mapping the API response to a consistent internal format:
      - `priceEur`: from `cardmarket.variants.normal.current.low` (multiply by 100 and round).
      - `priceUsd`: from `tcgplayer.variants.Normal.current.market` (multiply by 100 and round).
    - Includes basic error handling and logging.

    Update `.env.example` with `POKEMON_API_KEY`.
  </action>
  <verify>
    <automated>ls src/lib/sync/prices.ts</automated>
  </verify>
  <done>API client implemented and environment variable documented.</done>
</task>

<task type="auto">
  <name>Task 3: Verify API connectivity</name>
  <files>scripts/test-api-prices.ts</files>
  <action>
    Create a temporary script `scripts/test-api-prices.ts` that:
    - Loads environment variables.
    - Calls `fetchSetPrices('SOR')`.
    - Logs the first 5 results to verify the mapping works (especially the nested variant paths and case sensitivity).
    - Runs via `tsx --env-file=.env.local scripts/test-api-prices.ts`.
  </action>
  <verify>
    <automated>tsx --env-file=.env.local scripts/test-api-prices.ts</automated>
  </verify>
  <done>API connectivity and response mapping verified with real data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PokéWallet API → Backend | External pricing data ingested into card_definitions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | .env.local | mitigate | Ensure POKEMON_API_KEY is not committed (checked by .gitignore) |
| T-07-02 | Denial of Service | External API | mitigate | Implement throttling and daily sync limits to avoid being banned |
</threat_model>

<verification>
- `card_definitions` table has `price_eur`, `price_usd`, and `prices_updated_at` columns.
- `fetchSetPrices` correctly extracts prices for 'Normal' variants.
</verification>

<success_criteria>
Schema is ready for pricing data and the API client is verified against the live service.
</success_criteria>

<output>
After completion, create `.planning/phases/07-market-pricing/07-01-SUMMARY.md`
</output>
