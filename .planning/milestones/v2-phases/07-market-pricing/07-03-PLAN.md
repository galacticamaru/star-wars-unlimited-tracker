---
phase: 07-market-pricing
plan: 03
type: execute
wave: 3
depends_on: [07-02]
files_modified: [src/components/currency-context.tsx, src/components/catalog/top-bar.tsx, src/app/cards/[set-code]/[card-number]/page.tsx, src/db/queries/card-detail.ts]
autonomous: true
requirements: [MARKET-01]

must_haves:
  truths:
    - "User can switch between EUR and USD currencies globally"
    - "Card detail page displays current prices in both currencies"
  artifacts:
    - path: "src/components/currency-context.tsx"
      provides: "CurrencyProvider and useCurrency hook"
    - path: "src/components/catalog/top-bar.tsx"
      provides: "CurrencyToggle UI"
  key_links:
    - from: "src/components/catalog/top-bar.tsx"
      to: "src/components/currency-context.tsx"
      via: "setCurrency function"
---

<objective>
Implement global currency state and update the UI to allow users to choose their preferred currency and see prices on card detail pages.

Purpose: Core UI functionality for market pricing.
Output: Global currency toggle and price displays on cards.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-market-pricing/07-UI-SPEC.md
@src/components/nav-bar.tsx
@src/components/catalog/top-bar.tsx
@src/app/cards/[set-code]/[card-number]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement CurrencyContext</name>
  <files>src/components/currency-context.tsx, src/app/layout.tsx</files>
  <action>
    Create `src/components/currency-context.tsx`:
    - Define `Currency` type: `'EUR' | 'USD'`.
    - Create `CurrencyContext` with `currency` state and `setCurrency` function.
    - Implement `localStorage` persistence so the choice survives page refreshes.
    - Wrap the application in `src/app/layout.tsx` with the `CurrencyProvider`.
  </action>
  <verify>
    <automated>ls src/components/currency-context.tsx</automated>
  </verify>
  <done>Global currency state implemented and persisted.</done>
</task>

<task type="auto">
  <name>Task 2: Add CurrencyToggle to TopBar</name>
  <files>src/components/catalog/top-bar.tsx</files>
  <action>
    Integrate a `CurrencyToggle` component into `src/components/catalog/top-bar.tsx`:
    - Use shadcn `Tabs` or a similar segmented control.
    - Allow switching between 'EUR' and 'USD'.
    - Apply styling from UI-SPEC (accent colors for active state).
  </action>
  <verify>
    <automated>grep -i "toggle" src/components/catalog/top-bar.tsx</automated>
  </verify>
  <done>Currency toggle added to the catalog header.</done>
</task>

<task type="auto">
  <name>Task 3: Update Card Detail UI with prices</name>
  <files>src/db/queries/card-detail.ts, src/app/cards/[set-code]/[card-number]/page.tsx</files>
  <action>
    - Update `src/db/queries/card-detail.ts` to select `priceEur` and `priceUsd` from `card_definitions`.
    - Update `src/app/cards/[set-code]/[card-number]/page.tsx`:
      - Fetch the pricing data.
      - Render "Price Chips" (e.g., using `Badge`) below the stats row.
      - Label them "Market (NM)".
      - Display the values formatted as currency (e.g., `€0.45`, `$0.50`).
      - Handle null/zero cases by displaying `—`.
  </action>
  <verify>
    <automated>grep -i "price" src/db/queries/card-detail.ts</automated>
  </verify>
  <done>Card detail page now displays real-world prices.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → LocalStorage | User currency preference stored in browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Tampering | LocalStorage | accept | Malicious currency choice only affects the local user's UI experience |
</threat_model>

<verification>
- Switching currency in the TopBar is reflected in the global state.
- Card detail page shows both EUR and USD prices correctly formatted.
</verification>

<success_criteria>
User can switch currencies and see card values on individual card pages.
</success_criteria>

<output>
After completion, create `.planning/phases/07-market-pricing/07-03-SUMMARY.md`
</output>
