---
phase: 14-trade-binder-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/binder/[username]/page.tsx
autonomous: true
requirements:
  - REQ-TRADE-06
  - REQ-TRADE-07

must_haves:
  truths:
    - "Visiting /binder/[username] renders PublicBinderClient edge-to-edge (no centered container constraining it)"
    - "PublicBinderClient's full-height flex layout fills the viewport as intended"
  artifacts:
    - path: "src/app/binder/[username]/page.tsx"
      provides: "Full-width public binder page"
      contains: "PublicBinderClient"
  key_links:
    - from: "src/app/binder/[username]/page.tsx"
      to: "src/components/binder/public-binder-client.tsx"
      via: "direct return, no wrapper div"
      pattern: "return\\s*\\("
---

<objective>
Remove the `<div className="container mx-auto">` wrapper from the public binder page so `PublicBinderClient` fills the viewport edge-to-edge, matching the full-width pattern already used by the catalog page.

Purpose: The `container mx-auto` wrapper constrains `PublicBinderClient` to a centered column even though the component already has its own internal padding (`px-4 lg:px-8`) and a full-height flex layout (`flex flex-col h-screen overflow-hidden`). Removing the wrapper lets the existing component design work as intended (D-01, D-02, per REQ-TRADE-06 and REQ-TRADE-07).

Output: Updated `src/app/binder/[username]/page.tsx` where the JSX return wraps `<PublicBinderClient ... />` directly with no container div.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove container wrapper from public binder page</name>
  <files>src/app/binder/[username]/page.tsx</files>

  <read_first>
    - src/app/binder/[username]/page.tsx — read the current file before editing; lines 58-67 contain the wrapper to remove
    - src/app/cards/page.tsx — the full-width analog: CatalogClient is returned bare with no container div
  </read_first>

  <action>
Edit `src/app/binder/[username]/page.tsx` lines 58-67.

CURRENT (lines 58-67):
```tsx
return (
  <div className="container mx-auto">
    <PublicBinderClient
      username={username}
      offerings={offerings}
      lookingFor={lookingFor}
      filterOptions={filterOptions}
    />
  </div>
);
```

REPLACE WITH:
```tsx
return (
  <PublicBinderClient
    username={username}
    offerings={offerings}
    lookingFor={lookingFor}
    filterOptions={filterOptions}
  />
);
```

That is the only change. Do not touch any imports, logic, or other JSX. The `container mx-auto` div and its closing tag are simply deleted.
  </action>

  <verify>
    <automated>cd /c/Users/alan_/source/repos/galacticamaru/star-wars-unlimited-tracker && grep -n "container mx-auto" src/app/binder/\[username\]/page.tsx; echo "exit:$?"</automated>
  </verify>

  <acceptance_criteria>
    - `grep "container mx-auto" src/app/binder/\[username\]/page.tsx` returns no matches (exit 1)
    - The file still contains `PublicBinderClient` (the component is still rendered)
    - `grep -c "PublicBinderClient" src/app/binder/\[username\]/page.tsx` returns at least 2 (import + JSX usage)
    - `npm run build` exits 0 (TypeScript and Next.js compilation passes)
  </acceptance_criteria>

  <done>The public binder page returns PublicBinderClient directly with no wrapping div. grep for "container mx-auto" in the file finds zero matches. Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL param → server | `username` is a dynamic route segment; already handled by `getUserIdByUsername` null-check and `notFound()` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-01 | Information Disclosure | public binder page | accept | Page is intentionally public; no PII beyond what the user chose to expose. Username in URL is user-selected. |
| T-14-01-02 | Spoofing | username route param | mitigate | `getUserIdByUsername()` already maps username → userId; non-existent usernames return `notFound()`. No change in attack surface from this layout-only edit. |
</threat_model>

<verification>
- `grep "container mx-auto" src/app/binder/\[username\]/page.tsx` — must return no matches
- `grep "PublicBinderClient" src/app/binder/\[username\]/page.tsx` — must appear (import + JSX)
- `npm run build` exits 0
</verification>

<success_criteria>
Public binder page returns PublicBinderClient bare, no container wrapper. Build passes. The public binder at /binder/[username] renders full-width.
</success_criteria>

<output>
After completion, create `.planning/phases/14-trade-binder-polish/14-01-SUMMARY.md` using the summary template.
</output>
