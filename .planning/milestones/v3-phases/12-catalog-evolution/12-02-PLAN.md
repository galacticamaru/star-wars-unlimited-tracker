---
phase: 12-catalog-evolution
plan: 02
type: execute
wave: 2
depends_on:
  - "12-01"
files_modified:
  - src/components/catalog/sidebar-filters.tsx
  - src/components/catalog/variant-filter.tsx
  - src/components/catalog/mobile-filter-sheet.tsx
autonomous: true
requirements:
  - REQ-CAT-02

must_haves:
  truths:
    - "A sticky sidebar component for filters exists."
    - "A specialized component for filtering by card variant exists."
    - "A mobile-only sheet component for displaying filters exists."
  artifacts:
    - path: "src/components/catalog/sidebar-filters.tsx"
      provides: "The main container for all catalog filter controls."
      contains: "<aside"
    - path: "src/components/catalog/variant-filter.tsx"
      provides: "UI for selecting card variants."
      contains: "VariantFilter"
    - path: "src/components/catalog/mobile-filter-sheet.tsx"
      provides: "A slide-out panel for mobile filtering."
      contains: "<Sheet"
  key_links:
    - from: "src/components/catalog/sidebar-filters.tsx"
      to: "src/components/catalog/variant-filter.tsx"
      via: "Component import and composition."
      pattern: "<VariantFilter"
---

<objective>
This plan creates the new, but currently un-wired, React components required for the new catalog layout. This includes the main sidebar, a specialized variant filter, and the mobile filter drawer.

Purpose: To build the UI scaffolding in isolation before integrating it into the main application layout.
Output: Three new client-side React components.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-CONTEXT.md
@.planning/phases/12-catalog-evolution/12-UI-SPEC.md
@.planning/phases/12-catalog-evolution/12-PATTERNS.md
@.planning/phases/12-catalog-evolution/12-RESEARCH.md
@src/components/decks/deck-sidebar.tsx
@src/components/catalog/top-bar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SidebarFilters Component</name>
  <files>
    - src/components/catalog/sidebar-filters.tsx
  </files>
  <action>
    Create the main sidebar component that will house all filter controls.

    1.  Create a new file `src/components/catalog/sidebar-filters.tsx`.
    2.  Use the `deck-sidebar.tsx` as a structural analog, as suggested in `12-PATTERNS.md`.
    3.  Implement the layout based on `12-UI-SPEC.md` and `12-RESEARCH.md`'s recommendation:
        - Use an `<aside>` tag for semantic correctness.
        - Apply classes for width, border, and padding: `className="w-64 border-r p-4 shrink-0"`
        - The parent container will handle `h-screen` and `overflow-y-auto`, but for now, ensure the component is a single vertical column (`flex flex-col gap-4`).
    4.  Move the existing filter controls (Search input, Set filter, Aspect filter, etc.) from `src/components/catalog/top-bar.tsx` into this new component. For now, props can be stubbed as they will be wired in Plan 3.
    5.  Include a placeholder for the `VariantFilter` component.
  </action>
  <verify>
    <automated>test -f src/components/catalog/sidebar-filters.tsx && grep -q "<aside" src/components/catalog/sidebar-filters.tsx</automated>
  </verify>
  <done>
    - The `sidebar-filters.tsx` file is created.
    - It uses an `<aside>` element and contains placeholders for the filter controls.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create VariantFilter Component</name>
  <files>
    - src/components/catalog/variant-filter.tsx
  </files>
  <action>
    Create a new component specifically for selecting card variants.

    1.  Create `src/components/catalog/variant-filter.tsx`.
    2.  This component will manage the state for variant selection. It should use a `Checkbox` group or similar multi-select UI.
    3.  The filter options should be `Normal`, `Showcase`, `Hyperspace`, `Prestige`, `Serialized` as a starting point, based on `12-UI-SPEC.md`.
    4.  The component should accept `value` and `onChange` props for its state, which will be managed by `nuqs` in the parent component.
  </action>
  <verify>
    <automated>test -f src/components/catalog/variant-filter.tsx && grep -q "VariantFilter" src/components/catalog/variant-filter.tsx</automated>
  </verify>
  <done>
    - `variant-filter.tsx` is created and exports a `VariantFilter` component.
    - The component includes UI elements for selecting different card variants.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create MobileFilterSheet Component</name>
  <files>
    - src/components/catalog/mobile-filter-sheet.tsx
  </files>
  <action>
    Create the mobile drawer for filters using `shadcn/ui`'s `Sheet` component. This is a new pattern for the project.

    1.  Create `src/components/catalog/mobile-filter-sheet.tsx`.
    2.  Use the `Sheet` component from `shadcn/ui`. You will need `Sheet`, `SheetTrigger`, `SheetContent`, `SheetHeader`, `SheetTitle`, and `SheetDescription`.
    3.  The `SheetContent` should be configured to slide in from the right (`side="right"`).
    4.  The content of the sheet will be the `<SidebarFilters />` component itself. This ensures filter UI is consistent between desktop and mobile.
    5.  The `SheetTrigger` will be a button, as specified in the `12-UI-SPEC.md` (`[Filters Button]`).
  </action>
  <verify>
    <automated>test -f src/components/catalog/mobile-filter-sheet.tsx && grep -q "<Sheet" src/components/catalog/mobile-filter-sheet.tsx</automated>
  </verify>
  <done>
    - `mobile-filter-sheet.tsx` is created.
    - It correctly uses the `shadcn/ui Sheet` component to wrap the `SidebarFilters` component for mobile display.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
This plan creates UI components and involves no direct data processing or external communication, so there are no new trust boundaries.

## STRIDE Threat Register
No new threats are introduced in this plan as it only involves the creation of non-wired UI components. The components are scaffolds and do not yet handle user input in a way that could be exploited.

</threat_model>

<verification>
- All three new component files exist at their specified paths.
- The components are client components (`'use client'`).
- The components can be rendered in isolation (e.g. in Storybook or a test page) without errors, though they will not be functional yet.
</verification>

<success_criteria>
- The UI building blocks for the new catalog experience are created and ready for integration.
- Component structure follows the guidance from `12-PATTERNS.md` and `12-UI-SPEC.md`.
</success_criteria>

<output>
After completion, create `.planning/phases/12-catalog-evolution/12-02-SUMMARY.md`
</output>
