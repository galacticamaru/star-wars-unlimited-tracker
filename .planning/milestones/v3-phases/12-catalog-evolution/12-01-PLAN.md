---
phase: 12-catalog-evolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/sync/upsert-cards.ts
  - src/db/queries/catalog.ts
  - src/lib/filter-cards.ts
  - __tests__/upsert-cards.test.ts
autonomous: true
requirements:
  - REQ-CAT-01
  - REQ-CAT-03

must_haves:
  truths:
    - "The `upsertCards` function no longer incorrectly blocks the 'TS26' set ID."
    - "The `getAllCards` database query can filter cards by their `variantType`."
    - "The client-side `filterCards` function correctly filters the card list based on selected variants."
    - "New variants like 'Prestige' from the API are correctly inserted into the database."
  artifacts:
    - path: "src/lib/sync/upsert-cards.ts"
      provides: "Corrected sync logic for 'T' set codes."
      contains: "if (setId.startsWith('T') && setId.length > 3 && !setId.match(/^TS\d{2}$/))"
    - path: "src/db/queries/catalog.ts"
      provides: "Variant filtering capability in the main catalog query."
      contains: "variantType: string[] | undefined"
    - path: "src/lib/filter-cards.ts"
      provides: "Client-side filtering logic for variants."
      contains: "matchesVariant"
  key_links:
    - from: "API sync process"
      to: "src/lib/sync/upsert-cards.ts"
      via: "The guard condition for set IDs starting with 'T'."
      pattern: "!setId.match"
    - from: "src/components/catalog/catalog-client.tsx"
      to: "src/db/queries/catalog.ts"
      via: "`getAllCards` function call with `variantType` parameter."
      pattern: "getAllCards({ ...variantType })"
---

<objective>
This plan updates the data layer and synchronization logic to support card variants (Showcase, Prestige, etc.) and fixes a critical bug preventing the Twin Suns (TS26) set from being imported.

Purpose: To lay the data and logic foundation for the new catalog filtering UI.
Output: Modified data-access and utility functions, with updated tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-CONTEXT.md
@.planning/phases/12-catalog-evolution/12-RESEARCH.md
@.planning/phases/12-catalog-evolution/12-PATTERNS.md
@src/lib/sync/upsert-cards.ts
@src/db/queries/catalog.ts
@src/lib/filter-cards.ts
@__tests__/upsert-cards.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix 'TS26' Sync Guard Bug</name>
  <files>
    - src/lib/sync/upsert-cards.ts
    - __tests__/upsert-cards.test.ts
  </files>
  <behavior>
    - A test case should be added to `__tests__/upsert-cards.test.ts` that simulates a sync for `setId: 'TS26'`.
    - This test should fail before the code change.
    - The test should verify that the function does *not* return 0 prematurely.
    - After the code change, the test should pass.
  </behavior>
  <action>
    Per RESEARCH.md (Pitfall 1), the current sync logic incorrectly blocks sets with IDs like `TS26`.

    1.  In `src/lib/sync/upsert-cards.ts`, locate the guard condition:
        `if (setId.startsWith('T') && setId.length > 3) return 0;`
    2.  Update it to allow the `TS##` pattern, as recommended in `12-RESEARCH.md`:
        `if (setId.startsWith('T') && setId.length > 3 && !setId.match(/^TS\d{2}$/)) return 0;`
    3.  In `__tests__/upsert-cards.test.ts`, add a new test case to confirm that `upsertCards` can be called with `setId: 'TS26'` and does not exit early.
  </action>
  <verify>
    <automated>npm test -- --testNamePattern="upsert-cards" passes</automated>
  </verify>
  <done>
    - The `upsertCards` function is updated to correctly handle the `TS26` set ID.
    - A test case exists to prevent regressions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update `getAllCards` Query for Variant Filtering</name>
  <files>
    - src/db/queries/catalog.ts
  </files>
  <action>
    The catalog query needs to be updated to accept and filter by an array of `variantType` strings.

    1.  Open `src/db/queries/catalog.ts`.
    2.  Locate the `getAllCards` function.
    3.  Update its parameter interface to accept an optional `variantType: string[]`.
    4.  Modify the Drizzle query:
        - If `variantType` is provided and not empty, add a `where` clause using `inArray(cardPrintings.variantType, variantType)`.
        - Per `12-CONTEXT.md`, the default behavior should be to show 'Normal' variants. Ensure that if `variantType` is `undefined` or empty, the query defaults to filtering `where eq(cardPrintings.variantType, 'Normal')`. This should be handled by the calling code (`catalog-client`) passing `['Normal']` by default, but the query should be robust.
  </action>
  <verify>
    <automated>grep -c "inArray(cardPrintings.variantType, variantType)" src/db/queries/catalog.ts</automated>
    <manual>The `getAllCards` function signature now includes `variantType` as a parameter.</manual>
  </verify>
  <done>
    - `getAllCards` can now filter the returned cards based on a list of `variantType` strings.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update `filterCards` for Client-Side Variant Filtering</name>
  <files>
    - src/lib/filter-cards.ts
  </files>
  <action>
    Update the client-side filtering logic to accommodate the new variant filter.

    1.  Open `src/lib/filter-cards.ts`.
    2.  Find the `filterCards` function.
    3.  Update the `FilterOptions` type to include `selectedVariants?: string[] | null`.
    4.  Add a new filtering condition for variants, using the pattern from `12-RESEARCH.md`:
        `const matchesVariant = !options.selectedVariants?.length || options.selectedVariants.includes(card.variantType);`
    5.  Ensure `matchesVariant` is included in the final `return` statement of the filter predicate.
  </action>
  <verify>
    <automated>grep -c "matchesVariant" src/lib/filter-cards.ts</automated>
  </verify>
  <done>
    - The `filterCards` function now correctly filters cards by variant type on the client side.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External API -> Sync | Data from `swu-db.com` is ingested into the system. |
| Client -> API | User-controlled filter parameters are sent to the backend. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | `getAllCards` query | Mitigate | The `getAllCards` query uses Drizzle's `inArray` which parameterizes inputs, preventing SQL injection from the `variantType` filter array. |
| T-12-02 | Information Disclosure | `upsertCards` | Accept | The `TS26` bug was an availability issue, not an information disclosure. The fix correctly surfaces public data. No new sensitive data is handled. |
</threat_model>

<verification>
- After this plan, running the `db:sync` command with `TS26` should successfully add cards to the database.
- Manually querying the database should show `card_printings` with `variant_type` values other than 'Normal' if they exist in the sync source.
</verification>

<success_criteria>
- The database schema and queries are prepared for the UI changes in the next plans.
- The `TS26` set can be synced without error.
- All variants (`Showcase`, `Prestige`, etc.) present in the API feed are correctly stored in the database.
</success_criteria>

<output>
After completion, create `.planning/phases/12-catalog-evolution/12-01-SUMMARY.md`
</output>
