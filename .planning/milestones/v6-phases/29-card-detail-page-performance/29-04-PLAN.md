---
phase: 29-card-detail-page-performance
plan: 04
type: execute
wave: 2
depends_on: ["29-01", "29-02", "29-03"]
files_modified:
  - .planning/phases/29-card-detail-page-performance/29-04-SPEED-INSIGHTS.md
autonomous: false
requirements: [PERF-10]
must_haves:
  truths:
    - "The user has reviewed Vercel Speed Insights for /cards/[set]/[id] FCP, LCP, and INP"
    - "Either specific regressions are recorded for Wave 2b to fix, OR insufficient-data is recorded and the phase closes after Wave 1"
  artifacts:
    - path: ".planning/phases/29-card-detail-page-performance/29-04-SPEED-INSIGHTS.md"
      provides: "Recorded Speed Insights findings or insufficient-data determination"
      contains: "## Findings"
  key_links:
    - from: ".planning/phases/29-card-detail-page-performance/29-04-SPEED-INSIGHTS.md"
      to: "29-05-PLAN.md (Wave 2b)"
      via: "findings drive targeted fixes"
      pattern: "Findings"
---

<objective>
Pause for the user to review the Vercel Speed Insights dashboard for the `/cards/[set]/[id]` route and report specific FCP, LCP, and INP regressions (D-11 Wave 2a). The proactive fixes from Wave 1 (plans 01-03) are already shipped; this checkpoint determines whether any data-driven targeted fixes are still needed. If Speed Insights shows insufficient data, the phase closes after Wave 1 with no speculative fixes (D-12).

Purpose: PERF-10 requires that real-user regressions are *identified via Speed Insights* and each identified regression is fixed. This checkpoint captures the identification step; no speculative INP work is done without data.
Output: `29-04-SPEED-INSIGHTS.md` recording the user's findings (specific regressions) or an insufficient-data determination.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-card-detail-page-performance/29-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: User reviews Speed Insights and reports /cards/[set]/[id] regressions</name>
  <files>.planning/phases/29-card-detail-page-performance/29-04-SPEED-INSIGHTS.md</files>
  <read_first>
    - .planning/phases/29-card-detail-page-performance/29-CONTEXT.md (D-11 Wave 2a checkpoint intent; D-12 insufficient-data fallback)
    - .planning/phases/29-card-detail-page-performance/29-01-SUMMARY.md, 29-02-SUMMARY.md, 29-03-SUMMARY.md (what Wave 1 shipped, to frame the dashboard review)
  </read_first>
  <what-built>
    Wave 1 shipped three proactive performance fixes for the card detail page:
    - Public card definition query is now cached on the 'cards' tag; per-user printings query is cached per-user (TTFB/LCP).
    - A full two-column loading.tsx skeleton paints immediately on navigation (FCP/LCP).
    - The above-fold card image now uses the correct Next.js `priority` prop (LCP).
  </what-built>
  <how-to-verify>
    1. Open the Vercel dashboard for this project and go to the Speed Insights tab.
    2. Filter / select the route path `/cards/[set-code]/[card-number]` (the card detail dynamic route).
    3. Review the three Core Web Vitals for this route specifically:
       - FCP (First Contentful Paint)
       - LCP (Largest Contentful Paint)
       - INP (Interaction to Next Paint)
    4. For each metric, note the p75 value and whether it is rated Good / Needs Improvement / Poor.
    5. Identify the specific LCP element if the dashboard reports one (e.g., the card image, a text block), and any interaction that drives a poor INP (e.g., the variant +/- buttons, the leader flip toggle).
    6. Report back EITHER:
       (a) Specific regressions to fix — for each: the metric (FCP/LCP/INP), the p75 value, the rating, and the element/interaction responsible. Example: "INP poor at 320ms, driven by the variant +/- buttons" or "LCP needs-improvement at 3.1s, LCP element is the card image".
       OR
       (b) "Insufficient data" — if the route has too few samples (typical for a recently deployed route) or no Poor/Needs-Improvement metrics. In this case the phase closes after Wave 1 per D-12; no speculative fixes will be applied.
    The executor will record your verbatim findings into 29-04-SPEED-INSIGHTS.md under a `## Findings` heading, including a `Decision:` line of either `targeted-fixes-needed` (with the list) or `insufficient-data-close-after-wave-1`.
  </how-to-verify>
  <action>
    This is a human-verification checkpoint. The executor does NOT modify source code in this plan. After the user reports their Speed Insights findings, the executor writes them verbatim into `.planning/phases/29-card-detail-page-performance/29-04-SPEED-INSIGHTS.md` under a `## Findings` heading, and appends a `Decision:` line that is exactly one of `targeted-fixes-needed` (followed by the itemized regression list) or `insufficient-data-close-after-wave-1`. This recorded decision is the sole input that drives plan 29-05 (Wave 2b).
  </action>
  <resume-signal>
    Provide your findings as described, then type "approved" to continue. If you report "insufficient data", the executor will record the close-after-Wave-1 determination and the phase ends.
  </resume-signal>
  <verify>
    <human-check>User has reviewed the Speed Insights dashboard for /cards/[set-code]/[card-number] and reported either specific regressions or an insufficient-data determination.</human-check>
  </verify>
  <acceptance_criteria>
    - 29-04-SPEED-INSIGHTS.md exists and contains a `## Findings` heading
    - 29-04-SPEED-INSIGHTS.md contains a `Decision:` line equal to exactly one of `targeted-fixes-needed` or `insufficient-data-close-after-wave-1`
    - If the decision is `targeted-fixes-needed`, the Findings section lists each regression with its metric, p75 value, rating, and responsible element/interaction
  </acceptance_criteria>
  <done>The user's Speed Insights findings (or insufficient-data determination) are recorded with a machine-readable Decision line for Wave 2b.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → planning record | User-reported metrics are recorded verbatim; no code or execution path crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-09 | Tampering | 29-04-SPEED-INSIGHTS.md record | accept | Documentation-only artifact; no executable code, no external input processed — recording user findings has no attack surface |
| T-29-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — no dependency surface added |
</threat_model>

<verification>
- 29-04-SPEED-INSIGHTS.md exists with a `## Findings` section and a `Decision:` line
- The Decision line is one of: `targeted-fixes-needed` or `insufficient-data-close-after-wave-1`
</verification>

<success_criteria>
- User has reviewed Speed Insights for the card detail route
- Findings (or insufficient-data determination) are recorded to drive or close out Wave 2b
</success_criteria>

<output>
Create `.planning/phases/29-card-detail-page-performance/29-04-SUMMARY.md` when done, and write the findings to `.planning/phases/29-card-detail-page-performance/29-04-SPEED-INSIGHTS.md`
</output>
