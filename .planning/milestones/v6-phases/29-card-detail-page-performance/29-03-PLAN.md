---
phase: 29-card-detail-page-performance
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/cards/[set-code]/[card-number]/loading.tsx
  - src/components/catalog/card-image-section.tsx
autonomous: true
requirements: [PERF-10]
must_haves:
  truths:
    - "Navigating to a card detail URL shows a full two-column skeleton immediately, before DB queries resolve"
    - "The skeleton image placeholder matches CardImageSection container dimensions (md:w-[320px] aspect-[2/3])"
    - "The above-fold card image renders with the Next.js priority prop and no invalid preload prop or @ts-ignore"
  artifacts:
    - path: "src/app/cards/[set-code]/[card-number]/loading.tsx"
      provides: "Route-segment loading skeleton for the card detail page"
      contains: "animate-pulse"
    - path: "src/components/catalog/card-image-section.tsx"
      provides: "Image with priority prop for above-fold LCP"
      contains: "priority"
  key_links:
    - from: "src/app/cards/[set-code]/[card-number]/loading.tsx"
      to: "card detail route segment"
      via: "Next.js loading.tsx file convention"
      pattern: "export default function"
    - from: "src/components/catalog/card-image-section.tsx"
      to: "Next.js Image priority"
      via: "priority boolean prop"
      pattern: "priority"
---

<objective>
Add a route-segment `loading.tsx` skeleton for the card detail page (D-06, D-07, D-08) and replace the invalid `preload={true}` prop with the correct Next.js `priority` prop on the above-fold card image (D-09, D-10). These are the FCP/LCP improvements for PERF-10.

Purpose: The card detail route currently shows nothing until DB queries resolve (poor FCP), and the LCP image is not prioritized because `preload` is not a real Next.js Image prop. A full skeleton paints instantly and `priority` adds the correct `<link rel="preload">` for the hero image.
Output: New loading.tsx skeleton; card-image-section.tsx with `priority` replacing the `@ts-ignore` + `preload` prop.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-card-detail-page-performance/29-CONTEXT.md
@.planning/phases/29-card-detail-page-performance/29-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create the card detail route loading skeleton</name>
  <files>src/app/cards/[set-code]/[card-number]/loading.tsx</files>
  <read_first>
    - src/app/decks/[id]/loading.tsx (reference skeleton — animate-pulse, bg-slate-200 placeholder divs, aspect-[2/3] image placeholder pattern)
    - src/app/cards/[set-code]/[card-number]/page.tsx (the real page structure being mirrored: outer `max-w-5xl mx-auto px-4 py-12`, two-column `flex flex-col gap-8 md:flex-row md:gap-12`, left image column, right metadata column, and the back-button Link)
    - src/components/catalog/card-image-section.tsx (image container dimensions to mirror: `w-full md:w-[320px] md:flex-shrink-0` + `aspect-[2/3]`)
    - .planning/phases/29-card-detail-page-performance/29-PATTERNS.md (the full skeleton structure to produce — see "loading.tsx — CREATE new file" section)
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/loading.md (loading.tsx file convention — default-exported React component, no props, no client directive)
  </read_first>
  <action>
    Create `src/app/cards/[set-code]/[card-number]/loading.tsx` as a default-exported server component named `CardDetailLoading` with no imports beyond what JSX needs (no `'use client'`, no React import unless required by the project's JSX setup). Structure, using `bg-slate-200` placeholder divs to match the decks skeleton palette:
    - Outer container `div` with `max-w-5xl mx-auto px-4 py-12`.
    - A back-button placeholder: `h-9 w-32 bg-slate-200 rounded-md mb-6 animate-pulse`.
    - A two-column row `div` with `flex flex-col gap-8 md:flex-row md:gap-12 animate-pulse`.
    - Left image placeholder: `div` with `w-full md:w-[320px] md:flex-shrink-0 aspect-[2/3] bg-slate-200 rounded-lg` (matches CardImageSection container).
    - Right metadata column `div` with `flex-1 flex flex-col gap-4` containing: title+subtitle block (`h-6 w-48` and `h-4 w-32`), a badge row (3 pills `h-5 w-16`/`w-20`), a stat-chip row (3 chips `h-10 w-12 rounded-md`), a text box (`h-24 w-full rounded-md`), and a footer metadata block (`mt-auto pt-3 border-t border-border` with 4 lines `h-3 w-40`/`w-32`/`w-36`). All placeholder divs use `bg-slate-200 rounded` (or `rounded-md`).
    Cover above-fold content only (image + metadata column) — do NOT add placeholder rows for VariantCollectionSection or VariantTradeSection (D-08, below the fold). Mirror the exact skeleton in PATTERNS.md "Skeleton component structure".
  </action>
  <verify>
    <automated>Test-Path "src/app/cards/[set-code]/[card-number]/loading.tsx" ; npm run build 2>&1 | Select-String -Pattern "Compiled successfully|error" | Select-Object -First 5</automated>
  </verify>
  <acceptance_criteria>
    - File src/app/cards/[set-code]/[card-number]/loading.tsx exists and default-exports a function component
    - loading.tsx contains `animate-pulse`
    - loading.tsx contains an image placeholder with `md:w-[320px]` and `aspect-[2/3]` and `bg-slate-200`
    - loading.tsx contains `max-w-5xl mx-auto px-4 py-12` (matches real page container)
    - loading.tsx does NOT contain `'use client'`
    - loading.tsx does NOT reference VariantCollectionSection or VariantTradeSection (above-fold only)
    - `npm run build` completes with "Compiled successfully" and exits 0
  </acceptance_criteria>
  <done>The card detail route paints a full two-column skeleton immediately on navigation, before data loads.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace preload prop with priority on the card image</name>
  <files>src/components/catalog/card-image-section.tsx</files>
  <read_first>
    - src/components/catalog/card-image-section.tsx (the `<Image>` element at lines 62-77 — the `@ts-ignore` comment on line 75 and `preload={true}` on line 76 must both be removed and replaced with `priority`)
    - .planning/phases/29-card-detail-page-performance/29-CONTEXT.md (D-09: priority is the correct Next.js Image prop for above-fold LCP; D-10: no blur placeholder needed — existing animate-pulse + opacity-0 transition covers perceived loading)
  </read_first>
  <action>
    In the `<Image>` element, remove BOTH the line `// @ts-ignore - custom attribute used in this project's Next.js 16 setup` and the line `preload={true}`, and add a bare `priority` boolean prop in their place. Leave every other prop unchanged: `src`, `alt`, `fill`, `sizes="(max-width: 768px) 100vw, 320px"`, `className`, `onLoad`, `onError`, and `key={displayUrl}`. Do not add a `placeholder` or `blurDataURL` prop (D-10). Do not touch the `displayUrl` logic, the leader flip button, or the no-image fallback branch — `priority` correctly applies only to this initial above-fold render.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | Select-String "card-image-section" ; npm run build 2>&1 | Select-String -Pattern "Compiled successfully|error" | Select-Object -First 5</automated>
  </verify>
  <acceptance_criteria>
    - card-image-section.tsx contains `priority` as a prop on the `<Image>` element
    - card-image-section.tsx does NOT contain `preload={true}`
    - card-image-section.tsx does NOT contain `@ts-ignore` (the suppression comment is removed)
    - card-image-section.tsx does NOT contain `blurDataURL` or `placeholder=` (D-10)
    - The `sizes="(max-width: 768px) 100vw, 320px"` prop is still present and unchanged
    - `npx tsc --noEmit` reports no error in card-image-section.tsx (the prop is now valid, no suppression needed)
    - `npm run build` completes with "Compiled successfully" and exits 0
  </acceptance_criteria>
  <done>The above-fold card image uses the standard `priority` prop with no type suppression; build is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → client render | loading.tsx is static markup with no data — no untrusted input crosses |
| image src → DOM | displayUrl originates from cached public card data (already trusted upstream) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-07 | Denial of Service | priority image preload | accept | `priority` adds one `<link rel="preload">` for a single above-fold image; bounded, no amplification — standard Next.js LCP pattern |
| T-29-08 | Information Disclosure | loading.tsx skeleton | accept | Skeleton is static placeholder markup with zero data interpolation — no PII or user data rendered |
| T-29-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan — no dependency surface added |
</threat_model>

<verification>
- `npm run build` exits 0 with "Compiled successfully"
- loading.tsx exists and contains animate-pulse + matching image dimensions
- card-image-section.tsx contains `priority` and no longer contains `preload` or `@ts-ignore`
</verification>

<success_criteria>
- Full two-column skeleton paints immediately on card detail navigation (FCP/LCP)
- Above-fold image prioritized via the correct Next.js prop (LCP)
- Build is green with no type suppression
</success_criteria>

<output>
Create `.planning/phases/29-card-detail-page-performance/29-03-SUMMARY.md` when done
</output>
