---
phase: 26-mobile-deck-builder-ux
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/components/decks/deck-builder.test.tsx
  - src/components/catalog/card-item.deck.test.tsx
autonomous: true
requirements: [MOBILE-01, MOBILE-02, MOBILE-03, MOBILE-04]
tags: [test-infrastructure, nyquist-wave-0, vitest, jsdom]

must_haves:
  truths:
    - "A RED vitest spec file `src/components/decks/deck-builder.test.tsx` exists with `it.todo` stubs covering MOBILE-01 (sticky summary bar present, Sheet not rendered initially), MOBILE-02 (deck list +/- buttons have h-11 w-11), MOBILE-03 (toolbar root has flex-col md:flex-row), and MOBILE-04 (inline DeckSidebar wrapper has hidden md:flex)"
    - "The existing `src/components/catalog/card-item.deck.test.tsx` is extended with at least one new test asserting catalog overlay +/- buttons have `min-h-[44px]` or `min-w-[44px]` class (per D-10) — RED until Plan 03 implementation lands"
    - "All Wave 0 stubs use `it.todo` (pending) — they MUST NOT pass before Wave 1 implementation"
    - "`npx vitest run src/components/decks/deck-builder.test.tsx` exits 0 (todo tests count as pending, not failed)"
    - "`npx vitest run src/components/catalog/card-item.deck.test.tsx` exits 0"
  artifacts:
    - path: "src/components/decks/deck-builder.test.tsx"
      provides: "MOBILE-01 / MOBILE-02 / MOBILE-03 / MOBILE-04 RED stub assertions"
      contains: "@vitest-environment jsdom"
    - path: "src/components/catalog/card-item.deck.test.tsx"
      provides: "MOBILE-02 catalog overlay touch-target stub"
      contains: "min-h-[44px]"
  key_links:
    - from: "src/components/decks/deck-builder.test.tsx"
      to: "src/components/decks/deck-builder.tsx"
      via: "import { DeckBuilder } from './deck-builder'"
      pattern: "from './deck-builder'"
    - from: "src/components/catalog/card-item.deck.test.tsx"
      to: "src/components/catalog/card-item.tsx"
      via: "existing import { CardItem } from './card-item'"
      pattern: "min-h-\\[44px\\]"
---

<objective>
Establish Wave 0 RED test stubs for Phase 26 so the Wave 1 implementation plans (02/03/04) have failing tests to turn GREEN. Per the Nyquist validation strategy in `26-VALIDATION.md`, every implementation task in this phase needs a corresponding test that proves the behavior.

Purpose: Catch regressions in `deck-builder.tsx` (the file edited by all three Wave 1 plans in parallel) by encoding the expected post-implementation DOM state as automated assertions. Without Wave 0 stubs, parallel edits to `deck-builder.tsx` by Plans 02/03/04 risk silent conflicts.

Output: One new test file (`src/components/decks/deck-builder.test.tsx`) covering MOBILE-01/02/03/04 DOM-class assertions, plus one new `it.todo` extension to the existing `src/components/catalog/card-item.deck.test.tsx` for MOBILE-02 on the catalog overlay buttons.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@AGENTS.md
@.planning/phases/26-mobile-deck-builder-ux/26-CONTEXT.md
@.planning/phases/26-mobile-deck-builder-ux/26-RESEARCH.md
@.planning/phases/26-mobile-deck-builder-ux/26-UI-SPEC.md
@.planning/phases/26-mobile-deck-builder-ux/26-VALIDATION.md
@src/components/decks/deck-builder.tsx
@src/components/catalog/card-item.deck.test.tsx
@src/components/catalog/card-item.tsx

<interfaces>
<!-- DeckBuilder current props (from src/components/decks/deck-builder.tsx lines 119-123) -->

DeckBuilderProps signature:
- initialDeck: DeckState — { id, name, leaderCardDefinitionId, baseCardDefinitionId, isDraft, cards: { cardDefinitionId, quantity, isSideboard }[] }
- allCards: Card[]
- filterOptions: { sets: string[]; types: string[] }

Card type (from src/lib/deck-validation.ts via existing imports): { id, name, type, cost, frontArtUrl, backArtUrl, subtitle, priceEur, priceUsd, ... }

<!-- Existing jsdom test pattern from src/components/catalog/card-item.deck.test.tsx — REUSE this style -->

The existing card-item.deck.test.tsx already establishes the working pattern:
- `// @vitest-environment jsdom` as the first line
- `vi.mock('lucide-react', () => ({ Plus: () => <div data-testid="plus-icon" />, Minus: () => <div data-testid="minus-icon" /> }))`
- `vi.mock('next/image', () => ({ default: ({ src, alt }) => <img src={src} alt={alt} /> }))`
- `vi.mock('next/link', () => ({ default: ({ children, href }) => <a href={href}>{children}</a> }))`

<!-- Wave 1 plans will turn these stubs GREEN: -->
- Plan 02 implements MOBILE-01 (sticky bar, Sheet, DeckSidebar in Sheet) and MOBILE-04 (hidden md:flex on inline sidebar, height fix)
- Plan 03 implements MOBILE-02 (h-11 w-11 on deck list +/-; min-h-[44px] on catalog overlay)
- Plan 04 implements MOBILE-03 (flex-col md:flex-row toolbar, short tab labels, icon-only Export/Back)

<!-- Why it.todo and NOT render() in Wave 0: -->
Until Wave 1 lands, the production component does not yet have the sticky bar, h-11 buttons, or flex-col toolbar. Calling render(<DeckBuilder />) and asserting these classes WOULD fail (RED). That is technically valid Wave 0, but using `it.todo` is simpler: it marks the test as pending without needing to set up minimal DeckBuilder render props (allCards, filterOptions, etc.) before the implementation exists. Wave 1 plans replace `it.todo(...)` with full `it(...)` bodies including `render(<DeckBuilder {...} />)` and class assertions.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/components/decks/deck-builder.test.tsx with five it.todo RED stubs covering MOBILE-01, MOBILE-02, MOBILE-03, MOBILE-04</name>
  <files>src/components/decks/deck-builder.test.tsx</files>
  <read_first>
    - src/components/decks/deck-builder.tsx (current component structure — confirms no sticky bar, no Sheet, no flex-col toolbar yet)
    - src/components/catalog/card-item.deck.test.tsx (jsdom env directive + vi.mock pattern — copy this exact style)
    - .planning/phases/26-mobile-deck-builder-ux/26-VALIDATION.md (Per-Task Verification Map — confirms this file covers MOBILE-01/02/03/04)
    - .planning/phases/26-mobile-deck-builder-ux/26-UI-SPEC.md (Interaction Contracts — exact class strings to assert)
    - .planning/phases/26-mobile-deck-builder-ux/26-CONTEXT.md (D-01 through D-12 — the specific Tailwind class strings each stub will eventually assert)
  </read_first>
  <behavior>
    The new test file MUST contain exactly these five `it.todo` stubs inside one top-level `describe('DeckBuilder mobile layout', () => { ... })` block (descriptions are the literal strings to use):
    - it.todo('MOBILE-01: renders sticky summary bar with class "md:hidden fixed bottom-0 left-0 right-0 h-14 z-50" on mobile') — per D-01
    - it.todo('MOBILE-01: Sheet is not visible initially (SheetContent absent from DOM until trigger tapped)') — per D-02
    - it.todo('MOBILE-02: deck list quantity +/- buttons render with class "h-11 w-11" (NOT h-8 w-8)') — per D-09, D-10
    - it.todo('MOBILE-03: toolbar root div has class "flex flex-col md:flex-row" (or equivalent two-row mobile layout)') — per D-06
    - it.todo('MOBILE-04: inline DeckSidebar wrapper has class "hidden md:flex" so sidebar is desktop-only inline') — per D-05
    The file MUST NOT import DeckBuilder, render(), or screen from @testing-library yet — `it.todo` does not execute bodies, so no React/jsx imports are required. Only `describe, it` from `vitest` are needed.
  </behavior>
  <action>Create new file `src/components/decks/deck-builder.test.tsx`. First line MUST be `// @vitest-environment jsdom` (matching the comment-style directive in `src/components/catalog/card-item.deck.test.tsx`). Add a header block comment naming this as a Wave 0 stub file covering MOBILE-01, MOBILE-02, MOBILE-03, MOBILE-04, citing `.planning/phases/26-mobile-deck-builder-ux/26-VALIDATION.md` as the source of the verification map. Import `describe, it` from `vitest`. Write one top-level `describe('DeckBuilder mobile layout', () => { ... })` block containing the five `it.todo(...)` calls with the exact description strings listed in `<behavior>` above. Do NOT add `render()` calls, mocks, or imports of DeckBuilder — those are added by Wave 1 plans when they convert each `it.todo` to a full `it` body. Do NOT add any non-todo tests — every test in this file is a Wave 0 pending stub.</action>
  <verify>
    <automated>npx vitest run src/components/decks/deck-builder.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/decks/deck-builder.test.tsx` exists.
    - First non-empty line of the file is `// @vitest-environment jsdom`.
    - File contains string `describe('DeckBuilder mobile layout'`.
    - File contains exactly five `it.todo(` occurrences (grep `it\.todo\(` returns 5 matches).
    - File contains the substrings `MOBILE-01`, `MOBILE-02`, `MOBILE-03`, `MOBILE-04` in the test descriptions.
    - File contains the substrings `md:hidden fixed bottom-0`, `h-11 w-11`, `flex flex-col md:flex-row`, `hidden md:flex` somewhere in the test descriptions (these are the class strings Wave 1 plans must produce).
    - `npx vitest run src/components/decks/deck-builder.test.tsx` exits 0 and reports 5 todo/pending tests, 0 failed, 0 passed.
    - File does NOT contain `render(` or any `import` from `@testing-library/react` (those are Wave 1 additions).
  </acceptance_criteria>
  <done>Wave 0 stub file is created, exits 0 under vitest, and contains five pending tests that Wave 1 plans 02/03/04 will convert to passing assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend src/components/catalog/card-item.deck.test.tsx with an it.todo stub for MOBILE-02 catalog overlay touch-target sizing</name>
  <files>src/components/catalog/card-item.deck.test.tsx</files>
  <read_first>
    - src/components/catalog/card-item.deck.test.tsx (current contents — preserves all existing tests verbatim, only adds new it.todo)
    - src/components/catalog/card-item.tsx (lines 138-166 — current overlay +/- button structure showing p-1 class on the selector-mode buttons that Plan 03 will upgrade)
    - .planning/phases/26-mobile-deck-builder-ux/26-CONTEXT.md (D-10 — the p-1 catalog overlay buttons become min-h-[44px] min-w-[44px])
    - .planning/phases/26-mobile-deck-builder-ux/26-UI-SPEC.md (Touch Targets section — exact class string for overlay buttons)
  </read_first>
  <behavior>
    The extension MUST add ONE new `it.todo` to the existing `describe('CardItem Shortfall Logic', ...)` block (or a new sibling describe — implementer's choice):
    - it.todo('MOBILE-02: selector-mode overlay +/- buttons have class min-h-[44px] min-w-[44px] (replaces p-1)') — per D-10
    The new stub MUST be a `it.todo`, not a passing `it(...)`. The four existing tests in this file MUST remain unchanged.
  </behavior>
  <action>Read the existing `src/components/catalog/card-item.deck.test.tsx` and add ONE new `it.todo('MOBILE-02: selector-mode overlay +/- buttons have class min-h-[44px] min-w-[44px] (replaces p-1)')` line. Place it inside the existing `describe('CardItem Shortfall Logic', () => { ... })` block as the final test, AFTER the existing `it('highlights border when shortfall exists', ...)` test. Do NOT modify the four existing tests. Do NOT change any imports. Do NOT add a new describe block. The description string MUST include both the requirement id `MOBILE-02` and the literal Tailwind class strings `min-h-[44px]` and `min-w-[44px]` so Plan 03's implementation can grep for it.</action>
  <verify>
    <automated>npx vitest run src/components/catalog/card-item.deck.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/catalog/card-item.deck.test.tsx` contains the exact substring `MOBILE-02: selector-mode overlay +/- buttons have class min-h-[44px] min-w-[44px]`.
    - File contains exactly one `it.todo(` occurrence.
    - The four existing `it(...)` test descriptions are unchanged: `does not show shortfall in catalog mode`, `shows shortfall in selector mode when deckCount > ownedCount`, `does not show shortfall in selector mode when deckCount <= ownedCount`, `highlights border when shortfall exists`.
    - `npx vitest run src/components/catalog/card-item.deck.test.tsx` exits 0; vitest reports 4 passed and 1 todo/pending.
  </acceptance_criteria>
  <done>The existing test file gains a single MOBILE-02 `it.todo` stub without breaking the four existing tests; Plan 03 will convert this todo to a full `it(...)` body once the production class change lands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Phase 26 is a pure frontend layout/CSS change. Wave 0 only creates test stubs. No trust boundary crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | Test stub may pass silently if Wave 1 implementation is missing | mitigate | Use `it.todo` exclusively in Wave 0; `it.todo` shows as pending in vitest reporter — cannot accidentally report GREEN before implementation |
| T-26-02 | Information Disclosure | n/a | accept | No user data, no auth, no network — pure DOM class assertions on local React renders |
</threat_model>

<verification>
- `npx vitest run src/components/decks/deck-builder.test.tsx` exits 0 with 5 pending tests, 0 failures
- `npx vitest run src/components/catalog/card-item.deck.test.tsx` exits 0 with 4 passed and 1 pending
- `npx vitest run` (full suite) exits 0 — no other tests broken by adding the new file
- File `src/components/decks/deck-builder.test.tsx` does NOT contain `render(` (proves it stays at todo level)
</verification>

<success_criteria>
- Wave 0 stub file exists at `src/components/decks/deck-builder.test.tsx`
- Existing `src/components/catalog/card-item.deck.test.tsx` has exactly one new `it.todo` stub for MOBILE-02 catalog overlay
- All Wave 1 plans (02, 03, 04) have a corresponding RED stub to turn GREEN
- No existing tests broken
- Full vitest suite exits 0
</success_criteria>

<output>
Create `.planning/phases/26-mobile-deck-builder-ux/26-01-SUMMARY.md` when done.
</output>
