---
phase: 26-mobile-deck-builder-ux
plan: 05
type: execute
wave: 2
depends_on: ["26-02", "26-03", "26-04"]
files_modified: []
autonomous: true
requirements: [MOBILE-01, MOBILE-02, MOBILE-03, MOBILE-04]
tags: [build-gate, integration, verification]

must_haves:
  truths:
    - "Full vitest suite exits 0 with 0 failed tests after all Wave 1 plans complete"
    - "`npx tsc --noEmit` exits 0 — no TypeScript errors introduced by Phase 26 changes"
    - "`npm run build` exits 0 — production build succeeds after all Phase 26 changes"
    - "All five deck-builder.test.tsx tests pass (MOBILE-01 x2, MOBILE-02 deck list, MOBILE-03, MOBILE-04)"
    - "All five card-item.deck.test.tsx tests pass (4 original + 1 new MOBILE-02 catalog overlay)"
  artifacts: []
  key_links: []
---

<objective>
Final integration gate for Phase 26. After all three Wave 1 plans have executed (Plan 02: Sheet/sticky bar, Plan 03: touch targets, Plan 04: toolbar), confirm that the full test suite is green, there are no TypeScript errors, and the production build succeeds.

This plan makes no code changes. It only validates that the combined Wave 1 changes are internally consistent and production-ready.

Purpose: Wave 1 plans execute in parallel and each edits overlapping sections of `deck-builder.tsx`. This gate catches any merge conflict or leftover `it.todo` that may have been missed.

Output: A green build confirming Phase 26 is ready for `/gsd-verify-work`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-mobile-deck-builder-ux/26-CONTEXT.md
@.planning/phases/26-mobile-deck-builder-ux/26-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite, TypeScript check, and production build; confirm Phase 26 success criteria</name>
  <files></files>
  <read_first>
    - src/components/decks/deck-builder.tsx (current state — verify all five requirement areas implemented: height fix, Sheet+sticky bar, touch targets, toolbar, sidebar visibility)
    - src/components/decks/deck-builder.test.tsx (current state — verify 0 it.todo remain, 5 tests pass)
    - src/components/catalog/card-item.deck.test.tsx (current state — verify 0 it.todo remain, 5 tests pass)
    - src/components/decks/deck-sidebar.tsx (current state — verify root class is `h-auto md:h-full`)
    - src/components/catalog/card-item.tsx (current state — verify 2 occurrences of `min-h-[44px] min-w-[44px]` on selector overlay buttons)
  </read_first>
  <behavior>
    Run these commands in order and report the result of each:
    1. `npx vitest run` — full suite must exit 0
    2. `npx tsc --noEmit` — must exit 0
    3. `npm run build` — must exit 0

    If any command fails, read the error output, identify which Wave 1 change caused the failure, and fix it before re-running.

    After all three pass, perform a final source-level check:
    - Grep `it\.todo\(` in `src/components/decks/deck-builder.test.tsx` — must return 0 matches
    - Grep `it\.todo\(` in `src/components/catalog/card-item.deck.test.tsx` — must return 0 matches
    - Grep `h-\[calc\(100dvh-56px\)\]` in `deck-builder.tsx` — must return ≥1 match (height fix from Plan 02)
    - Grep `md:hidden fixed bottom-0 left-0 right-0 h-14` in `deck-builder.tsx` — must return ≥1 match (sticky bar from Plan 02)
    - Grep `hidden md:flex` in `deck-builder.tsx` — must return ≥2 matches (inline sidebar wrapper + desktop tab group)
    - Grep `h-11 w-11` in `deck-builder.tsx` — must return ≥4 matches (deck list +/- buttons)
    - Grep `flex flex-col md:flex-row` in `deck-builder.tsx` — must return 1 match (toolbar)
    - Grep `min-h-\[44px\]` in `card-item.tsx` — must return 2 matches (selector overlay buttons)
    - Grep `h-auto md:h-full` in `deck-sidebar.tsx` — must return 1 match
  </behavior>
  <action>
    Run commands sequentially:
    1. `npx vitest run` — if fails, read errors, identify the file/test, fix the specific issue (do NOT skip or suppress tests).
    2. `npx tsc --noEmit` — if fails, read the TypeScript error, fix the type issue in the indicated file.
    3. `npm run build` — if fails, read the build error, fix the indicated issue.
    Perform the source-level grep checks listed in `<behavior>`. Report all results. If any grep returns unexpected counts, investigate and fix the discrepancy before declaring the plan complete.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0 — full suite green.
    - `npx tsc --noEmit` exits 0 — no TypeScript errors.
    - `npm run build` exits 0 — production build clean.
    - Grep `it\.todo\(` in `deck-builder.test.tsx` returns 0 matches.
    - Grep `it\.todo\(` in `card-item.deck.test.tsx` returns 0 matches.
    - Grep `h-\[calc\(100dvh-56px\)\]` in `deck-builder.tsx` returns ≥1 match.
    - Grep `md:hidden fixed bottom-0` in `deck-builder.tsx` returns ≥1 match.
    - Grep `h-11 w-11` in `deck-builder.tsx` returns ≥4 matches.
    - Grep `flex flex-col md:flex-row` in `deck-builder.tsx` returns 1 match.
    - Grep `min-h-\[44px\]` in `card-item.tsx` returns 2 matches.
    - Grep `h-auto md:h-full` in `deck-sidebar.tsx` returns 1 match.
  </acceptance_criteria>
  <done>Full test suite, TypeScript, and production build all green. All Phase 26 acceptance criteria confirmed in source. Phase 26 is ready for `/gsd-verify-work`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Build gate only — no code changes, no trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-GATE | Tampering | Silent test skip masking a real failure | mitigate | Plan explicitly checks for 0 `it.todo` remaining — no pending stub can be misread as a pass |
</threat_model>

<verification>
- `npx vitest run` exits 0
- `npx tsc --noEmit` exits 0
- `npm run build` exits 0
</verification>

<success_criteria>
- All Phase 26 requirements implemented and verified in source
- Full vitest suite green, TypeScript clean, build clean
- No `it.todo` stubs remain in test files
- Phase 26 ready for `/gsd-verify-work`
</success_criteria>

<output>
Create `.planning/phases/26-mobile-deck-builder-ux/26-05-SUMMARY.md` when done.
</output>
