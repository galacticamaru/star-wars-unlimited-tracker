---
phase: 27-decks-route-performance
plan: 03
type: execute
wave: 2
depends_on: ["27-01", "27-02"]
files_modified:
  - src/app/decks/[id]/loading.tsx
  - src/app/decks/[id]/page.tsx
autonomous: false
requirements: [PERF-09]
must_haves:
  truths:
    - "The Vercel Speed Insights dashboard has been reviewed for /decks and /decks/[id] FCP/LCP/INP and specific findings (or 'insufficient data') are recorded in the SUMMARY"
    - "Every identified regression has a corresponding fix applied in this phase, OR the documented fallback (catalog-parity improvements) is applied when no data is available"
  artifacts:
    - path: ".planning/phases/27-decks-route-performance/27-03-SUMMARY.md"
      provides: "Recorded Speed Insights findings (per-metric) and the fix-or-fallback decision (PERF-09, D-09/D-10)"
      contains: "Speed Insights"
  key_links:
    - from: "Speed Insights findings"
      to: "applied fix in this phase"
      via: "each identified regression maps to a concrete code change or to the documented fallback"
      pattern: "FCP|LCP|INP"
---

<objective>
Implement PERF-09: review the Vercel Speed Insights dashboard for the /decks and /decks/[id] routes (FCP, LCP, INP), record specific findings, and apply a targeted fix for each identified regression. This plan is gated behind a mandatory human checkpoint (D-09) because dashboard access requires a human login. If Speed Insights shows insufficient /decks traffic to surface actionable data, the executor applies the documented fallback (D-10): catalog-parity improvements — verify/strengthen the Suspense `loading.tsx` skeleton and add image lazy-loading where applicable on /decks routes.

Purpose: Close the data-driven half of Phase 27. PERF-07 and PERF-08 fix known issues; PERF-09 catches any regression the dashboard reveals after those land.
Output: Recorded findings in the SUMMARY plus either targeted fixes per finding or the applied fallback improvements, with a clear note on which path was taken.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-decks-route-performance/27-RESEARCH.md
@.planning/phases/27-decks-route-performance/27-CONTEXT.md
@.planning/phases/27-decks-route-performance/27-VALIDATION.md
@src/app/decks/[id]/loading.tsx
@src/app/decks/[id]/page.tsx
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Human reviews Vercel Speed Insights for /decks routes</name>
  <what-built>
    PERF-07 (per-user cache tagging + invalidation) and PERF-08 (startTransition INP fix) are implemented in Plans 01 and 02. This checkpoint gathers real-user performance data before applying any further fixes — per D-09, the Speed Insights review is mandatory and human-only (dashboard requires login). This checkpoint is NOT auto-approvable.
  </what-built>
  <action>
    PAUSE for the human to perform the Speed Insights review described in how-to-verify below. The executor does NOT proceed past this task until the human pastes their per-route, per-metric findings (or an explicit "insufficient data — apply fallback") and types "approved". This is a blocking-human checkpoint and is never auto-approved (D-09). Capture the pasted findings verbatim into the plan SUMMARY so Task 2 can branch on them.
  </action>
  <how-to-verify>
    1. Ensure Plans 01 and 02 are merged and deployed to production (Speed Insights only reports on deployed routes).
    2. Open the Vercel project → Speed Insights tab.
    3. Filter to route `/decks`, then to `/decks/[id]`.
    4. For each route, read the FCP, LCP, and INP values (and their percentile / "Real Experience Score" if shown).
    5. Record, for each route and each metric: the value, whether it is flagged Good / Needs Improvement / Poor, and whether there is enough sample size to be meaningful (Vercel shows a data-points count — under ~roughly a few dozen samples, treat as insufficient).
    6. Provide the executor your specific findings in this form per route:
       - "/decks LCP = X ms (Needs Improvement), N data points — regression: <suspected cause>"
       - OR "/decks: insufficient data (N data points) — apply fallback"
  </how-to-verify>
  <verify>
    <human-check>Human has pasted per-route FCP/LCP/INP findings (or "insufficient data — apply fallback") and approved.</human-check>
  </verify>
  <resume-signal>
    Paste the per-route, per-metric findings (or "insufficient data — apply fallback") and type "approved" to continue. The executor will branch to the targeted-fix path or the D-10 fallback path based on what you provide.
  </resume-signal>
  <done>Human findings (or insufficient-data declaration) are recorded and approval given; executor may proceed to Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Apply targeted fixes per findings, or the D-10 fallback if data is insufficient</name>
  <read_first>
    - src/app/decks/[id]/loading.tsx — existing Suspense skeleton for the deck builder route (already mirrors deck-builder layout); the fallback path strengthens/verifies this
    - src/app/decks/[id]/loading.test.tsx — existing green structural test for the skeleton; must stay green after any change
    - src/app/decks/[id]/page.tsx — RSC for the deck detail; Promise.all over getDeckWithCards/getAllCards/getFilterOptions; the discretionary Suspense-boundary placement (CONTEXT "Claude's Discretion") applies here if a finding warrants it
    - src/app/decks/page.tsx — /decks RSC; no loading.tsx currently exists for /decks itself (only for [id]); the fallback MAY add src/app/decks/loading.tsx if an FCP/LCP finding on /decks warrants a streamed skeleton
    - node_modules/next/dist/docs/ — REQUIRED per AGENTS.md before writing any Suspense/loading or next/image code (read the loading-ui / Suspense and next/image docs relevant to whatever fix is chosen)
    - .planning/phases/27-decks-route-performance/27-CONTEXT.md D-10 — fallback scope: Suspense boundaries, loading.tsx skeleton, image lazy loading
  </read_first>
  <action>
    Branch on the checkpoint findings provided in Task 1:

    PATH A — Findings show one or more regressions with sufficient data: For EACH identified regression, apply one targeted fix and record the mapping (finding → fix) in the SUMMARY. Choose the fix to match the metric:
    - LCP regression on /decks or /decks/[id]: add/adjust a Suspense boundary so above-fold content streams first, and/or apply `priority` to the largest above-fold image; for /decks itself, consider adding `src/app/decks/loading.tsx` mirroring the existing [id]/loading.tsx skeleton style so the list shell paints before data resolves.
    - INP regression: verify Plan 02's startTransition covers the interaction; if a different interaction is implicated, apply startTransition there too (only if it is in deck-builder.tsx — otherwise record as a follow-up, do not expand file ownership beyond this plan's files without noting it).
    - FCP regression: ensure no blocking data fetch sits above the first paint; add a streamed skeleton via loading.tsx for the affected route.
    Keep each fix surgical and scoped to src/app/decks/[id]/loading.tsx, src/app/decks/[id]/page.tsx, and (if warranted) a new src/app/decks/loading.tsx.

    PATH B — Findings say insufficient data (D-10 fallback): apply catalog-parity baseline improvements and document that no regression data was available:
    - Verify src/app/decks/[id]/loading.tsx renders a complete skeleton matching the deck builder layout (it already does — confirm via the existing loading.test.tsx) and add `src/app/decks/loading.tsx` if /decks lacks a streamed skeleton, mirroring the existing skeleton style.
    - Ensure any below-fold card images on /decks routes use lazy loading (next/image lazy by default; confirm no above-fold image is missing `priority` and no below-fold image forces eager loading).
    - Do NOT invent regressions; explicitly write in the SUMMARY: "Speed Insights showed insufficient /decks data (N points); applied catalog-parity fallback per D-10."

    In BOTH paths: do not modify deck query caching or revalidateTag logic (owned by Plan 01) or startTransition in handleDeckUpdate (owned by Plan 02) — those are already done.
  </action>
  <verify>
    <automated>npx vitest run src/app/decks/[id]/loading.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - 27-03-SUMMARY.md records, per route and per metric, the Speed Insights findings OR an explicit "insufficient data" statement with the data-point count
    - If PATH A: every recorded regression has a corresponding code change noted in the SUMMARY (finding → fix mapping table)
    - If PATH B: the SUMMARY explicitly states the fallback was taken per D-10 and lists the catalog-parity improvements applied
    - src/app/decks/[id]/loading.test.tsx still exits 0 after any skeleton change
    - No changes were made to src/db/queries/decks.ts, the deck API route handlers, or handleDeckUpdate in deck-builder.tsx (those belong to Plans 01/02)
    - `npx vitest run` introduces no NEW failures beyond the pre-existing DATABASE_URL-environment failures
  </acceptance_criteria>
  <done>Either every identified /decks regression has an applied fix, or the D-10 fallback is applied and documented; the loading skeleton test stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel dashboard → human → executor | Performance findings are read by a human and relayed as plan input; no automated untrusted input |
| RSC render → streamed UI | loading.tsx / Suspense skeleton streams before authenticated data resolves |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03-01 | Information Disclosure | loading.tsx skeleton | accept | The skeleton renders only static placeholder shapes (no deck data, no user data); it streams before the authenticated RSC resolves, so no user-scoped data is exposed pre-auth |
| T-27-03-02 | Spoofing | Fabricated Speed Insights findings | mitigate | The checkpoint is blocking-human and non-auto-approvable (D-09); the SUMMARY must record the data-point count so a fabricated/empty review is visible; fallback path is explicit when data is insufficient |
| T-27-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs; next/image and Suspense are built-in (RESEARCH.md Package Legitimacy Audit: zero new packages) |
</threat_model>

<verification>
- `npx vitest run src/app/decks/[id]/loading.test.tsx` exits 0.
- 27-03-SUMMARY.md contains the per-metric findings (or the insufficient-data fallback statement) and the finding→fix mapping or the fallback action list.
- `npx vitest run` shows no NEW failures beyond the documented pre-existing DATABASE_URL-environment failures.
</verification>

<success_criteria>
- Speed Insights /decks FCP/LCP/INP reviewed and findings recorded (D-09).
- Each identified regression has an applied fix, OR the documented fallback is applied when data is insufficient (D-10).
- The deck-builder loading skeleton test remains green.
</success_criteria>

<output>
Create `.planning/phases/27-decks-route-performance/27-03-SUMMARY.md` when done
</output>
