---
phase: 27-decks-route-performance
plan: 02
type: execute
wave: 1
depends_on: ["27-00"]
files_modified:
  - src/components/decks/deck-builder.tsx
autonomous: true
requirements: [PERF-08, PERF-07]
must_haves:
  truths:
    - "Tapping add/remove on a card in the deck builder does not block the UI — dispatch runs as a React transition"
    - "Leader/base selection still resets the auto-filter override immediately (not deferred)"
    - "Saving a deck busts the client Router Cache so /decks reflects the change after navigation"
  artifacts:
    - path: "src/components/decks/deck-builder.tsx"
      provides: "startTransition-wrapped dispatch in handleDeckUpdate (PERF-08, D-06) + router.refresh() in handleSave (PERF-07, D-05)"
      contains: "startTransition(() =>"
  key_links:
    - from: "src/components/decks/deck-builder.tsx handleDeckUpdate"
      to: "React scheduler"
      via: "startTransition wrapping each dispatch call"
      pattern: "startTransition\\(\\(\\) =>"
    - from: "src/components/decks/deck-builder.tsx handleSave"
      to: "Router Cache"
      via: "router.refresh() inside if(res.ok)"
      pattern: "router\\.refresh\\(\\)"
---

<objective>
Implement PERF-08: wrap the three high-frequency dispatch calls in `handleDeckUpdate` (SET_LEADER, SET_BASE, UPDATE_CARD) in React `startTransition` so rapid add/remove taps no longer block input handling (INP regression fix). While in this file, also complete the deck-builder portion of PERF-07 (D-05): add `router.refresh()` to `handleSave` on success so a saved/finalized deck does not show stale data on /decks. This plan owns deck-builder.tsx exclusively and runs in parallel with Plan 01.

Purpose: Every add/remove tap currently triggers a synchronous reducer + multiple useMemo recomputations (grouping, validation, totals) that can block the main thread on rapid taps; marking the dispatch as a transition lets the browser stay responsive.
Output: deck-builder.tsx with transition-wrapped dispatches and a Router Cache refresh on save.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-decks-route-performance/27-RESEARCH.md
@.planning/phases/27-decks-route-performance/27-PATTERNS.md
@.planning/phases/27-decks-route-performance/27-CONTEXT.md
@.planning/phases/26-mobile-deck-builder-ux/26-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wrap handleDeckUpdate dispatch calls in startTransition</name>
  <read_first>
    - src/components/decks/deck-builder.tsx line 3 — current React import `import { useReducer, useState, useMemo, useEffect, useRef } from 'react';`
    - src/components/decks/deck-builder.tsx lines 245-258 — handleDeckUpdate: three branches (Leader→SET_LEADER, Base→SET_BASE, else→UPDATE_CARD); note `setIsAutoFilterOverridden(false)` follows the Leader/Base dispatches (lines 251, 254)
    - .planning/phases/27-decks-route-performance/27-CONTEXT.md D-06, D-07, D-08 — wrap only the three dispatch calls; useDeferredValue NOT used; handleSave NOT wrapped in startTransition
    - .planning/phases/27-decks-route-performance/27-PATTERNS.md lines 191-240 — exact wrapping pattern and Pitfall 4 (keep setIsAutoFilterOverridden outside the transition)
    - .planning/phases/26-mobile-deck-builder-ux/26-CONTEXT.md — Phase 26 changes to this file (two-row toolbar, Sheet, h-11 touch targets) so you don't disturb them
  </read_first>
  <behavior>
    - deck-builder.test.tsx (Wave 0): startTransition is imported from 'react'
    - deck-builder.test.tsx: SET_LEADER, SET_BASE, and UPDATE_CARD dispatches are each wrapped in startTransition(() => ...)
    - deck-builder.test.tsx: setIsAutoFilterOverridden(false) remains OUTSIDE any startTransition callback
  </behavior>
  <action>
    Add `startTransition` to the existing React import on line 3 so it reads `import { useReducer, useState, useMemo, useEffect, useRef, startTransition } from 'react';` (named export from react; do NOT import from next).
    In handleDeckUpdate, wrap ONLY the dispatch call in each of the three branches inside `startTransition(() => { ... })`:
    - Leader branch: wrap `dispatch({ type: 'SET_LEADER', payload: quantity > 0 ? cardDefinitionId : null });` in startTransition; leave `setIsAutoFilterOverridden(false);` OUTSIDE the transition, on its own line after the startTransition call (Pitfall 4 — the override reset must apply immediately, not deferred, or the auto-filter chip flashes).
    - Base branch: wrap `dispatch({ type: 'SET_BASE', ... });` in startTransition; leave `setIsAutoFilterOverridden(false);` OUTSIDE.
    - else branch: wrap `dispatch({ type: 'UPDATE_CARD', payload: { cardDefinitionId, quantity, isSideboard: false } });` in startTransition.
    Do NOT wrap handleMoveToSideboard, handleMoveToMain, the SET_NAME dispatch, or any of the inline Remove-button dispatches (lines 488-539) — D-06 scopes the transition to the add/remove high-frequency path in handleDeckUpdate only. Do NOT add useDeferredValue (D-07).
  </action>
  <verify>
    <automated>npx vitest run src/components/decks/deck-builder.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Line 3 React import includes `startTransition` (and still includes useReducer, useState, useMemo, useEffect, useRef)
    - `startTransition` is imported from `'react'`, not from `'next/...'`
    - SET_LEADER dispatch is inside `startTransition(() => { ... })`
    - SET_BASE dispatch is inside `startTransition(() => { ... })`
    - UPDATE_CARD dispatch in the else branch is inside `startTransition(() => { ... })`
    - Both `setIsAutoFilterOverridden(false)` statements remain OUTSIDE any startTransition callback
    - No `useDeferredValue` appears in the file
    - `npx vitest run src/components/decks/deck-builder.test.tsx` exits 0
  </acceptance_criteria>
  <done>The three add/remove dispatches run as React transitions; the auto-filter reset stays immediate; deck-builder.test.tsx is green.</done>
</task>

<task type="auto">
  <name>Task 2: Add router.refresh() to handleSave on success (PERF-07 D-05)</name>
  <read_first>
    - node_modules/next/dist/docs/01-app/03-api-reference/04-functions/use-router.md — router.refresh() semantics; REQUIRED per AGENTS.md before writing this code
    - src/components/decks/deck-builder.tsx lines 134, 283-316 — router from useRouter (line 134); handleSave success block (lines 299-303) sets cleanStateRef then conditionally pushes to /decks when !isDraft
    - .planning/phases/27-decks-route-performance/27-CONTEXT.md D-05 and D-08 — refresh after save success; handleSave is NOT wrapped in startTransition
    - .planning/phases/27-decks-route-performance/27-PATTERNS.md lines 229-240 — applied insertion (refresh before the conditional push)
  </read_first>
  <action>
    In handleSave, inside the existing `if (res.ok)` block (line 299): after `cleanStateRef.current = { ...state, isDraft };` and BEFORE the `if (!isDraft) { router.push('/decks'); }` line, add `router.refresh();` (Pitfall 2 — refresh must precede the push so the /decks Router Cache is busted before navigation). router.refresh() must stay inside `if (res.ok)` — never in the 400/error/catch branches.
    Do NOT wrap handleSave or any part of it in startTransition (D-08 — it is an async network call and wrapping conflicts with the setIsSaving loading state). No import changes — router already initialized on line 134.
  </action>
  <verify>
    <automated>npx vitest run src/components/decks/deck-builder.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - handleSave calls `router.refresh()` inside `if (res.ok)`, after `cleanStateRef.current = ...` and before `if (!isDraft)`
    - No `router.refresh()` call exists in the `res.status === 400` branch, the else branch, or the catch block
    - handleSave is NOT wrapped in startTransition (no `startTransition` token surrounds the handleSave body or its fetch)
    - `npx vitest run src/components/decks/deck-builder.test.tsx` exits 0
    - `npx tsc --noEmit` reports no NEW type errors in deck-builder.tsx (run if a quick typecheck is available)
  </acceptance_criteria>
  <done>handleSave busts the Router Cache on save success without being wrapped in a transition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → API route | handleSave PATCHes the deck through the authenticated route (auth + revalidateTag handled in Plan 01) |
| user input → React state | Add/remove taps dispatch reducer actions; no untrusted input crosses a security boundary here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-02-01 | Tampering | startTransition deferring the auto-filter override reset | mitigate | `setIsAutoFilterOverridden(false)` is deliberately left OUTSIDE the transition (Pitfall 4) so the override state cannot be left in a stale/incorrect value; deck-builder.test.tsx asserts it stays outside |
| T-27-02-02 | Information Disclosure | Stale /decks Router Cache after rename | mitigate | router.refresh() in handleSave busts the client Router Cache; combined with Plan 01's revalidateTag the user never sees another user's or a stale deck name |
| T-27-02-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs; startTransition is a built-in React export (RESEARCH.md Package Legitimacy Audit: zero new packages) |
</threat_model>

<verification>
- `npx vitest run src/components/decks/deck-builder.test.tsx` exits 0 (PERF-08 source stub green).
- `npx vitest run` shows no NEW failures introduced beyond the pre-existing DATABASE_URL-environment failures.
- Manual (optional, post-deploy on a phone or with CPU throttle): rapidly tap +/- on a card — the list updates without a visible freeze (INP responsive).
</verification>

<success_criteria>
- The three add/remove dispatches in handleDeckUpdate run as React transitions (D-06).
- useDeferredValue is not introduced (D-07); handleSave is not wrapped in a transition (D-08).
- handleSave calls router.refresh() on save success (D-05).
- deck-builder.test.tsx PERF-08 stub is green.
</success_criteria>

<output>
Create `.planning/phases/27-decks-route-performance/27-02-SUMMARY.md` when done
</output>
