---
phase: 24-catalog-page-load-performance
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/components/catalog/card-grid.test.tsx
  - src/db/queries/catalog.test.ts
autonomous: true
requirements: [PERF-01, PERF-02, PERF-03]
tags: [test-infrastructure, nyquist-wave-0, vitest, jsdom]

must_haves:
  truths:
    - "A RED vitest spec file exists asserting CardGrid renders virtualized rows (only visible rows in DOM, not all cards)"
    - "A RED vitest spec exists asserting first 22 cards have priority=true and cards at index >=22 have priority=false"
    - "Existing catalog.test.ts stubs are updated to assert getAllCards() takes no userId parameter and the return type excludes collectionCount"
    - "All Wave 0 stubs use `it.todo` or `test.fails` (RED) until Wave 1 implementation lands — they MUST NOT pass before implementation"
  artifacts:
    - path: "src/components/catalog/card-grid.test.tsx"
      provides: "PERF-01 virtualization + PERF-03 priority threshold assertions"
      contains: "@vitest-environment jsdom"
    - path: "src/db/queries/catalog.test.ts"
      provides: "PERF-02 getAllCards() signature stubs (no userId, no collectionCount)"
      contains: "no userId parameter"
  key_links:
    - from: "src/components/catalog/card-grid.test.tsx"
      to: "src/components/catalog/card-grid.tsx"
      via: "import { CardGrid } from './card-grid'"
      pattern: "from './card-grid'"
    - from: "src/db/queries/catalog.test.ts"
      to: "src/db/queries/catalog.ts"
      via: "Test descriptions matching getAllCards signature"
      pattern: "no userId parameter"
---

<objective>
Establish Wave 0 test infrastructure for Phase 24 by creating a jsdom-environment test file for `CardGrid` and updating the existing `catalog.test.ts` stubs to reflect the new `getAllCards()` signature.

Purpose: Per Nyquist validation strategy, every implementation task needs a failing test (RED) before implementation begins. Wave 0 establishes those RED tests so Wave 1 implementations can turn them GREEN. This catches regressions and ensures behavior is verifiable.

Output: New `card-grid.test.tsx` covering PERF-01 (virtualized row rendering) and PERF-03 (priority threshold). Updated `catalog.test.ts` covering PERF-02 (new getAllCards signature).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-catalog-page-load-performance/24-CONTEXT.md
@.planning/phases/24-catalog-page-load-performance/24-RESEARCH.md
@.planning/phases/24-catalog-page-load-performance/24-PATTERNS.md
@.planning/phases/24-catalog-page-load-performance/24-VALIDATION.md
@src/components/catalog/card-grid.tsx
@src/components/catalog/card-item.test.tsx
@src/db/queries/catalog.test.ts
@src/components/catalog/catalog-client.browser.test.tsx

<interfaces>
<!-- Key types and existing test patterns. Executor should use these directly. -->

From src/components/catalog/card-grid.tsx (current CardGrid props):
- cards: CardForFilter[]
- collection: CollectionMap
- printingArtMap?: PrintingArtMap
- onUpdateCount?: (id: number, count: number) => void
- mode?: 'catalog' | 'selector' | 'want-list' | 'binder' | 'want'
- deckCounts?: Record<number, number>
- onDeckUpdate?: (cardDefinitionId: number, count: number) => void

NOTE: After Wave 1 (Plan 03), CardGridProps will gain `scrollContainerRef: React.RefObject<HTMLElement | null>`. Wave 0 tests SHOULD include this prop (passing a stub ref) so they remain valid when implementation lands.

From src/components/catalog/card-item.test.tsx (existing jsdom test pattern):
```
// @vitest-environment jsdom
import { expect, test, vi } from 'vitest';
import { render, screen } from '@testing-library/react';
import React from 'react';

vi.mock('next/image', () => ({
  default: (props: any) => <img {...props} />,
}));

vi.mock('next/link', () => ({
  default: ({ children, href }: any) => <a href={href}>{children}</a>,
}));
```

The next/image mock spreads all props (including `priority`) onto the rendered <img> — tests can assert `priority` attribute presence via DOM query.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create card-grid.test.tsx with RED stubs for PERF-01 virtualization and PERF-03 priority threshold</name>
  <files>src/components/catalog/card-grid.test.tsx</files>
  <read_first>
    - src/components/catalog/card-item.test.tsx (copy the jsdom env directive, vi.mock for next/image, vi.mock for next/link)
    - src/components/catalog/card-grid.tsx (understand current CardGridProps shape)
    - src/components/catalog/catalog-client.browser.test.tsx (existing Wave 0 stub style — uses `it.todo`)
    - .planning/phases/24-catalog-page-load-performance/24-VALIDATION.md (Per-Task Verification Map — confirms this file covers PERF-01 + PERF-03)
    - .planning/phases/24-catalog-page-load-performance/24-RESEARCH.md (Pattern 2 — virtualization implementation details)
  </read_first>
  <behavior>
    - Test 1: `it.todo('only renders virtual rows visible in the scroll container — not all cards in the DOM')` — PERF-01 acceptance: virtualization
    - Test 2: `it.todo('first 22 cards have priority=true on their <Image> element')` — PERF-03 acceptance: priority above fold
    - Test 3: `it.todo('cards at index 22 and beyond have priority=false (or undefined) on their <Image>')` — PERF-03 acceptance: lazy below fold
    - Test 4: `it.todo('row column count matches breakpoint — 3 at base, 5 sm, 7 md, 9 lg, 11 xl')` — PERF-01 acceptance: D-04 column count
    - Test 5: `it.todo('outer wrapper has position:relative with height equal to virtualizer total size (NOT a CSS grid)')` — PERF-01 acceptance: RESEARCH.md Pitfall 5 guard
    - All tests are `it.todo` stubs — they MUST be RED (skipped/pending) until Plan 02/03 implementation lands. They MUST NOT call `render(<CardGrid ... />)` yet because CardGrid does not have the new props (scrollContainerRef, priority threading) until Wave 1.
  </behavior>
  <action>Create new file `src/components/catalog/card-grid.test.tsx`. Start with `// @vitest-environment jsdom` as the first line (NOT a JSDoc /** */ block — that style is used in card-item.test.tsx but the simpler `//` form is used in catalog-client.browser.test.tsx; either works, prefer the `//` form). Import `describe, it` from `vitest`. Write a top-level `describe('CardGrid', () => { ... })` block containing five `it.todo('...')` calls with the exact descriptions listed in `<behavior>` above. Do not import CardGrid or @testing-library yet — `it.todo` does not execute test bodies, so no imports are needed beyond `vitest`. Add a header comment block matching the style of `catalog-client.browser.test.tsx` (Wave 0 stub — covers PERF-01 + PERF-03). Reference the requirement IDs PERF-01 and PERF-03 in the header comment.</action>
  <verify>
    <automated>npm test -- --run src/components/catalog/card-grid.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/catalog/card-grid.test.tsx` exists
    - File first line is `// @vitest-environment jsdom`
    - File contains `describe('CardGrid'` (exact string)
    - File contains exactly five `it.todo(` calls
    - File contains all five behavior descriptions verbatim from `<behavior>` block above
    - Test command `npm test -- --run src/components/catalog/card-grid.test.tsx` exits 0 with 5 todo tests reported (todos count as passing/skipped in vitest, not failing)
    - File contains the strings `PERF-01` AND `PERF-03` in header comments (requirement traceability)
    - File does NOT import `CardGrid`, `render`, or `screen` (those come in Wave 1 when stubs become real tests)
  </acceptance_criteria>
  <done>card-grid.test.tsx exists with five `it.todo` Wave 0 stubs covering PERF-01 virtualization and PERF-03 priority threshold; vitest reports 5 todo tests and exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update catalog.test.ts stubs to reflect new getAllCards() signature (no userId, no collectionCount)</name>
  <files>src/db/queries/catalog.test.ts</files>
  <read_first>
    - src/db/queries/catalog.test.ts (current stubs — line 7 `it.todo('returns an array of card objects')` etc.)
    - .planning/phases/24-catalog-page-load-performance/24-RESEARCH.md (D-07 — userId removal; Pitfall 3 — collectionCount removal)
    - .planning/phases/24-catalog-page-load-performance/24-PATTERNS.md (catalog.ts target state lines 414–461)
  </read_first>
  <behavior>
    - Updated stub: `it.todo('returns an array of card objects (no userId parameter)')` — PERF-02 acceptance: D-07 signature change
    - Updated stub: `it.todo('return type does NOT include collectionCount field')` — PERF-02 acceptance: Pitfall 3 guard
    - Existing stubs `excludes cards whose type contains "token"`, `each returned card has a frontArtUrl field`, `each returned card has aspects as an array`, `returns only Normal variant cards` remain unchanged
    - The `describe('getFilterOptions()')` block remains unchanged
    - The `describe('getTopCardsByPrice()')` block remains unchanged
  </behavior>
  <action>Edit `src/db/queries/catalog.test.ts`. In the `describe('getAllCards()', ...)` block: (1) Change the first stub from `it.todo('returns an array of card objects');` to `it.todo('returns an array of card objects (no userId parameter)');`. (2) Add a new stub immediately before the closing brace of the `describe('getAllCards()')` block: `it.todo('return type does NOT include collectionCount field');`. Leave all other stubs in that describe block unchanged. Leave the `describe('getFilterOptions()')` and `describe('getTopCardsByPrice()')` blocks entirely unchanged. Keep the existing header comment `// Wave 0 stub — covers CATALOG-01 query behavior` and add a second comment line below it: `// Phase 24 update (PERF-02): getAllCards signature change — no userId, no collectionCount in return type`.</action>
  <verify>
    <automated>npm test -- --run src/db/queries/catalog.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `src/db/queries/catalog.test.ts` still contains `describe('getAllCards()'`
    - File contains the exact string `it.todo('returns an array of card objects (no userId parameter)');`
    - File contains the exact string `it.todo('return type does NOT include collectionCount field');`
    - File contains the exact string `Phase 24 update (PERF-02)` in a comment
    - File still contains the unchanged stubs: `excludes cards whose type contains "token"`, `each returned card has a frontArtUrl field`, `each returned card has aspects as an array`, `returns only Normal variant cards`
    - File still contains the unchanged `describe('getFilterOptions()'` and `describe('getTopCardsByPrice()'` blocks
    - Test command `npm test -- --run src/db/queries/catalog.test.ts` exits 0
    - The `getAllCards()` describe block now contains exactly 6 `it.todo` calls (5 original — first one renamed — plus the new collectionCount one)
  </acceptance_criteria>
  <done>catalog.test.ts updated with two PERF-02 signature change stubs; original test file structure preserved; vitest exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test files only — no runtime trust boundary changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering | Test stub files | accept | Test files are not runtime code — they cannot be tampered with by an external attacker; CI fail signals stub regression |
| T-24-02 | Information Disclosure | Test descriptions | accept | Test stubs contain feature descriptions only — no secrets, credentials, or PII |
</threat_model>

<verification>
**Phase verification (this plan's contribution):**
- Run `npm test -- --run` — full suite must exit 0
- Run `npm test -- --run src/components/catalog/card-grid.test.tsx` — must report 5 todo tests, exit 0
- Run `npm test -- --run src/db/queries/catalog.test.ts` — must report all todo tests including the 2 new ones, exit 0
- `grep -c "it.todo" src/db/queries/catalog.test.ts` — should be 12 (was 11; +1 for collectionCount stub; first stub was renamed not added)

**Wave 1 dependency satisfaction:**
After this plan, Wave 1 plans (02/03/04) can refer to the test files as concrete RED targets to turn GREEN. Plan 03 (virtualization) will replace `it.todo` calls in card-grid.test.tsx with real `test(...)` bodies once the CardGrid implementation lands.
</verification>

<success_criteria>
- card-grid.test.tsx exists with 5 `it.todo` stubs covering PERF-01 + PERF-03 acceptance behaviors
- catalog.test.ts updated with 2 new/renamed PERF-02 signature change stubs
- All test commands exit 0
- No production code is modified by this plan
- Wave 1 plans have RED test references to turn GREEN
</success_criteria>

<output>
Create `.planning/phases/24-catalog-page-load-performance/24-01-SUMMARY.md` when done. Include:
- Files created/modified (paths)
- Test count per file (before/after)
- Confirmation that all tests still pass (exit 0)
- Wave 0 readiness confirmation for downstream plans
</output>
