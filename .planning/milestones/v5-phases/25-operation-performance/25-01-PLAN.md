---
phase: 25-operation-performance
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/db/queries/collection.test.ts
  - src/app/collection/page.test.tsx
  - src/app/decks/[id]/loading.test.tsx
autonomous: true
requirements: [PERF-04, PERF-05]
tags: [test-infrastructure, nyquist-wave-0, vitest, jsdom, batch-upsert, loading-skeleton]

must_haves:
  truths:
    - "A RED vitest spec file exists for collection.ts asserting batchIncrementVariantCounts is additive (count + EXCLUDED.count), batchUpsertVariantCounts is overwrite (EXCLUDED.count), batchRecomputeTotals sums multiple definitions in one query, and all three helpers guard against empty arrays"
    - "A RED vitest spec file exists for collection/page.tsx asserting the 'uploading' status text includes the pre-computed card count from normalized.length (CSV) and from deck.cards.reduce(...) (Quick Add)"
    - "A RED vitest spec file exists for decks/[id]/loading.tsx asserting DeckBuilderLoading renders without crashing and contains animate-pulse skeleton placeholders"
    - "All Wave 0 stubs use `it.todo` (RED) — they MUST NOT pass before Wave 1 implementation lands"
  artifacts:
    - path: "src/db/queries/collection.test.ts"
      provides: "PERF-04 batch helper unit test stubs"
      contains: "batchIncrementVariantCounts"
    - path: "src/app/collection/page.test.tsx"
      provides: "PERF-04 progress text unit test stubs"
      contains: "@vitest-environment jsdom"
    - path: "src/app/decks/[id]/loading.test.tsx"
      provides: "PERF-05 loading skeleton smoke test stub"
      contains: "DeckBuilderLoading"
  key_links:
    - from: "src/db/queries/collection.test.ts"
      to: "src/db/queries/collection.ts"
      via: "Test descriptions naming batchIncrementVariantCounts / batchUpsertVariantCounts / batchRecomputeTotals"
      pattern: "batchIncrementVariantCounts"
    - from: "src/app/collection/page.test.tsx"
      to: "src/app/collection/page.tsx"
      via: "Test descriptions referencing importCardCount in status text"
      pattern: "Importing.*cards"
    - from: "src/app/decks/[id]/loading.test.tsx"
      to: "src/app/decks/[id]/loading.tsx"
      via: "import DeckBuilderLoading from './loading'"
      pattern: "from './loading'"
---

<objective>
Establish Wave 0 RED test stubs covering all three PERF-04 batch helpers, the PERF-04 progress text behavior, and the PERF-05 loading skeleton.

Purpose: Per Nyquist validation strategy (`25-VALIDATION.md`), every implementation task needs a RED test before implementation begins. Wave 0 creates `it.todo` stubs that Wave 1 plans turn into real tests once production code lands. This guarantees behavior is verifiable and prevents silent regressions.

Output: Three new test files. Zero production code modified. All `it.todo` calls reported by vitest with exit 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-operation-performance/25-CONTEXT.md
@.planning/phases/25-operation-performance/25-RESEARCH.md
@.planning/phases/25-operation-performance/25-PATTERNS.md
@.planning/phases/25-operation-performance/25-VALIDATION.md
@src/db/queries/catalog.test.ts
@src/db/queries/collection.ts
@src/app/collection/page.tsx

<interfaces>
<!-- Existing Wave 0 stub pattern (from src/db/queries/catalog.test.ts) -->
<!-- Use `it.todo` for all stubs. Header comment must include the requirement ID. -->

From src/db/queries/catalog.test.ts:
// @vitest-environment node
// Wave 0 stub — covers CATALOG-01 query behavior
// These tests require a live DB connection; mark as todo for CI
import { describe, it } from 'vitest';

describe('getAllCards()', () => {
  it.todo('returns an array of card objects (no userId parameter)');
  ...
});

Conventions:
- DB query test files use `@vitest-environment node`
- Component test files use `@vitest-environment jsdom`
- Stubs are `it.todo('description')` — no test body, no imports beyond `vitest`
- Header comment must reference requirement IDs (e.g., PERF-04)

Planned new batch helper signatures (Plan 02 will implement these):
- batchIncrementVariantCounts(items: Array<{ cardPrintingId: number; qtyToAdd: number }>, userId: number): Promise
- batchUpsertVariantCounts(items: Array<{ cardPrintingId: number; count: number }>, userId: number): Promise
- batchRecomputeTotals(cardDefinitionIds: number[], userId: number): Promise
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create collection.test.ts with RED stubs for three batch helpers and empty-array guards</name>
  <files>src/db/queries/collection.test.ts</files>
  <read_first>
    - src/db/queries/catalog.test.ts (existing Wave 0 stub style — header comment, @vitest-environment node, import from vitest, it.todo descriptions)
    - src/db/queries/collection.ts (existing incrementVariantCount lines 238-254, upsertVariantCount lines 222-231, recomputeTotal lines 261-282 — understand additive vs overwrite semantics that batch helpers must preserve)
    - .planning/phases/25-operation-performance/25-RESEARCH.md (Pattern 1 + Pattern 2 — exact behaviors batch helpers must implement; Pitfall 1 additive-vs-overwrite distinction; Pitfall 2 empty-array guard)
    - .planning/phases/25-operation-performance/25-VALIDATION.md (Per-Task Verification Map row 25-01-01)
  </read_first>
  <behavior>
    - Test 1: `it.todo('batchIncrementVariantCounts increments existing count additively (count + EXCLUDED.count) on conflict')` — PERF-04 acceptance D-01: Quick Add must preserve additive semantics from existing incrementVariantCount
    - Test 2: `it.todo('batchIncrementVariantCounts inserts new row with given count when no conflict')` — PERF-04 acceptance: fresh insert path
    - Test 3: `it.todo('batchUpsertVariantCounts overwrites existing count (EXCLUDED.count) on conflict')` — PERF-04 acceptance: CSV Import overwrite semantics from existing upsertVariantCount
    - Test 4: `it.todo('batchRecomputeTotals computes SUM per cardDefinitionId across multiple definitions in one query and upserts user_collections')` — PERF-04 acceptance D-02: replaces M-loop recomputeTotal
    - Test 5: `it.todo('batchIncrementVariantCounts returns early without throwing when items array is empty')` — PERF-04 acceptance: Pitfall 2 (Drizzle throws on .values([]))
    - Test 6: `it.todo('batchUpsertVariantCounts returns early without throwing when items array is empty')` — PERF-04 acceptance: empty-array guard
    - Test 7: `it.todo('batchRecomputeTotals returns early without throwing when cardDefinitionIds array is empty')` — PERF-04 acceptance: empty-array guard on both SELECT and INSERT
    - All seven tests are `it.todo` stubs — they MUST be RED (pending) until Plan 02 implementation lands.
  </behavior>
  <action>Create new file `src/db/queries/collection.test.ts`. First line: `// @vitest-environment node`. Add a multi-line header comment block matching the style of `catalog.test.ts` lines 1-5: state this is a Wave 0 stub covering PERF-04 batch helpers, that tests require a live DB connection so they are marked as todo for CI, and that full integration validation is a manual smoke test after Wave 1 deployment. Reference requirement ID PERF-04 in the header. Import `describe, it` from `vitest`. Write three top-level `describe` blocks: `describe('batchIncrementVariantCounts()', () => { ... })` containing tests 1, 2, and 5; `describe('batchUpsertVariantCounts()', () => { ... })` containing tests 3 and 6; `describe('batchRecomputeTotals()', () => { ... })` containing tests 4 and 7. Use exactly the seven `it.todo` strings from `<behavior>` above, verbatim. Do not import the batch helpers — `it.todo` does not execute test bodies.</action>
  <verify>
    <automated>npx vitest run src/db/queries/collection.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `src/db/queries/collection.test.ts` exists
    - File first line is `// @vitest-environment node`
    - File contains `PERF-04` in header comment (requirement traceability)
    - File contains exactly three `describe(` calls with names `batchIncrementVariantCounts()`, `batchUpsertVariantCounts()`, `batchRecomputeTotals()`
    - File contains exactly seven `it.todo(` calls
    - All seven `it.todo` description strings appear verbatim from the `<behavior>` block
    - File does NOT import `batchIncrementVariantCounts`, `batchUpsertVariantCounts`, or `batchRecomputeTotals` from `./collection` (those imports come in Wave 1)
    - Command `npx vitest run src/db/queries/collection.test.ts` exits 0
    - vitest output reports 7 todo tests
  </acceptance_criteria>
  <done>collection.test.ts exists with seven it.todo Wave 0 stubs across three describe blocks covering PERF-04 batch helper semantics; vitest exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create collection/page.test.tsx with RED stubs for PERF-04 card count progress text</name>
  <files>src/app/collection/page.test.tsx</files>
  <read_first>
    - src/db/queries/catalog.test.ts (existing Wave 0 stub style — header comment, vitest import, it.todo)
    - src/app/collection/page.tsx (current status state machine lines 14-23; current "Syncing with database..." text line 164; current "Adding..." button label line 213 — understand what Plan 03 will change)
    - .planning/phases/25-operation-performance/25-RESEARCH.md (Pattern 4 — status text extension; Pitfall 4 — pre-computed count must come from normalized.length before POST, not API response)
    - .planning/phases/25-operation-performance/25-CONTEXT.md (D-05 CSV status text "Importing 847 cards..."; D-06 Quick Add status "Adding 55 cards from [Deck Name]...")
  </read_first>
  <behavior>
    - Test 1: `it.todo('CSV Import uploading status renders "Importing {N} cards..." where N is normalized.length set before fetch fires')` — PERF-04 acceptance D-05
    - Test 2: `it.todo('CSV Import success status renders "Done! {N} cards imported." with count from API response')` — PERF-04 acceptance D-05 success message
    - Test 3: `it.todo('Quick Add button label renders "Adding {N} cards from {deckName}..." during deckStatus loading')` — PERF-04 acceptance D-06
    - Test 4: `it.todo('Quick Add success status renders "Added {N} cards from {deckName} to your collection." after success')` — PERF-04 acceptance D-06 success message
    - Test 5: `it.todo('CSV import card count state is set before fetch is invoked (not from response)')` — PERF-04 acceptance: Pitfall 4 (in-flight message uses pre-computed count, not stale 0)
    - All five tests are `it.todo` stubs — they MUST be RED until Plan 03 implementation lands.
  </behavior>
  <action>Create new file `src/app/collection/page.test.tsx`. First line: `// @vitest-environment jsdom` (Client Component requires DOM). Add a multi-line header comment block stating: Wave 0 stub covering PERF-04 progress text behavior; tests use jsdom rendering of CollectionPage; full integration validation is a manual smoke test after Wave 1 deployment. Reference requirement ID PERF-04 in the header. Import `describe, it` from `vitest`. Write one top-level `describe('CollectionPage progress text (PERF-04)', () => { ... })` block containing all five `it.todo` calls verbatim from the `<behavior>` block above. Do not import CollectionPage, React, or @testing-library — `it.todo` does not execute test bodies.</action>
  <verify>
    <automated>npx vitest run src/app/collection/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `src/app/collection/page.test.tsx` exists
    - File first line is `// @vitest-environment jsdom`
    - File contains `PERF-04` in header comment
    - File contains exactly one `describe(` block named `CollectionPage progress text (PERF-04)`
    - File contains exactly five `it.todo(` calls
    - All five `it.todo` description strings match verbatim from the `<behavior>` block
    - File does NOT import `CollectionPage`, `render`, `screen`, or React
    - Command `npx vitest run src/app/collection/page.test.tsx` exits 0
    - vitest output reports 5 todo tests
  </acceptance_criteria>
  <done>page.test.tsx exists with five it.todo Wave 0 stubs covering PERF-04 progress text for both CSV Import and Quick Add; vitest exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create decks/[id]/loading.test.tsx with RED smoke stub for PERF-05 skeleton</name>
  <files>src/app/decks/[id]/loading.test.tsx</files>
  <read_first>
    - src/db/queries/catalog.test.ts (existing Wave 0 stub style)
    - src/components/decks/deck-builder.tsx lines 306-345 (outer container layout the skeleton mirrors: flex h-[calc(100svh-56px)] overflow-hidden + flex-1 flex flex-col overflow-hidden + toolbar)
    - src/components/decks/deck-sidebar.tsx line 60 (sidebar container w-80 bg-slate-50 border-l p-4 the skeleton mirrors)
    - .planning/phases/25-operation-performance/25-RESEARCH.md (Pattern 3 — Next.js loading.tsx file convention; Pitfall 3 — must not call auth)
    - .planning/phases/25-operation-performance/25-PATTERNS.md (decks/[id]/loading.tsx skeleton structure — exact CSS classes)
  </read_first>
  <behavior>
    - Test 1: `it.todo('DeckBuilderLoading renders without throwing')` — PERF-05 acceptance: smoke test
    - Test 2: `it.todo('DeckBuilderLoading default export contains animate-pulse class somewhere in rendered output')` — PERF-05 acceptance D-08: skeleton uses Tailwind animate-pulse
    - Test 3: `it.todo('DeckBuilderLoading outer container has h-[calc(100svh-56px)] (mirrors DeckBuilder layout height)')` — PERF-05 acceptance D-08: structurally mirrors real DeckBuilder, not generic spinner
    - Test 4: `it.todo('DeckBuilderLoading contains a w-80 sidebar placeholder (mirrors DeckSidebar)')` — PERF-05 acceptance D-08: two-panel layout
    - Test 5: `it.todo('DeckBuilderLoading does not call auth.api.getSession or read cookies/headers (loading.tsx server component constraint)')` — PERF-05 acceptance: Pitfall 3 + Pitfall 5
    - All five tests are `it.todo` stubs — they MUST be RED until Plan 03 implementation lands.
  </behavior>
  <action>Create new file `src/app/decks/[id]/loading.test.tsx`. First line: `// @vitest-environment jsdom`. Add a multi-line header comment block stating: Wave 0 stub covering PERF-05 loading skeleton; tests use jsdom rendering of DeckBuilderLoading server component; structural assertions verify layout mirrors DeckBuilder + DeckSidebar. Reference requirement ID PERF-05 in the header. Import `describe, it` from `vitest`. Write one top-level `describe('DeckBuilderLoading (PERF-05)', () => { ... })` block containing all five `it.todo` calls verbatim from the `<behavior>` block above. Do not import the loading component, render, or screen — `it.todo` does not execute test bodies and the component does not yet exist.</action>
  <verify>
    <automated>npx vitest run src/app/decks/[id]/loading.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `src/app/decks/[id]/loading.test.tsx` exists
    - File first line is `// @vitest-environment jsdom`
    - File contains `PERF-05` in header comment
    - File contains exactly one `describe(` block named `DeckBuilderLoading (PERF-05)`
    - File contains exactly five `it.todo(` calls
    - All five `it.todo` description strings match verbatim from the `<behavior>` block
    - File does NOT import the (not-yet-existing) `./loading` module, `render`, or `screen`
    - Command `npx vitest run src/app/decks/[id]/loading.test.tsx` exits 0
    - vitest output reports 5 todo tests
  </acceptance_criteria>
  <done>loading.test.tsx exists with five it.todo Wave 0 stubs covering PERF-05 skeleton structure and constraints; vitest exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test files only — no runtime trust boundary changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-W0-01 | Tampering | Test stub files | accept | Test files are not runtime code — they cannot be tampered with by an external attacker; CI failure signals stub regression |
| T-25-W0-02 | Information Disclosure | Test descriptions | accept | Test stubs contain feature descriptions only — no secrets, credentials, or PII |
</threat_model>

<verification>
**Phase verification (this plan's contribution):**
- `npx vitest run` — full suite must exit 0
- `npx vitest run src/db/queries/collection.test.ts` — must report 7 todo tests, exit 0
- `npx vitest run src/app/collection/page.test.tsx` — must report 5 todo tests, exit 0
- `npx vitest run src/app/decks/[id]/loading.test.tsx` — must report 5 todo tests, exit 0
- Grep gate: `grep -v '^//' src/db/queries/collection.test.ts | grep -c 'it.todo'` should be 7
- Grep gate: `grep -v '^//' src/app/collection/page.test.tsx | grep -c 'it.todo'` should be 5
- Grep gate: `grep -v '^//' src/app/decks/[id]/loading.test.tsx | grep -c 'it.todo'` should be 5

**Wave 1 dependency satisfaction:**
After this plan, Wave 1 plans (02 and 03) have concrete RED targets. Plan 02 turns the 7 collection.test.ts todos into real tests once batch helpers land. Plan 03 turns the 5 page.test.tsx todos and 5 loading.test.tsx todos into real tests once the UI changes land.
</verification>

<success_criteria>
- All three test files exist with correct vitest environment directives
- All 17 (7 + 5 + 5) `it.todo` stubs exist verbatim from the behavior blocks
- `npx vitest run` exits 0
- No production code is modified by this plan
- Wave 1 plans have RED test references to turn GREEN
</success_criteria>

<output>
Create `.planning/phases/25-operation-performance/25-01-SUMMARY.md` when done. Include:
- Files created (3 paths)
- Test count per file (collection.test.ts: 7, page.test.tsx: 5, loading.test.tsx: 5)
- Confirmation that `npx vitest run` exits 0
- Wave 0 readiness confirmation for Plans 02 and 03
</output>
