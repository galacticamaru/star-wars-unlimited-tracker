---
phase: 25.1-speed-insights-integration
verified: 2026-05-27T00:00:00Z
status: human_needed
score: 7/8 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Verify Web Vitals events flow to Vercel Speed Insights dashboard after production deployment"
    expected: "Web Vitals data (LCP, CLS, INP, FCP, TTFB) appears in the Vercel Dashboard -> Project -> Speed Insights tab within ~30 minutes of production traffic"
    why_human: "Requires a production deployment on Vercel with Speed Insights enabled in the project dashboard. Cannot verify programmatically from the codebase."
---

# Phase 25.1: Speed Insights Integration Verification Report

**Phase Goal:** Install and configure @vercel/speed-insights to capture real-user performance metrics in production
**Verified:** 2026-05-27
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | package.json lists @vercel/speed-insights in dependencies (not devDependencies) | VERIFIED | `package.json` line 23: `"@vercel/speed-insights": "^2.0.0"` under `dependencies`. Not present in `devDependencies`. Node verify command exits 0 printing `OK ^2.0.0`. |
| 2 | @vercel/speed-insights is installed in node_modules and importable from '@vercel/speed-insights/next' | VERIFIED | `node -e "require.resolve('@vercel/speed-insights/next')"` exits 0 and prints `resolvable`. package-lock.json entry confirmed at version 2.0.0. |
| 3 | src/app/layout.tsx imports SpeedInsights from '@vercel/speed-insights/next' and renders `<SpeedInsights />` inside `<body>` | VERIFIED | Line 9: `import { SpeedInsights } from '@vercel/speed-insights/next';`. Line 52: `<SpeedInsights debug={process.env.NODE_ENV === 'development'} />` is inside `<body>`. Body-match verify command exits 0. |
| 4 | D-01: No sampleRate prop is passed to `<SpeedInsights />` — default 100% sample rate is preserved | VERIFIED | `sampleRate` does not appear anywhere in `src/app/layout.tsx`. Grep confirms no match. |
| 5 | D-02: No beforeSend hook or route exclusion props are passed to `<SpeedInsights />` | VERIFIED | `beforeSend` does not appear anywhere in `src/app/layout.tsx`. Grep confirms no match. |
| 6 | D-03: `<SpeedInsights />` receives a debug prop (conditional expression) so the local debug panel surfaces during npm run dev | VERIFIED | Exact JSX: `<SpeedInsights debug={process.env.NODE_ENV === 'development'} />`. debug prop present and uses the preferred conditional form per CONTEXT.md. SUMMARY confirms Task 4 human checkpoint approved — debug panel surfaced locally with no console errors. |
| 7 | npm run build exits with code 0 with no new TypeScript or ESLint errors compared to baseline | VERIFIED (partial — see note) | SUMMARY documents: "TypeScript compilation passed with no new errors in layout.tsx or any production source file." Pre-existing build failure at static generation phase (Neon WebSocket SSL disconnect) predates this phase and is unrelated to SpeedInsights. No new errors introduced. Commits f73c47d and 61f495e both exist and contain only the expected file changes. |
| 8 | After production deployment, Web Vitals events flow to the Vercel Speed Insights dashboard (verified by human post-deploy) | UNCERTAIN — human needed | Requires production deployment with Speed Insights enabled in the Vercel dashboard. Cannot be verified from the codebase. |

**Score:** 7/8 truths verified (1 pending human post-deploy confirmation)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `package.json` | @vercel/speed-insights dependency declaration | VERIFIED | `"@vercel/speed-insights": "^2.0.0"` present under `dependencies`, absent from `devDependencies` |
| `package-lock.json` | Locked resolution of @vercel/speed-insights and transitive deps | VERIFIED | `node_modules/@vercel/speed-insights` entry present, version 2.0.0, 3 occurrences in lock file |
| `src/app/layout.tsx` | Root layout rendering `<SpeedInsights />` on every page | VERIFIED | Import on line 9, JSX on line 52 as last direct child of `<body>` |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `src/app/layout.tsx` | `@vercel/speed-insights/next` | named import | WIRED | `import { SpeedInsights } from '@vercel/speed-insights/next'` — regex match confirmed |
| `<body>` in `src/app/layout.tsx` | `<SpeedInsights />` JSX usage | Component rendered as last child of `<body>` | WIRED | `<SpeedInsights debug={process.env.NODE_ENV === 'development'} />` at line 52; verified as last direct child with no trailing siblings |

### Data-Flow Trace (Level 4)

Not applicable. `<SpeedInsights />` is a telemetry component — it does not render user-visible data from application state. It passively observes browser performance events and beacons them to Vercel's infrastructure. No data-flow trace needed.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Package in dependencies | `node -e "require('./package.json').dependencies['@vercel/speed-insights']"` | `^2.0.0` | PASS |
| /next subpath resolvable | `node -e "require.resolve('@vercel/speed-insights/next')"` | `resolvable` | PASS |
| Import present in layout | `grep -c "SpeedInsights" src/app/layout.tsx` | 2 lines (import + JSX) | PASS |
| SpeedInsights inside body | body-match node script | `inside body OK` | PASS |
| SpeedInsights is last body child | last-child node script | `SpeedInsights last child? true` | PASS |
| debug prop present | prop check node script | `debug prop present: true` | PASS |
| No sampleRate prop | prop check node script | `sampleRate prop: false` | PASS |
| No beforeSend prop | prop check node script | `beforeSend prop: false` | PASS |
| Commits exist | `git log --oneline f73c47d 61f495e` | Both commits confirmed | PASS |

### Probe Execution

No `scripts/*/tests/probe-*.sh` files declared or present for this phase. Step 7c: SKIPPED (no probes defined).

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| PERF-06 | 25.1-01 | Real-user performance metrics (Web Vitals) captured via @vercel/speed-insights — every page instrumented and data flows to Vercel dashboard | SATISFIED (pending post-deploy dashboard confirmation) | Package installed as production dependency; `<SpeedInsights />` renders on every page via root layout; REQUIREMENTS.md marks PERF-06 as checked (`[x]`) and traces to Phase 25.1 / 25.1-01 with verified date 2026-05-27 |

No orphaned requirements: only PERF-06 maps to Phase 25.1 in REQUIREMENTS.md, and it is covered by plan 25.1-01.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None | — | — | — | — |

No `TBD`, `FIXME`, `XXX`, placeholder text, empty returns, or hardcoded empty data found in `src/app/layout.tsx`. No anti-patterns in files modified by this phase.

### Human Verification Required

#### 1. Production Web Vitals Dashboard Confirmation

**Test:** Deploy the application to Vercel production. In the Vercel Dashboard, navigate to Project -> Settings -> Speed Insights and enable the feature. After enabling, generate production traffic (navigate several pages). Wait up to 30 minutes.

**Expected:** Web Vitals metrics (LCP, CLS, INP, FCP, TTFB) appear in the Speed Insights tab of the Vercel dashboard, confirming the beacon endpoint is active and data is flowing.

**Why human:** Requires a production deployment on Vercel infrastructure. The beacon endpoint is provisioned by Vercel and only activates when Speed Insights is enabled in the project dashboard. No programmatic verification is possible from the codebase alone. This item was explicitly planned in `user_setup` block of the PLAN and is tracked under PERF-06 traceability.

### Gaps Summary

No blocking gaps. All automatable must-haves are fully verified:

- `@vercel/speed-insights@^2.0.0` is correctly placed in `dependencies` (not `devDependencies`), locked in `package-lock.json` at version 2.0.0, and resolvable via the `/next` subpath export.
- `src/app/layout.tsx` imports `SpeedInsights` and renders it as the final direct child of `<body>` with the `debug={process.env.NODE_ENV === 'development'}` prop — satisfying D-01, D-02, and D-03.
- Existing layout structure (NavBar, NuqsAdapter, CurrencyProvider, Suspense, children) is untouched.
- Both commits (f73c47d, 61f495e) are confirmed in git history with accurate descriptions.
- REQUIREMENTS.md records PERF-06 as satisfied and verified on 2026-05-27.

The one remaining item (post-deploy dashboard confirmation) is a known human-verification step explicitly deferred to the production deployment lifecycle in the PLAN's `user_setup` block. It does not indicate an implementation gap.

---

_Verified: 2026-05-27_
_Verifier: Claude (gsd-verifier)_
