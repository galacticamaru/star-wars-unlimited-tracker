---
phase: 05.1-want-list-gap-fix
plan: 01
verified: 2026-05-07T06:10:00Z
status: human_needed
score: 5/5 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Open a saved deck where you do NOT own the Leader card. Switch to the Want List tab."
    expected: "The Leader card appears in the Want List under the 'Leader' type header with the correct shortfall quantity."
    why_human: "Requires a live browser session with real collection and deck data to confirm the per-deck WantListTab renders the leader shortfall end-to-end."
  - test: "Open a saved deck where you DO own the Leader and Base. Switch to the Want List tab."
    expected: "Leader and Base do NOT appear in the Want List (no false shortfalls)."
    why_human: "Requires live collection data to confirm the owned-count comparison suppresses the synthetic rows correctly."
  - test: "Navigate to /decks. The combined want list section should now include any Leader or Base shortfalls."
    expected: "Leaders/Bases you need appear in the combined want list alongside missing main deck cards."
    why_human: "End-to-end route test requiring real data; the /api/want-list route is a server component that cannot be exercised without running the app."
  - test: "Open a deck that has no Leader or Base selected (draft deck). Switch to the Want List tab."
    expected: "No synthetic row appears for Leader or Base — the tab shows only the main deck shortfalls (or the empty-deck state if no cards either)."
    why_human: "Requires live session to confirm null-guard silences synthetic rows for null FK slots."
---

# Phase 5.1: Want List Gap Fix — Verification Report

**Phase Goal:** Extend the want list to include Leader and Base card shortfalls in both per-deck (WantListTab) and combined (/api/want-list) views. Fix the gap where WANT-01 and WANT-02 were partially satisfied — main deck shortfalls worked but Leader/Base slots (stored as FK columns on the decks table) were silently excluded.
**Verified:** 2026-05-07T06:10:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Step 0: Previous Verification

No previous VERIFICATION.md found in `.planning/phases/05.1-want-list-gap-fix/`. Proceeding as initial verification.

---

## Step 1: Context Loaded

PLAN: `.planning/phases/05.1-want-list-gap-fix/05.1-01-PLAN.md`
SUMMARY: `.planning/phases/05.1-want-list-gap-fix/05.1-01-SUMMARY.md`
Commits claimed: `433acbb` and `8756c56` — both confirmed present in `git log` and touch exactly the two modified files.

---

## Step 2: Must-Haves

### ROADMAP Success Criteria (Phase 5.1)

From ROADMAP.md `Phase 5.1 (INSERTED)`:

1. The per-deck want list includes the Leader and Base if the user does not own the required copy
2. The combined want list aggregates Leader and Base shortfalls across all decks alongside main deck shortfalls
3. Existing want list behaviour for main deck cards is unchanged

### PLAN Frontmatter Truths

1. Per-deck want list includes the Leader card shortfall if the user does not own the required copy
2. Per-deck want list includes the Base card shortfall if the user does not own the required copy
3. Combined want list aggregates Leader and Base shortfalls across all decks alongside main deck shortfalls
4. Decks with null leader or null base emit no synthetic row for that slot
5. Existing want list behaviour for main deck cards is unchanged

**Merged must-haves:** PLAN truths 1-5 cover all three ROADMAP SCs with additional specificity. All 5 truths verified below.

---

## Step 3: Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Per-deck want list includes the Leader card shortfall if the user does not own the required copy | ? UNCERTAIN (human) | Code path: `deck-builder.tsx` line 347 injects `{ cardDefinitionId: state.leaderCardDefinitionId, quantity: 1 }` into `deckCards` prop when non-null. `WantListTab` compares `dc.quantity - owned` and surfaces shortfall > 0. Logic is correct but runtime output requires human check. |
| 2 | Per-deck want list includes the Base card shortfall if the user does not own the required copy | ? UNCERTAIN (human) | Same pattern as Truth 1 — `deck-builder.tsx` line 348 injects base. Structurally identical and correct. Runtime requires human check. |
| 3 | Combined want list aggregates Leader and Base shortfalls across all decks | ? UNCERTAIN (human) | `getDeckCardsForUser()` emits synthetic rows (lines 230-253, `decks.ts`). `/api/want-list/route.ts` imports and calls `getDeckCardsForUser(1)` (lines 1, 7); aggregation loop at line 19-24 filters `isSideboard` (synthetic rows have `isSideboard: false`) and computes max-quantity per card. Code path is fully wired. Runtime output requires human check. |
| 4 | Decks with null leader or null base emit no synthetic row for that slot | ✓ VERIFIED | `decks.ts` line 235: `if (!defId) continue;` inside loop over `[deck.leaderCardDefinitionId, deck.baseCardDefinitionId]`. The skip is unconditional for null/undefined values. TypeScript `number \| null` type enforces this. |
| 5 | Existing want list behaviour for main deck cards is unchanged | ✓ VERIFIED | `deckCardRows` (lines 183-204) is the same query as the pre-phase implementation (confirmed by PLAN's context snapshot). Synthetic rows are only appended after (`return [...deckCardRows, ...syntheticRows]` line 253). `WantListTab` interface is unchanged — no modifications to `want-list-tab.tsx`. |

**Score:** 5/5 truths structurally verified; 3 truths require human runtime confirmation.

---

## Step 4: Artifact Verification

### Level 1 — Existence

| Artifact | Exists | Status |
|----------|--------|--------|
| `src/db/queries/decks.ts` | Yes — commit `433acbb` | ✓ |
| `src/components/decks/deck-builder.tsx` | Yes — commit `8756c56` | ✓ |
| `src/components/decks/want-list-tab.tsx` | Yes (unchanged as intended) | ✓ |
| `src/app/api/want-list/route.ts` | Yes (unchanged as intended) | ✓ |

### Level 2 — Substantive (not stubs)

**`src/db/queries/decks.ts` — `getDeckCardsForUser()`**

- Step 1 select extended: `leaderCardDefinitionId: decks.leaderCardDefinitionId`, `baseCardDefinitionId: decks.baseCardDefinitionId` — line 172-173. ✓
- `resolvePrinting()` helper defined at lines 207-228 with full DB query using `cardDefinitions` + `cardPrintings` join, `variantType = 'Normal'` filter, `limit(1)`. ✓
- `syntheticRows` declared (line 231), populated via `push` (line 238), returned via spread (line 253). ✓
- `isSideboard: false` hardcoded in synthetic row (line 242). ✓
- `if (!printing) continue;` guard (line 237) prevents rows for cards with no Normal printing. ✓
- `variantType = 'Normal'` appears 4 times (lines 129, 155, 202, 223) — at least 2 are inside `getDeckCardsForUser` (lines 202, 223). ✓

**`src/components/decks/deck-builder.tsx` — WantListTab call site**

- `deckCards` prop is now a spread array (lines 344-350), not the original single-line filter. ✓
- Leader entry: `...(state.leaderCardDefinitionId ? [{ cardDefinitionId: state.leaderCardDefinitionId, quantity: 1 }] : [])` — line 347. ✓
- Base entry: `...(state.baseCardDefinitionId ? [{ cardDefinitionId: state.baseCardDefinitionId, quantity: 1 }] : [])` — line 348. ✓
- `allCards={allCards}` prop unchanged (line 350). ✓

### Level 3 — Wiring

| Link | From | To | Via | Status |
|------|------|----|-----|--------|
| Per-deck | `deck-builder.tsx` | `want-list-tab.tsx` | `deckCards` prop with leader/base entries | ✓ WIRED — import at line 9; call site at lines 343-351 |
| Combined | `decks.ts` `getDeckCardsForUser()` | `/api/want-list/route.ts` | `getDeckCardsForUser(1)` call | ✓ WIRED — import at line 1 of route; called at line 7 |
| Aggregation | route.ts loop | synthetic rows | `isSideboard` filter passes `false` rows | ✓ WIRED — `if (row.isSideboard) continue` (line 20) passes synthetic rows correctly |

---

## Step 4b: Data-Flow Trace (Level 4)

**WantListTab** fetches real collection data via `fetch('/api/collection')` inside `useEffect` (want-list-tab.tsx lines 18-29). State variable `collection` is populated with real API data, then `shortfallCards` memo computes `dc.quantity - owned` where `owned = collection[dc.cardDefinitionId] ?? 0` (line 36). This is a real data query, not a static return.

**`/api/want-list/route.ts`** calls `getUserCollection(1)` (line 8) to build `ownedMap`, then computes shortfall against `getDeckCardsForUser()` results. Both are live DB queries.

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|--------------------|--------|
| `want-list-tab.tsx` | `collection` | `GET /api/collection` | Yes — real DB query | ✓ FLOWING |
| `route.ts` | `allDeckCards` | `getDeckCardsForUser(1)` | Yes — Drizzle ORM DB query | ✓ FLOWING |
| `route.ts` | `collection` | `getUserCollection(1)` | Yes — Drizzle ORM DB query | ✓ FLOWING |

---

## Step 5: Key Link Verification

| From | To | Via | Status | Detail |
|------|----|-----|--------|--------|
| `deck-builder.tsx` | `want-list-tab.tsx` | `deckCards` prop with leader/base entries | ✓ WIRED | `state.leaderCardDefinitionId` at line 347; `state.baseCardDefinitionId` at line 348; shape matches `{ cardDefinitionId: number; quantity: number }[]` interface |
| `decks.ts getDeckCardsForUser()` | `/api/want-list/route.ts` | Return value includes synthetic rows | ✓ WIRED | Route imports and calls function; synthetic rows have `isSideboard: false` so aggregation loop processes them |

---

## Step 6: Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| WANT-01 | 05.1-01-PLAN.md | User can see which cards each individual deck is missing, based on their owned collection | ✓ SATISFIED | Leader/Base now injected into `deckCards` prop in `deck-builder.tsx`; `WantListTab` computes shortfall for all entries |
| WANT-02 | 05.1-01-PLAN.md | User can view a combined want list that aggregates missing cards across all their decks | ✓ SATISFIED | `getDeckCardsForUser()` now emits synthetic rows for leader/base; `/api/want-list` aggregates them alongside main deck rows |

No orphaned requirements — REQUIREMENTS.md maps both WANT-01 and WANT-02 to Phase 5 (now completed via Phase 5.1 extension). Both are marked `[x]` Complete in REQUIREMENTS.md.

---

## Step 7: Anti-Pattern Scan

Files modified in this phase: `src/db/queries/decks.ts`, `src/components/decks/deck-builder.tsx`.

| File | Pattern | Finding | Severity |
|------|---------|---------|---------|
| `decks.ts` | TODO/FIXME | None found | — |
| `decks.ts` | `return null` / empty returns | `return null` in `resolvePrinting()` guard (line 208) — intentional null-guard, not a stub | ℹ️ Info |
| `decks.ts` | Hardcoded empty | `syntheticRows: typeof deckCardRows = []` — initial empty array that is populated in the loop; not a stub | ℹ️ Info |
| `deck-builder.tsx` | `placeholder="Deck Name"` | React input `placeholder` attribute — not an anti-pattern | — |
| `deck-builder.tsx` | TODO/FIXME | None found | — |

No blocker or warning anti-patterns found.

---

## Step 7b: Behavioral Spot-Checks

Direct behavioral checks require a running server and real database. Skipped — see Human Verification Required section below.

---

## Step 8: Human Verification Required

### 1. Per-deck want list shows missing Leader

**Test:** Open a saved deck where you do NOT own the Leader card. Switch to the Want List tab.
**Expected:** The Leader card appears in the Want List under the 'Leader' type header with the correct shortfall quantity (shortfall = 1, since quantity 1 and owned 0).
**Why human:** Requires a live browser session with real collection and deck data. The `WantListTab` logic is client-side and depends on `fetch('/api/collection')` returning real owned counts.

### 2. Per-deck want list suppresses owned Leader/Base

**Test:** Open a saved deck where you DO own the Leader and Base cards. Switch to the Want List tab.
**Expected:** Leader and Base do NOT appear in the Want List (shortfall = 0 for both, so they are filtered out).
**Why human:** Requires live collection data to confirm the `dc.quantity - owned` comparison suppresses the synthetic rows when owned >= 1.

### 3. Combined want list surfaces Leader/Base shortfalls

**Test:** Navigate to /decks. Check the combined want list section.
**Expected:** Any Leader or Base cards you need appear in the combined list alongside missing main deck cards.
**Why human:** The `/api/want-list` route is a Next.js server route; verifying its JSON output end-to-end requires running the app and checking the rendered /decks page.

### 4. Null leader/base produces no phantom row

**Test:** Open a deck that has no Leader or Base selected (draft deck). Switch to the Want List tab.
**Expected:** No synthetic row appears for Leader or Base — only main deck shortfalls (or the empty-deck message if no cards are added yet).
**Why human:** Requires live session to confirm the `if (!defId) continue;` null-guard prevents phantom rows in the rendered output.

---

## Gaps Summary

No hard gaps. All five truths are structurally verified at the code level. The three ROADMAP success criteria are all covered by the implementation. The four human verification items test that runtime behavior matches the correct code logic — they are not blocking code gaps but are standard confirmation steps for an interactive UI feature.

---

_Verified: 2026-05-07T06:10:00Z_
_Verifier: Claude (gsd-verifier)_
