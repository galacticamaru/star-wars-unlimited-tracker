# Phase 5.1: Want List Gap Fix - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-07
**Phase:** 5.1-want-list-gap-fix
**Areas discussed:** Incomplete deck edge case, WantListTab prop interface, getDeckCardsForUser() shape

---

## Incomplete Deck Edge Case

| Option | Description | Selected |
|--------|-------------|----------|
| Skip silently | Don't show shortfall for unassigned slots — deck is WIP, confusing to show "missing" before card is chosen | ✓ |
| Show a notice | Display "No leader assigned yet" message at top of want list | |

**User's choice:** Skip silently — null leader/base slots produce no shortfall in either per-deck or combined want list.

**Notes:** Confirmed consistent behavior for both per-deck (WantListTab) and combined (/api/want-list). Null slots are ignored throughout.

---

## WantListTab Prop Interface

| Option | Description | Selected |
|--------|-------------|----------|
| Extend deckCards array | Inject leader/base as `{cardDefinitionId, quantity: 1}` entries at call site. No interface change to WantListTab. | ✓ |
| Add leaderId/baseId props | Add explicit nullable props to WantListTab's interface | |

**User's choice:** Extend deckCards array at the call site in deck-builder.tsx.

**Notes:** User confirmed via preview selection. Empty-state check (`deckCards.length === 0`) confirmed correct as-is — only fires when deck has no leader, no base, and no main deck cards.

---

## getDeckCardsForUser() Shape

| Option | Description | Selected |
|--------|-------------|----------|
| Merge into same result set | Extend function to emit synthetic leader/base rows (quantity: 1, isSideboard: false). Uniform shape, caller unchanged. | ✓ |
| Separate query function | New `getDeckLeaderBaseForUser()` alongside existing, combined in route | |

**User's choice:** Merge into same result set — synthetic rows appended to existing query result.

**Notes:** User confirmed via preview selection. Normal variant filter (`variantType = 'Normal'`) for image URL join confirmed correct for leader/base rows.

---

## Claude's Discretion

- Join implementation detail (UNION vs separate query + concat in application layer) left to planner/executor.

## Deferred Ideas

None — discussion stayed within phase scope.
