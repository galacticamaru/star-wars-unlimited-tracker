---
phase: 03-collection
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [src/lib/filter-cards.ts, src/components/catalog/catalog-client.tsx, src/components/catalog/top-bar.tsx]
autonomous: true
requirements: [COLLECT-01, COLLECT-02]
must_haves:
  truths:
    - "Catalog filters persist in the URL"
    - "Users can filter by Rarity, Arena, and Cost"
---

<objective>
Implement URL-synced search and filters using `nuqs` and expand the catalog filtering logic to support advanced card metadata.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: URL Sync with nuqs</name>
  <files>package.json, src/components/catalog/catalog-client.tsx</files>
  <action>
    Install `nuqs` (npm install nuqs). Replace `useState` for filters in `CatalogClient` with `useQueryState`.
    Ensure 'q', 'sets', 'types', and 'aspects' are synced to URL.
  </action>
  <verify>Updating filters changes URL; refreshing preserves filters</verify>
  <done>URL is source of truth for filters</done>
</task>

<task type="auto">
  <name>Task 2: Advanced Filter Expansion</name>
  <files>src/lib/filter-cards.ts, src/components/catalog/top-bar.tsx</files>
  <action>
    Add Arena, Trait, Rarity, Keyword, and Cost (0-9+) to FilterState and filterCards function.
    Update TopBar to multi-line layout (D-06) to fit new filter dropdowns.
  </action>
  <verify>npm test src/lib/filter-cards.test.ts</verify>
  <done>All 5 new filters functional and display in UI</done>
</task>
</tasks>
