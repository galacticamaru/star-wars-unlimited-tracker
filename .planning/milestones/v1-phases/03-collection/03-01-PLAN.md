---
phase: 03-collection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/db/schema.ts, src/app/api/collection/route.ts, src/db/queries/collection.ts]
autonomous: true
requirements: [COLLECT-01]
must_haves:
  truths:
    - "Database can store owned counts per card identity"
    - "API returns owned counts for the catalog"
  artifacts:
    - path: "src/db/queries/collection.ts"
      provides: "Collection CRUD"
---

<objective>
Establish the database foundation for collection tracking and create the primary API endpoint for reading and updating card counts.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Collection Schema & Migration</name>
  <files>src/db/schema.ts</files>
  <action>
    Add `userCollections` table to schema:
    - `userId`: integer, not null (D-04: default 1)
    - `cardDefinitionId`: references cardDefinitions.id, not null
    - `count`: integer, not null, default 0
    - Composite PK: [userId, cardDefinitionId]
    Run `npx drizzle-kit push`.
  </action>
  <verify>drizzle-kit verify or check DB directly</verify>
  <done>Table exists in database</done>
</task>

<task type="auto">
  <name>Task 2: Collection API Endpoint</name>
  <files>src/app/api/collection/route.ts, src/db/queries/collection.ts</files>
  <action>
    Create GET (fetch all counts for user 1) and POST (upsert count for specific card) endpoints.
    Include a LEFT JOIN in the GET handler to associate counts with card definition IDs.
  </action>
  <verify>curl -X GET /api/collection</verify>
  <done>API returns JSON mapping card IDs to counts</done>
</task>
</tasks>
