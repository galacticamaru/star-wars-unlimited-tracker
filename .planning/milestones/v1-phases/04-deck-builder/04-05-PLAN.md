---
phase: 04-deck-builder
plan: 05
type: execute
wave: 5
depends_on: ["04-04"]
files_modified: [src/lib/export.ts, src/app/api/decks/[id]/export/route.ts, src/app/api/decks/[id]/route.ts, src/app/decks/[id]/page.tsx, __tests__/api-deck-validation.test.ts]
autonomous: true
requirements: [DECK-01, DECK-02, DECK-05]

must_haves:
  truths:
    - "Decks can be exported in Melee (text) and raw JSON formats"
    - "Illegal decks cannot be saved as completed (non-draft)"
    - "The deck builder experience is polished and functional"
  artifacts:
    - path: "src/lib/export.ts"
      provides: "Formatters for Melee and JSON"
    - path: "src/app/api/decks/[id]/route.ts"
      provides: "Legality enforcement during save"
  key_links:
    - from: "src/app/api/decks/[id]/route.ts"
      to: "src/lib/deck-validation.ts"
      via: "Server-side check"
---

<objective>
Implement deck export functionality, enforce server-side legality for non-draft decks, and perform final polish.

Purpose: Ensure data integrity and provide standard export options.
Output: Downloadable deck files and a robust API that prevents illegal saves.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-deck-builder/04-RESEARCH.md
@src/lib/deck-validation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce Legality in PATCH Route</name>
  <files>src/app/api/decks/[id]/route.ts, __tests__/api-deck-validation.test.ts</files>
  <action>
    Update the `PATCH /api/decks/[id]` handler:
    - Import `validateDeck` from `src/lib/deck-validation.ts`.
    - If `isDraft === false` in the request body:
      - Fetch full card data for all cards in the request (needed for validation).
      - Call `validateDeck`.
      - If `isValid` is `false`, return `400 Bad Request` with the list of `errors`.
    - If `isDraft === true`, skip validation and save normally.
    - Create `__tests__/api-deck-validation.test.ts` to verify this enforcement.
  </action>
  <verify>
    <automated>npx vitest __tests__/api-deck-validation.test.ts --run</automated>
  </verify>
  <done>API rejects illegal decks when they are not marked as drafts.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Export Logic and API</name>
  <files>src/lib/export.ts, src/app/api/decks/[id]/export/route.ts</files>
  <action>
    Create export formatters and download routes:
    - `toMeleeFormat(deck, cards)`: Returns string in `Quantity CardName [SET] [NUMBER]` format.
    - `toJSONFormat(deck, cards)`: Returns serialized JSON.
    - `GET /api/decks/[id]/export?format=melee|json`: Returns file with `Content-Disposition: attachment`.
  </action>
  <verify>
    <automated>curl -I "http://localhost:3000/api/decks/1/export?format=melee"</automated>
  </verify>
  <done>Export functionality is available via API for Melee and JSON formats.</done>
</task>

<task type="auto">
  <name>Task 3: Final Polish and UI Wiring</name>
  <files>src/app/decks/[id]/page.tsx, src/components/decks/deck-builder.tsx</files>
  <action>
    - Add "Export" dropdown to the builder UI.
    - Add a "Draft" toggle or "Finish Deck" button that sets `isDraft: false` and saves.
    - Implement "Dirty State" warning using `beforeunload` or Next.js router events.
    - Show validation errors from API if a non-draft save fails.
  </action>
  <verify>
    <automated>npx vitest src/components/decks/deck-builder.test.tsx --run</automated>
  </verify>
  <done>User can finalize decks, export them, and receives clear feedback on illegal states.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Export Download | Server sending file to client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | Information Disclosure | Export API | mitigate | Ensure userId check is performed before allowing deck export |
</threat_model>

<verification>
Run `npx vitest __tests__/api-deck-validation.test.ts --run`.
</verification>

<success_criteria>
- Illegal decks can be saved as drafts.
- Illegal decks return 400 when attempting to save with `isDraft: false`.
- Melee export matches standard format.
</success_criteria>

<output>
After completion, create `.planning/phases/04-deck-builder/04-05-SUMMARY.md`
</output>
