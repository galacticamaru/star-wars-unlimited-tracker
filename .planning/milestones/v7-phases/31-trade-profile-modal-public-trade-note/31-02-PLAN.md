---
phase: 31-trade-profile-modal-public-trade-note
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/dialog.tsx
  - src/components/ui/textarea.tsx
autonomous: true
requirements: [BINDER-15, BINDER-16]
must_haves:
  truths:
    - "A centered, dismissible `Dialog` component exists as a sibling to `Sheet`, built on the same Base UI dialog primitive."
    - "A `Textarea` component exists, styled to match the existing `Input`, wrapping a native `<textarea>`."
  artifacts:
    - "src/components/ui/dialog.tsx — `Dialog`, `DialogTrigger`, `DialogClose`, `DialogContent`, `DialogHeader`, `DialogFooter`, `DialogTitle`, `DialogDescription`"
    - "src/components/ui/textarea.tsx — `Textarea`"
  key_links:
    - "`DialogContent` renders Portal + Backdrop + centered Popup with a close button carrying `aria-label=\"Close\"` (UI-SPEC Surface 2)."
  prohibitions:
    - "MUST NOT import from `@radix-ui/*` anywhere (hard project constraint, AGENTS.md) — use `@base-ui/react/dialog`."
    - "MUST NOT introduce a new npm dependency — Base UI is already installed."
    - "MUST NOT change `DialogTitle`'s inherited `font-medium` weight (mirror `SheetTitle` verbatim; UI-SPEC Typography inherited note)."
---

<objective>
Create the two new shadcn-style UI primitives this phase needs: a centered `Dialog` (the modal shell) and a `Textarea` (the trade-note input). Both mirror existing repo components — `Dialog` is a sibling to `src/components/ui/sheet.tsx` (same `@base-ui/react/dialog` primitive, centered positioning instead of side-anchored), `Textarea` mirrors `src/components/ui/input.tsx`'s Tailwind styling around a native `<textarea>` (Base UI ships no textarea primitive; RESEARCH Alternatives Considered).

Purpose: These are pure UI infrastructure with no business logic, isolated here so Plan 03 can compose the profile modal without also authoring primitives. No file overlap with Plan 01 → runs in parallel (Wave 1).

Output: `dialog.tsx` and `textarea.tsx` under `src/components/ui/`.
</objective>

<execution_context>
@/Users/alan/Documents/Repos/Personal/star-wars-unlimited-tracker/.claude/gsd-core/workflows/execute-plan.md
@/Users/alan/Documents/Repos/Personal/star-wars-unlimited-tracker/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/31-trade-profile-modal-public-trade-note/31-RESEARCH.md
@.planning/phases/31-trade-profile-modal-public-trade-note/31-UI-SPEC.md

@src/components/ui/sheet.tsx
@src/components/ui/input.tsx
@src/components/ui/button.tsx
@AGENTS.md
</context>

<artifacts>
Symbols this plan produces (for Plan 03):
- `Dialog`, `DialogTrigger`, `DialogClose`, `DialogContent`, `DialogHeader`, `DialogFooter`, `DialogTitle`, `DialogDescription` (src/components/ui/dialog.tsx)
- `Textarea` (src/components/ui/textarea.tsx)
</artifacts>

<!-- planner-discipline-allow: @radix-ui -->

<tasks>

<task type="auto">
  <name>Task 1: Create the centered Dialog primitive</name>
  <files>src/components/ui/dialog.tsx</files>
  <read_first>
    - src/components/ui/sheet.tsx (mirror its export shape and part-wrapping pattern exactly)
    - src/components/ui/button.tsx (for the ghost `icon-sm` close button variant)
    - 31-RESEARCH.md → Pattern 3 (Base UI Dialog parts and centered positioning)
    - 31-UI-SPEC.md → "Surface 2 — Profile modal" and Component Inventory
  </read_first>
  <action>
    Create a `'use client'` component importing `Dialog as DialogPrimitive from "@base-ui/react/dialog"` (NEVER `@radix-ui`). Mirror `sheet.tsx`'s structure and export the same shape renamed for dialog: `Dialog` (Root), `DialogTrigger`, `DialogClose`, `DialogContent`, `DialogHeader`, `DialogFooter`, `DialogTitle`, `DialogDescription`.

    `DialogContent` wraps `DialogPrimitive.Portal` + `DialogPrimitive.Backdrop` (`fixed inset-0 z-50 bg-black/10 supports-backdrop-filter:backdrop-blur-xs`, with the same starting/ending opacity transition classes `sheet.tsx` uses) + `DialogPrimitive.Popup`. The Popup is CENTERED-fixed: `fixed top-1/2 left-1/2 z-50 w-full max-w-md -translate-x-1/2 -translate-y-1/2 rounded-lg border bg-popover p-6 shadow-lg text-popover-foreground` (UI-SPEC Surface 2; spacing lg=`p-6`, `max-w-md`). Accept a `showCloseButton = true` prop; when true, render a top-right close using `DialogPrimitive.Close` rendering a `Button variant="ghost" size="icon-sm"` at `absolute top-3 right-3` containing an `XIcon` and an `sr-only` "Close" span — and it MUST carry `aria-label="Close"` (required, UI-SPEC Surface 2).

    `DialogTitle` mirrors `SheetTitle` verbatim: `font-heading text-base font-medium text-foreground` — do not alter the inherited weight. `DialogHeader`/`DialogFooter` mirror `SheetHeader`/`SheetFooter` (flex column, padding). `DialogDescription` mirrors `SheetDescription` (`text-sm text-muted-foreground`).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>grep -q '@base-ui/react/dialog' src/components/ui/dialog.tsx && test $(grep -v '^\s*//' src/components/ui/dialog.tsx | grep -c '@radix-ui') -eq 0 && grep -q 'aria-label="Close"' src/components/ui/dialog.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Exports match `sheet.tsx`'s shape (Root/Trigger/Close/Content/Header/Footer/Title/Description).
    - Popup is centered (`-translate-x-1/2 -translate-y-1/2`, `max-w-md`).
    - Close button has `aria-label="Close"`; no `@radix-ui` import anywhere.
  </acceptance_criteria>
  <done>A centered, dismissible Dialog primitive built on Base UI is available for the modal.</done>
</task>

<task type="auto">
  <name>Task 2: Create the Textarea primitive</name>
  <files>src/components/ui/textarea.tsx</files>
  <read_first>
    - src/components/ui/input.tsx (mirror the Tailwind class string and `data-slot` convention)
    - 31-RESEARCH.md → Code Examples "Textarea with character counter" and Alternatives Considered (no Base UI textarea primitive)
  </read_first>
  <action>
    Create a `Textarea` component wrapping a native `<textarea>` (there is no Base UI textarea part). Type it as `React.ComponentProps<"textarea">`, set `data-slot="textarea"`, and apply Tailwind classes matching `input.tsx` but sized for multi-line: `min-h-16 w-full rounded-lg border border-input bg-transparent px-2.5 py-1.5 text-base transition-colors outline-none placeholder:text-muted-foreground focus-visible:border-ring focus-visible:ring-3 focus-visible:ring-ring/50 disabled:pointer-events-none disabled:opacity-50 md:text-sm dark:bg-input/30`, merged via `cn(...)`. Spread remaining props (so `value`, `onChange`, `maxLength`, `rows`, `placeholder` pass through). No character-counter logic here — that lives in the modal (Plan 03).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>grep -q 'data-slot="textarea"' src/components/ui/textarea.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `Textarea` renders a native `<textarea>` with Input-matching styling and forwards all textarea props.
    - `tsc --noEmit` is clean.
  </acceptance_criteria>
  <done>A styled Textarea primitive is available for the trade-note field.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | These are presentational primitives with no data flow, network, or trust boundary of their own. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-04 | Tampering | new UI primitives | low | accept | No data handling, no `dangerouslySetInnerHTML`, no user input parsed as markup. Rendering safety is enforced where these are consumed (Plan 03 modal, Plan 04 callout). |
| T-31-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages — `@base-ui/react`, `lucide-react` already installed (RESEARCH Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- No `@radix-ui` import in either file; `@base-ui/react/dialog` used for the Dialog.
- Dialog close button exposes `aria-label="Close"`.
</verification>

<success_criteria>
`Dialog` and `Textarea` primitives exist, match the repo's existing `Sheet`/`Input` conventions, and are ready for Plan 03 to compose the profile modal — with zero new dependencies and no `@radix-ui`.
</success_criteria>

<output>
Create `.planning/phases/31-trade-profile-modal-public-trade-note/31-02-SUMMARY.md` when done.
</output>
