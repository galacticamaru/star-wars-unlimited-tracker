---
phase: 30-unified-search-driven-add-flow
plan: 02
type: execute
wave: 2
depends_on: [30-01]
files_modified:
  - src/components/binder/variant-trade-sheet.tsx
autonomous: true
requirements: [BINDER-12, BINDER-13, BINDER-14]
must_haves:
  truths:
    - "Opening the sheet for a card lists every variant of that card — owned AND unowned (no ownedCount > 0 filter)"
    - "Each variant row exposes two independent steppers: a trade stepper (gated by ownership) and a want stepper (always available)"
    - "The sheet passes each variant's want quantity and ownership into the correct section"
  artifacts: []
  key_links:
    - "VariantTradeSheet composes the gated VariantTradeSection (Plan 01 T1) and the new VariantWantSection (Plan 01 T2)"
    - "SheetPrinting carries id, variantType, ownedCount, tradeQuantity, quantity so both steppers initialize correctly"
    - "onWantQuantityChange prop is threaded to VariantWantSection.onQuantityChange"
---

<objective>
Extend `VariantTradeSheet` so it presents every variant of a card with two independent steppers — trade (ownership-gated) and want (always available) — over the building blocks from Plan 01.

Purpose: This is the variant-picker + destination surface behind BINDER-12/13/14. Isolating it as its own wave keeps the prop contract stable before the page (Plan 03) consumes it.
Output: An extended `VariantTradeSheet` accepting want quantity + a want-change handler, rendering the want section alongside the gated trade section, showing all printings.
</objective>

<execution_context>
@/Users/alan/Documents/Repos/Personal/star-wars-unlimited-tracker/.claude/gsd-core/workflows/execute-plan.md
@/Users/alan/Documents/Repos/Personal/star-wars-unlimited-tracker/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-unified-search-driven-add-flow/30-CONTEXT.md
@.planning/phases/30-unified-search-driven-add-flow/30-PATTERNS.md
@.planning/phases/30-unified-search-driven-add-flow/30-UI-SPEC.md

# The file being extended + the two sections it composes (delivered by Plan 01)
@src/components/binder/variant-trade-sheet.tsx
@src/components/catalog/variant-trade-section.tsx
@src/components/catalog/variant-collection-section.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend the sheet's printing shape and prop contract</name>
  <files>src/components/binder/variant-trade-sheet.tsx</files>
  <read_first>
    @src/components/binder/variant-trade-sheet.tsx
    @.planning/phases/30-unified-search-driven-add-flow/30-PATTERNS.md (§variant-trade-sheet "Required changes")
  </read_first>
  <action>
    Update the `SheetPrinting` interface: add `quantity: number` (the manual-want quantity for that printing). Keep the existing `id`, `variantType`, `ownedCount`, `tradeQuantity` fields — `ownedCount` now feeds the trade section's ownership gating (Plan 01 T1). Add `onWantQuantityChange: (cardPrintingId: number, quantity: number) => void` to `VariantTradeSheetProps` alongside the existing `onTradeQuantityChange`. Do not change the `open`/`onOpenChange`/`cardName`/`cardSubtitle`/`printings` props. This task establishes the contract only; composition happens in Task 2.
  </action>
  <verify>
    <automated>npm run build</automated>
    Also: `grep -n "quantity" src/components/binder/variant-trade-sheet.tsx` shows the new want-quantity field, and `grep -n "onWantQuantityChange" src/components/binder/variant-trade-sheet.tsx` shows the new prop.
  </verify>
  <acceptance_criteria>
    - `SheetPrinting` includes `quantity: number` and retains `ownedCount` + `tradeQuantity`
    - `VariantTradeSheetProps` includes `onWantQuantityChange`
    - `npm run build` passes
  </acceptance_criteria>
  <done>The sheet's types carry want quantity + ownership and expose a want-change handler prop. Build passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Compose the gated trade section and the want section, show all printings</name>
  <files>src/components/binder/variant-trade-sheet.tsx</files>
  <read_first>
    @src/components/binder/variant-trade-sheet.tsx
    @src/components/catalog/variant-want-section.tsx (created in Plan 01 T2)
    @.planning/phases/30-unified-search-driven-add-flow/30-UI-SPEC.md (§Interaction Contract, §Layout Contract)
  </read_first>
  <action>
    Import `VariantWantSection` and render it inside the existing `flex flex-col gap-6` body container, alongside the existing `VariantCollectionSection` and `VariantTradeSection`. Pass `printings={printings}` to `VariantWantSection` and wire its `onQuantityChange={onWantQuantityChange}`. Leave `VariantTradeSection`'s `onQuantityChange={onTradeQuantityChange}` as-is; the trade section now self-gates on each printing's `ownedCount` (Plan 01 T1), so no extra prop is needed there. Per D-05 the sheet must show every printing — the parent (Plan 03) passes unfiltered printings, so remove reliance on an owned-only path here: repurpose the `printings.length === 0` guard copy to "No printings found for this card." (a pathological case), since owned and unowned variants now both render. Keep the header, the `side="right"` sheet layout, and section spacing unchanged.
  </action>
  <verify>
    <automated>npm run build</automated>
    Also: `grep -n "VariantWantSection" src/components/binder/variant-trade-sheet.tsx` shows the import + render, and `grep -n "onTradeQuantityChange" src/components/binder/variant-trade-sheet.tsx` confirms the trade wiring is retained.
  </verify>
  <acceptance_criteria>
    - `VariantWantSection` is imported and rendered in the sheet body with `onQuantityChange={onWantQuantityChange}`
    - `VariantTradeSection` and `VariantCollectionSection` still render; trade section gates per `ownedCount`
    - The empty-state guard copy no longer implies "owned only"
    - `npm run build` passes
  </acceptance_criteria>
  <done>The sheet renders collection, ownership-gated trade, and always-available want steppers for every variant. Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client component → existing authenticated write endpoints | The sheet only forwards user actions to `/api/trade` and `/api/binder/wants` via handler props; it introduces no new endpoint or data source |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-04 | Elevation of Privilege | trade stepper rendered in sheet | medium | mitigate | Trade stepper is disabled for unowned variants (client, Plan 01 T1) AND rejected server-side (Plan 01 T3); the sheet composes both — no independent write path |
| T-30-05 | Information Disclosure | sheet variant list showing unowned variants | low | accept | Variant metadata (name/subtitle/variantType) is public catalog data; showing unowned variants exposes no private data — only the current user's own counts are pre-filled |
| T-30-SC | Tampering (supply chain) | npm installs | low | accept | No new dependencies this plan; `VariantWantSection` is an in-repo import |
</threat_model>

<verification>
- `npm run build` and `npm run lint` pass.
- Sheet composes `VariantCollectionSection`, gated `VariantTradeSection`, and `VariantWantSection` (grep in Task 2).
- Sheet prop contract exposes `onWantQuantityChange` and `SheetPrinting.quantity` (grep in Task 1).
</verification>

<success_criteria>
- Selecting a card opens a sheet listing every variant with an ownership-gated trade stepper and an always-available want stepper (BINDER-12/13/14), ready for the page to drive in Plan 03.
</success_criteria>

<output>
Create `.planning/phases/30-unified-search-driven-add-flow/30-02-SUMMARY.md` when done.
</output>
