---
phase: 32-combined-wants-exclusions-list
plan: 02
type: execute
wave: 2
depends_on: [32-01]
files_modified: []
autonomous: false
requirements: [BINDER-18, BINDER-19, BINDER-20]

must_haves:
  truths:
    - "A human confirms the combined Looking For list renders as two labeled sections (Deck Wants over Manual Wants) with count pills and no standalone Exclusions section"
    - "A human confirms the exclude→restore round trip on a Deck Want works and the dimmed excluded row sinks to the bottom"
    - "A human confirms the manual want quantity stepper and remove control work from the list without a page reload"
  artifacts:
    - "Human verification sign-off recorded in the plan summary"
  key_links:
    - "Ban → onToggleExclusion(id, true) → dimmed row at bottom; restore → onToggleExclusion(id, false) → active row"
    - "stepper/remove → onUpdateWantQuantity / onRemoveWant → live list update"
---

<objective>
Human visual/functional verification of the redesigned `ManageWantsList` (this is a UI phase — ROADMAP UI hint: yes). Confirms the three phase success criteria hold in the running app after plan 32-01 ships.

Purpose: Sign-off that BINDER-18/19/20 are satisfied as observable UI behavior.
Output: Recorded verification result.
</objective>

<execution_context>
@/Users/alan/Documents/Repos/Personal/star-wars-unlimited-tracker/.claude/gsd-core/workflows/execute-plan.md
@/Users/alan/Documents/Repos/Personal/star-wars-unlimited-tracker/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/32-combined-wants-exclusions-list/32-CONTEXT.md
@src/components/binder/manage-wants-list.tsx
@src/app/binder/manage/page.tsx
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human verification of the combined Looking For list</name>
  <action>Human verifies the redesigned ManageWantsList in the running app against the four checks in how-to-verify (combined two-section layout, exclude→restore round trip, manual quantity/remove, no console errors). No code changes in this plan.</action>
  <what-built>
    `ManageWantsList` (right column of `/binder/manage`) redesigned into one "Looking For" list with two labeled sections — **Deck Wants** (deck-driven auto-wants, active first, excluded dimmed at bottom) over **Manual Wants** (per-printing, with quantity stepper + remove + conditional variant badge). The standalone Exclusions section was removed; exclude/restore and manual quantity/remove behaviors are preserved. No API/schema changes.
  </what-built>
  <how-to-verify>
    1. Start the app (`npm run dev`) and open `/binder/manage`. Ensure the account has at least one deck (so auto-wants appear) and add at least one manual want via the search-add flow if none exist.
    2. In the right column, confirm you see **one list with two labeled sections**: "Deck Wants" (with a count pill) on top and "Manual Wants" (with a count pill) below — and **no separate "Exclusions" section** and no per-row Auto/Manual badges. (BINDER-18)
    3. On an active Deck Want, click the exclude (Ban) control. Confirm the row becomes **dimmed (~50% opacity)**, shows an **"Excluded" tag**, gains a **restore** control, and **sorts to the bottom** of the Deck Wants section. Then click restore and confirm the row returns to active (full round trip). (BINDER-19)
    4. On a Manual Want, click − and + on the stepper and confirm the quantity updates live (and − does not go below the removal point); click [x] and confirm the row is removed — all without a page reload. Confirm a non-Normal printing shows its variant badge (e.g. FOIL) and a Normal printing shows none. (BINDER-20)
    5. Confirm no console errors during these interactions.
  </how-to-verify>
  <resume-signal>Type "approved" if all four checks pass, or describe what looked or behaved wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → existing `/api/binder/*` | Unchanged by this phase; verification exercises existing endpoints only, no new surface. |

## STRIDE Threat Register

Verification-only plan; introduces no code, endpoints, or dependencies.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-V1 | Tampering | Reused `/api/binder/*` during manual verification | low | accept | No new payloads or call sites; existing auth/validation unchanged and out of scope. |
</threat_model>

<verification>
- Human confirms all four checks in the checkpoint pass in the running app.
</verification>

<success_criteria>
- Combined two-section list confirmed (BINDER-18).
- Exclude→restore round trip confirmed (BINDER-19).
- Manual quantity change + remove confirmed from the list (BINDER-20).
</success_criteria>

<output>
Create `.planning/phases/32-combined-wants-exclusions-list/32-02-SUMMARY.md` when done.
</output>
