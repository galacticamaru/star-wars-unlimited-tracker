---
phase: 27-decks-route-performance
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/db/queries/decks.test.ts
  - __tests__/api-deck-revalidate.test.ts
  - src/components/decks/deck-builder.test.tsx
  - src/app/decks/page.test.tsx
autonomous: true
requirements: [PERF-07, PERF-08]
must_haves:
  truths:
    - "Running the four Phase 27 test files exits without a DATABASE_URL load error"
    - "Every Phase 27 behavior has at least one failing-or-todo test that will turn green once implemented"
  artifacts:
    - path: "src/db/queries/decks.test.ts"
      provides: "Source-inspection stubs for 'use cache' + cacheTag in getDecks / getDeckWithCards (PERF-07)"
      contains: "describe('getDecks"
    - path: "__tests__/api-deck-revalidate.test.ts"
      provides: "Mock-based stubs asserting revalidateTag(tag,'max') in POST/PATCH/DELETE deck handlers (PERF-07)"
      contains: "revalidateTag"
    - path: "src/components/decks/deck-builder.test.tsx"
      provides: "Source-inspection stubs for startTransition wrapping dispatch in handleDeckUpdate (PERF-08)"
      contains: "startTransition"
    - path: "src/app/decks/page.test.tsx"
      provides: "Retargeted test on DecksClient asserting router.refresh() after delete success (PERF-07)"
      contains: "refresh: vi.fn()"
  key_links:
    - from: "__tests__/api-deck-revalidate.test.ts"
      to: "src/app/api/decks/route.ts"
      via: "dynamic import of POST handler with next/cache + db/queries/decks mocked"
      pattern: "import\\('.*api/decks/route'\\)"
    - from: "src/app/decks/page.test.tsx"
      to: "src/components/decks/decks-client.tsx"
      via: "render(<DecksClient .../>) with mocked useRouter"
      pattern: "DecksClient"
---

<objective>
Create the Wave 0 test infrastructure for Phase 27 so every PERF-07 and PERF-08 behavior has an automated check that fails (or is `it.todo`) before implementation and turns green after. Also retarget the existing `src/app/decks/page.test.tsx` — it currently imports the RSC `./page` (which throws `DATABASE_URL ... is not set` at module load) and tests stale "Loading decks..." behavior that no longer exists in the component. PERF-09's checks reuse the already-green `src/app/decks/[id]/loading.test.tsx`, so no new file is needed there.

Purpose: Establish the RED baseline the Nyquist sampling loop depends on (VALIDATION.md sampling command runs `decks.test.ts` + `api-deck-revalidate.test.ts` after every task commit). Without these stubs, Wave 1/2 tasks have no automated verify target.

Output: Two new test files, one retargeted test file, plus a corrected smoke test file — all importing without a DATABASE_URL crash.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-decks-route-performance/27-RESEARCH.md
@.planning/phases/27-decks-route-performance/27-PATTERNS.md
@.planning/phases/27-decks-route-performance/27-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create source-inspection stub for deck query caching (decks.test.ts)</name>
  <read_first>
    - src/db/queries/decks.ts — current getDecks (lines 5-11) and getDeckWithCards (lines 13-30); confirm no caching directives present yet
    - src/db/queries/catalog.test.ts — exact analog stub structure (// @vitest-environment node header, it.todo pattern) per 27-PATTERNS.md lines 244-267
    - .planning/phases/27-decks-route-performance/27-PATTERNS.md — section "src/db/queries/decks.test.ts (new test file)" for the canonical stub body
  </read_first>
  <action>
    Create src/db/queries/decks.test.ts. Use the `// @vitest-environment node` header on line 1 (matches catalog.test.ts). DO NOT import from `@/db/queries/decks` at module top level if that import would load the DB connection — instead, read the file source via `readFileSync` and assert against its text content so the test never touches `DATABASE_URL`. Import `readFileSync` from `node:fs` and `join` from `node:path`, read `src/db/queries/decks.ts` relative to the test file, store in a `const source`.
    Write two `describe` blocks: `describe('getDecks() caching (PERF-07)', ...)` and `describe('getDeckWithCards() caching (PERF-07)', ...)`.
    Make these REAL assertions (not it.todo) so they fail RED now and pass after Plan 01:
    - getDecks: assert `source` matches the getDecks function body containing the literal `'use cache'` directive — use a regex that scopes to the getDecks function (e.g. match `export async function getDecks` followed by `'use cache'` before the next `export`).
    - getDecks: assert `source` contains the template tag `cacheTag(\`decks-user-${userId}\`)` exact substring.
    - getDeckWithCards: assert the getDeckWithCards function body contains `'use cache'`.
    - getDeckWithCards: assert `source` contains `cacheTag(\`deck-${deckId}-user-${userId}\`)` exact substring.
    - getDeckWithCards ownership guard (Pitfall 3): assert `source` still contains `and(eq(decks.id, deckId), eq(decks.userId, userId))` — caching must not remove the ownership clause.
    Because these are source-string assertions, escape backticks/dollar signs correctly in the expected substrings. Add a comment at top noting these assert source text, not runtime behavior, to avoid the DATABASE_URL trap that breaks RSC-importing tests.
  </action>
  <verify>
    <automated>npx vitest run src/db/queries/decks.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File src/db/queries/decks.test.ts exists with `// @vitest-environment node` as line 1
    - `npx vitest run src/db/queries/decks.test.ts` executes WITHOUT a `DATABASE_URL environment variable is not set` error (it may report assertion failures — that is the expected RED state)
    - The test file contains the literal expected substring `cacheTag(\`decks-user-${userId}\`)` as a value being asserted
    - The test file contains the literal expected substring `cacheTag(\`deck-${deckId}-user-${userId}\`)` as a value being asserted
    - The test file asserts presence of the ownership clause `eq(decks.userId, userId)` in getDeckWithCards
    - At least 5 distinct assertions exist across the two describe blocks
  </acceptance_criteria>
  <done>decks.test.ts runs without DB error and currently fails on the missing 'use cache'/cacheTag assertions (RED).</done>
</task>

<task type="auto">
  <name>Task 2: Create revalidateTag mock stub for deck mutation handlers (api-deck-revalidate.test.ts)</name>
  <read_first>
    - __tests__/cron-route.test.ts — analog: dynamic `await import('../src/app/api/...route')` per beforeEach, why vi.resetModules is avoided (lines 13-23)
    - __tests__/api-deck-validation.test.ts — analog mock set for deck routes: it mocks `@/lib/auth`, `@/db/queries/decks`, `@/lib/deck-validation` (lines 8-25); replicate ALL THREE mocks so the route never loads the real DB module
    - src/app/api/decks/route.ts — POST handler; createDeck call then response (lines 21-41)
    - src/app/api/decks/[id]/route.ts — PATCH (lines 36-121) and DELETE (lines 123-146) handlers; note PATCH calls validateDeck via @/lib/deck-validation
    - .planning/phases/27-decks-route-performance/27-PATTERNS.md — section "__tests__/api-deck-revalidate.test.ts" for the canonical mock + assertion body (lines 271-326)
  </read_first>
  <action>
    Create __tests__/api-deck-revalidate.test.ts with `// @vitest-environment node` on line 1.
    Add hoisted `vi.mock` calls for: `next/cache` (export `revalidateTag: vi.fn()`), `@/db/queries/decks` (export `createDeck`, `updateDeck`, `deleteDeck`, `getDeckWithCards`, `getCardsByDefinitionIds` as vi.fn() with resolved values per 27-PATTERNS.md), `@/lib/auth` (getSession resolving `{ user: { id: '7' } }`), and `@/lib/deck-validation` (export `validateDeck: vi.fn(() => ({ isValid: true, errors: [] }))` so the PATCH non-draft path passes validation). Mock `next/headers` `headers: vi.fn(async () => new Headers())` if needed for the handler to run.
    Use a `beforeEach` that calls `vi.clearAllMocks()` and re-imports the handlers (`await import('../src/app/api/decks/route')` for POST; `await import('../src/app/api/decks/[id]/route')` for PATCH/DELETE).
    Write these assertions as REAL tests (fail RED now, pass after Plan 01):
    - POST: build a NextRequest with body `{ name: 'Test Deck' }`, call POST, assert `revalidateTag` toHaveBeenCalledWith `'decks-user-7'` and `'max'`.
    - PATCH (draft save, isDraft true): call PATCH with params `{ id: '5' }` and body `{ isDraft: true, name: 'x' }`, assert revalidateTag called with `'deck-5-user-7'`,`'max'` AND with `'decks-user-7'`,`'max'` (both tags per D-04).
    - DELETE: call DELETE with params `{ id: '5' }`, assert revalidateTag called with `'deck-5-user-7'`,`'max'` AND `'decks-user-7'`,`'max'`.
    Import `revalidateTag` from `next/cache` after the mock to read the spy. Match the params shape the handlers expect: `{ params: Promise.resolve({ id: '5' }) }` (handlers `await params`).
  </action>
  <verify>
    <automated>npx vitest run __tests__/api-deck-revalidate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File __tests__/api-deck-revalidate.test.ts exists
    - `npx vitest run __tests__/api-deck-revalidate.test.ts` runs WITHOUT a `DATABASE_URL` error (DB queries module is fully mocked)
    - Test mocks `next/cache` with `revalidateTag: vi.fn()`
    - Test mocks `@/db/queries/decks`, `@/lib/auth`, and `@/lib/deck-validation`
    - POST test asserts `revalidateTag` called with `('decks-user-7', 'max')`
    - PATCH test asserts BOTH `('deck-5-user-7', 'max')` and `('decks-user-7', 'max')`
    - DELETE test asserts BOTH `('deck-5-user-7', 'max')` and `('decks-user-7', 'max')`
    - Tests currently FAIL because handlers do not yet call revalidateTag (RED state)
  </acceptance_criteria>
  <done>api-deck-revalidate.test.ts runs without DB error and fails because revalidateTag is not yet called in any handler (RED).</done>
</task>

<task type="auto">
  <name>Task 3: Create startTransition source stub + retarget page.test.tsx to DecksClient</name>
  <read_first>
    - src/components/decks/deck-builder.tsx — handleDeckUpdate (lines 245-258), handleSave (lines 283-316), React import (line 3); these are the PERF-08 + PERF-07 targets
    - src/app/decks/page.test.tsx — CURRENT broken state: imports `./page` (RSC) and tests "Loading decks..." which no longer exists; this whole file must be retargeted
    - src/components/decks/decks-client.tsx — DecksClient takes `initialDecks`/`initialWantList` props, has handleDeleteDeck (lines 77-91) where router.refresh() will be added; this is the correct render target (no DB import)
    - .planning/phases/27-decks-route-performance/27-PATTERNS.md — sections "src/components/decks/deck-builder.test.tsx" (lines 330-352) and the page.test.tsx mockRouter refresh note
  </read_first>
  <action>
    Part A — Create src/components/decks/deck-builder.test.tsx (source-inspection stub for PERF-08). Use `// @vitest-environment node` line 1. Read deck-builder.tsx source via `readFileSync`/`node:fs` (do NOT render it — it imports many client deps and the catalog; source assertion avoids that). Write `describe('deck-builder startTransition (PERF-08)', ...)` with REAL assertions (RED now):
    - assert source imports `startTransition` from `'react'` (regex: `import \{[^}]*startTransition[^}]*\} from 'react'`).
    - assert each of the three dispatch types is wrapped in startTransition: the source contains `startTransition(() =>` and the SET_LEADER, SET_BASE, and UPDATE_CARD dispatches appear inside such wrappers (assert `startTransition` count >= 3 using a non-comment-filtered match, or assert the substring `startTransition(() => {` appears and `SET_LEADER`/`SET_BASE`/`UPDATE_CARD` each appear after a startTransition opener — keep assertions concrete via indexOf ordering).
    - assert `setIsAutoFilterOverridden(false)` is NOT inside a startTransition callback (Pitfall 4): assert the source still contains the standalone `setIsAutoFilterOverridden(false);` statements.
    Part B — Replace src/app/decks/page.test.tsx ENTIRELY. Change line 1 to `/** @vitest-environment jsdom */`. Stop importing `./page`. Instead `import { DecksClient } from '@/components/decks/decks-client'`. Mock `next/navigation` `useRouter` to return `const mockRouter = { push: vi.fn(), refresh: vi.fn() }`. Stub `global.fetch`. Render `<DecksClient initialDecks={[{ id: 1, name: 'Deck to Delete', updatedAt: new Date().toISOString() }]} initialWantList={[]} />`. Set `window.confirm = vi.fn(() => true)`. Click the Delete button, mock fetch DELETE to resolve `{ ok: true }`, then `await waitFor` and assert `mockRouter.refresh` toHaveBeenCalled (RED now — refresh not yet wired). Keep a create-deck test that asserts `mockRouter.push` called with `/decks/123` and (after Plan 01) `mockRouter.refresh` called. Mock `@/components/catalog/card-item` if its import drags in heavy deps that break jsdom — replace with `vi.mock('@/components/catalog/card-item', () => ({ CardItem: () => null }))`.
  </action>
  <verify>
    <automated>npx vitest run src/components/decks/deck-builder.test.tsx src/app/decks/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/decks/deck-builder.test.tsx exists and runs WITHOUT a DATABASE_URL error
    - deck-builder.test.tsx asserts `startTransition` is imported from `'react'`
    - deck-builder.test.tsx asserts SET_LEADER, SET_BASE, and UPDATE_CARD are each wrapped in startTransition (currently FAILS / RED)
    - deck-builder.test.tsx asserts `setIsAutoFilterOverridden(false)` remains outside the transition
    - src/app/decks/page.test.tsx line 1 is `/** @vitest-environment jsdom */` and the file imports `DecksClient`, NOT `./page`
    - page.test.tsx no longer references the string `Loading decks`
    - page.test.tsx `mockRouter` object contains `refresh: vi.fn()`
    - page.test.tsx delete test asserts `mockRouter.refresh` was called (currently FAILS / RED)
    - `npx vitest run src/app/decks/page.test.tsx` runs WITHOUT a DATABASE_URL error
  </acceptance_criteria>
  <done>Both files run without DB errors; PERF-08 source assertions and the router.refresh delete assertion are RED, ready to turn green in Plans 01/02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → source files | Tests read source text / mock modules; no untrusted runtime input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-00-01 | Tampering | Test mocks hiding real behavior | mitigate | api-deck-revalidate.test.ts mocks ONLY revalidateTag/db/auth/validation — handler control flow (auth gate → mutation → revalidateTag → response) runs unmocked so the test proves real ordering |
| T-27-00-02 | Information Disclosure | Cross-user cache tag regression slips past tests | mitigate | decks.test.ts asserts the exact per-user tag substrings `decks-user-${userId}` and `deck-${deckId}-user-${userId}` plus the ownership clause `eq(decks.userId, userId)` — a tag missing userId fails the suite |
| T-27-00-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; RESEARCH.md Package Legitimacy Audit reports zero new packages |
</threat_model>

<verification>
- `npx vitest run src/db/queries/decks.test.ts __tests__/api-deck-revalidate.test.ts src/components/decks/deck-builder.test.tsx src/app/decks/page.test.tsx` runs with NO `DATABASE_URL` load errors in any of the four files.
- All four files report assertion failures or todo (RED) — none should report "no tests".
- The pre-existing unrelated DB-dependent failures (cron-route.test.ts, api-deck-validation.test.ts) are OUT OF SCOPE — do not fix them in this phase.
</verification>

<success_criteria>
- Four Phase 27 test files import and execute without a DATABASE_URL crash.
- PERF-07 cache-directive, revalidateTag, and router.refresh-after-delete checks exist and are RED.
- PERF-08 startTransition checks exist and are RED.
- VALIDATION.md sampling command (`decks.test.ts __tests__/api-deck-revalidate.test.ts`) is now runnable.
</success_criteria>

<output>
Create `.planning/phases/27-decks-route-performance/27-00-SUMMARY.md` when done
</output>
