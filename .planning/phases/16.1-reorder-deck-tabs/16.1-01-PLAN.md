---
phase: 16.1-reorder-deck-tabs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/decks/deck-builder.tsx
autonomous: true
requirements:
  - polish-16.1
must_haves:
  truths:
    - "Opening a deck shows Deck List as the active tab by default"
    - "Tab order left-to-right is: Deck List, Add Cards, Want List"
    - "All three tabs are still functional and switch views correctly"
  artifacts:
    - path: src/components/decks/deck-builder.tsx
      provides: "Updated tab order and default state"
      contains: "useState<'editor' | 'catalog' | 'want-list'>('editor')"
  key_links:
    - from: "src/components/decks/deck-builder.tsx line ~128"
      to: "initial view state"
      via: "useState default value"
      pattern: "useState.*'editor'"
    - from: "src/components/decks/deck-builder.tsx lines ~318-343"
      to: "tab button render order"
      via: "JSX sibling order"
      pattern: "Deck List.*Add Cards.*Want List"
---

<objective>
Swap the two leftmost deck-builder tabs so the order reads Deck List → Add Cards → Want List, and change the default active tab from `'catalog'` to `'editor'`.

Purpose: Users land on Deck List first, which is the most useful view once a deck has content.
Output: `src/components/decks/deck-builder.tsx` with two surgical edits — the useState default and the JSX tab button order.
</objective>

<execution_context>
@C:\Users\alan_\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\alan_\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16.1-reorder-deck-tabs/16.1-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Change default tab and reorder tab buttons in deck-builder.tsx</name>
  <files>src/components/decks/deck-builder.tsx</files>

  <read_first>
    - src/components/decks/deck-builder.tsx — read lines 125–135 (useState default) and lines 318–343 (tab button JSX block) before making any edit
  </read_first>

  <action>
Two edits in `src/components/decks/deck-builder.tsx`:

**Edit 1 — Default tab state (line ~128):**

Change:
```tsx
const [view, setView] = useState<'editor' | 'catalog' | 'want-list'>('catalog');
```
To:
```tsx
const [view, setView] = useState<'editor' | 'catalog' | 'want-list'>('editor');
```

**Edit 2 — Tab button order (lines ~318–343):**

Current order inside `<div className="flex bg-slate-100 rounded-lg p-1">`:
1. Add Cards button (`view === 'catalog'`)
2. Deck List button (`view === 'editor'`)
3. Want List button (`view === 'want-list'`)

Reorder to:
1. Deck List button (`view === 'editor'`) — move to first position
2. Add Cards button (`view === 'catalog'`) — move to second position
3. Want List button (`view === 'want-list'`) — leave in third position (no change)

Final JSX block must be:
```tsx
<div className="flex bg-slate-100 rounded-lg p-1">
    <Button
        variant={view === 'editor' ? 'secondary' : 'ghost'}
        size="sm"
        onClick={() => setView('editor')}
        className={view === 'editor' ? 'bg-white shadow-sm' : ''}
    >
        Deck List
    </Button>
    <Button
        variant={view === 'catalog' ? 'secondary' : 'ghost'}
        size="sm"
        onClick={() => setView('catalog')}
        className={view === 'catalog' ? 'bg-white shadow-sm' : ''}
    >
        Add Cards
    </Button>
    <Button
        variant={view === 'want-list' ? 'secondary' : 'ghost'}
        size="sm"
        onClick={() => setView('want-list')}
        className={view === 'want-list' ? 'bg-white shadow-sm' : ''}
    >
        Want List
    </Button>
</div>
```

No other changes. Tab styling, variant logic, onClick handlers, and all view rendering below remain identical.
  </action>

  <verify>
    <automated>
      cd "C:\Users\alan_\source\repos\galacticamaru\star-wars-unlimited-tracker" && npx tsc --noEmit 2>&1 | head -20
    </automated>
  </verify>

  <acceptance_criteria>
    - src/components/decks/deck-builder.tsx contains the string `useState<'editor' | 'catalog' | 'want-list'>('editor')` (grep: `useState.*'editor'\)`)
    - The Deck List button JSX appears BEFORE the Add Cards button JSX in deck-builder.tsx (grep: file contains `Deck List` before `Add Cards` in the tab block)
    - TypeScript compilation exits 0 with no new errors
    - The Want List button position is unchanged (still third in the tab block)
  </acceptance_criteria>

  <done>
    - Default view is `'editor'` (Deck List), not `'catalog'`
    - Tab render order in JSX: Deck List button, then Add Cards button, then Want List button
    - No TypeScript errors introduced
    - No other behavior changes made
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No new boundaries | This change is purely client-side JSX reordering and a useState default value. No data flows across trust boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.1-01 | Tampering | deck-builder.tsx tab state | accept | Pure UI rearrangement; no auth, no data, no server interaction affected |
</threat_model>

<verification>
1. Open any deck — confirm Deck List is the active tab on load (no manual tab click needed)
2. Click Add Cards — confirms tab switches and catalog renders
3. Click Want List — confirms tab switches and want list renders
4. Click Deck List — confirms tab returns to deck list
5. Tab order in the UI reads left-to-right: Deck List | Add Cards | Want List
</verification>

<success_criteria>
- Deck List is the first (leftmost) tab and is active by default when a deck opens
- Add Cards is the second tab
- Want List is the third tab (unchanged position)
- All three tabs remain functional — clicking each switches the view
- TypeScript build passes with no new errors
</success_criteria>

<output>
After completion, create `.planning/phases/16.1-reorder-deck-tabs/16.1-01-SUMMARY.md` using the summary template.
</output>
