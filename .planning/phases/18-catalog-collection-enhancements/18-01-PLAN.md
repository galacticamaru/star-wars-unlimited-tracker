---
phase: 18-catalog-collection-enhancements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/starter-decks.ts
  - src/db/queries/collection.ts
  - src/app/api/collection/starter-deck/route.ts
  - src/app/collection/page.tsx
  - src/db/queries/catalog.ts
  - src/app/cards/page.tsx
  - src/components/catalog/catalog-client.tsx
  - src/components/catalog/card-grid.tsx
  - src/components/catalog/card-item.tsx
  - tests/starter-deck-api.test.ts
  - tests/catalog-variant.test.ts
autonomous: true
requirements: [REQ-COLLECT-08, REQ-CAT-04]
must_haves:
  truths:
    - "A catalog card tile displays the art of whichever variant the user owns the most copies of"
    - "User can trigger a single action to add all cards from a starter deck"
    - "After quick-add, the user receives confirmation of how many cards were added"
  artifacts:
    - path: "src/data/starter-decks.ts"
      provides: "Starter deck definitions"
    - path: "src/db/queries/collection.ts"
      provides: "incrementVariantCount query"
    - path: "src/app/api/collection/starter-deck/route.ts"
      provides: "POST endpoint for quick-add"
    - path: "src/components/catalog/card-grid.tsx"
      provides: "Best variant art computation"
  key_links:
    - from: "src/app/cards/page.tsx"
      to: "src/components/catalog/catalog-client.tsx"
      via: "printingArtMap prop"
    - from: "src/app/collection/page.tsx"
      to: "src/app/api/collection/starter-deck/route.ts"
      via: "POST request"
---

<objective>
Enhance the collection experience by dynamically showing the art of the highest-owned variant in the catalog, and provide a single-click Quick-Add for official starter decks.

Purpose: Make the visual catalog accurately reflect the user's specific collection (e.g., showing Hyperspace art if they own it), and reduce friction for new players adding known starter sets.
Output: Working variant art resolution in the catalog and a functional starter deck importer on the collection page.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-catalog-collection-enhancements/18-RESEARCH.md

<interfaces>
From src/app/api/collection/collection-shape.ts:
```typescript
export interface CollectionMap {
  [cardDefinitionId: number]: {
    total: number;
    variants: Record<number, number>; // cardPrintingId -> count
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Starter Deck API & Database</name>
  <files>src/data/starter-decks.ts, src/db/queries/collection.ts, src/app/api/collection/starter-deck/route.ts, tests/starter-deck-api.test.ts</files>
  <behavior>
    - Test 1: POST /api/collection/starter-deck validates deckId and increments `user_printing_collections` for Normal variants.
    - Test 2: `recomputeTotal` is called for all affected definitions, updating `user_collections`.
  </behavior>
  <action>
    1. Create `src/data/starter-decks.ts` with the static lists provided in 18-RESEARCH.md.
    2. Add `incrementVariantCount(cardPrintingId: number, qtyToAdd: number, userId: number)` to `collection.ts` using `sql\`${userPrintingCollections.count} + ${qtyToAdd}\`` on conflict.
    3. Implement `POST /api/collection/starter-deck` that reads `deckId`, queries `card_printings` for matching `setCode` + `collectorNumber` + `variantType='Normal'`, calls `incrementVariantCount` for each, then `recomputeTotal` for distinct `cardDefinitionId`s. Return `{ cardsAdded }`. (Enforce auth `session.user.id`).
  </action>
  <verify>
    <automated>npx vitest run tests/starter-deck-api.test.ts</automated>
  </verify>
  <done>API successfully increments counts and total collections without overwriting.</done>
</task>

<task type="auto">
  <name>Task 2: Starter Deck UI</name>
  <files>src/app/collection/page.tsx</files>
  <action>
    Add a new section for "Starter Decks" to the Collection page, alongside the CSV Import.
    - Use a `<select>` or similar dropdown containing the known starter decks from `src/data/starter-decks.ts`.
    - Provide an "Add to Collection" button.
    - On success, set a local success state displaying: "Added {N} cards from {Deck Name} to your collection." (following D-10).
    - Call `router.refresh()` to update the page data.
  </action>
  <verify>
    <automated>npm run lint -- src/app/collection/page.tsx</automated>
  </verify>
  <done>UI allows selecting a starter deck, sends the POST request, and displays success message inline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Catalog Variant Art Display</name>
  <files>src/db/queries/catalog.ts, src/app/cards/page.tsx, src/components/catalog/catalog-client.tsx, src/components/catalog/card-grid.tsx, src/components/catalog/card-item.tsx, tests/catalog-variant.test.ts</files>
  <behavior>
    - Test 1: `CardGrid` selects the correct `frontArtUrl` based on highest owned count.
    - Test 2: Tie-breaking uses precedence (Showcase > Hyperspace Foil > Hyperspace > Foil > Normal).
    - Test 3: Falls back to Normal art if zero owned.
  </behavior>
  <action>
    1. In `catalog.ts`, create `getPrintingArtMap()` returning a map of `cardPrintingId` to `{ variantType, frontArtUrl }`.
    2. In `cards/page.tsx`, await `getPrintingArtMap()` and pass it to `<CatalogClient printingArtMap={...} />`.
    3. Thread `printingArtMap` through `CatalogClient` to `CardGrid`.
    4. In `CardGrid`, compute `bestVariantArtUrl` for each card using `collection[card.id]?.variants` and `printingArtMap` according to D-02 and D-04. (Parse the string keys of variants as numbers!).
    5. Pass `bestVariantArtUrl` (if exists) as the `frontArtUrl` prop to `CardItem` (overriding the default Normal art).
  </action>
  <verify>
    <automated>npx vitest run tests/catalog-variant.test.ts</automated>
  </verify>
  <done>Catalog correctly displays variant art based on the user's highest owned count.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | Untrusted input (deckId) crosses to server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | starter-deck route | mitigate | Validate deckId against the known static list before processing; enforce session.user.id |
</threat_model>

<verification>
1. Starter deck added via UI successfully increments variants in DB.
2. Catalog tiles show Hyperspace art when a user owns more Hyperspace variants than Normal variants.
3. Tests pass for both tie-breaking logic and API increment safety.
</verification>

<success_criteria>
- `POST /api/collection/starter-deck` works reliably and is idempotent-safe (increments).
- Catalog `CardGrid` dynamically falls back to highest variant art via `printingArtMap`.
- All Phase 18 ROADMAP success criteria met.
</success_criteria>

<output>
After completion, create `.planning/phases/18-catalog-collection-enhancements/18-01-SUMMARY.md`
</output>