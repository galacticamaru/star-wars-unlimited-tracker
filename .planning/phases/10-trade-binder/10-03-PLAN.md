---
depends_on: ["10-02"]
requirements: [TRADE-03, TRADE-04, TRADE-05]
must_haves:
  truths:
    - "Public binder resolves username to offerings without leaking inventory counts."
    - "Standard catalog filters work correctly on the public binder page."
    - "Logged out visitors have read-only access to the binder."
  artifacts:
    - path: "src/app/binder/[username]/page.tsx"
      provides: "Public binder page with catalog filters"
      min_lines: 150
    - path: "src/db/queries/binder.ts"
      provides: "Optimized queries for public binder data"
      min_lines: 50
  key_links:
    - from: "/binder/manage"
      to: "/binder/[username]"
      via: "Human-readable slug"
---

# Phase 10-03: Trade Binder - Public View

This plan implements the public-facing binder page with filtering and read-only access.

## Tasks

<task id="10.6">
<files>
- src/app/binder/[username]/page.tsx
- src/db/queries/binder.ts
- tests/binder-queries.test.ts
</files>
<action>
Implement the public binder route and data fetching logic.
1. Resolve `username` slug to `userId`.
2. Fetch trade offerings where `tradeQuantity > 0`.
3. Fetch composite "Looking For" list using `binder-logic.ts`.
Ensure the query specifically excludes total owned counts (inventory leak prevention).
Add integration tests for the query logic.
</action>
<verify>
<automated>
npm test tests/binder-queries.test.ts
</automated>
</verify>
<done>Public route correctly resolves and fetches filtered data.</done>
</task>

<task id="10.7">
<files>
- src/components/catalog/card-item.tsx
- src/components/catalog/card-item.test.tsx
</files>
<action>
Refactor `CardItem` to support `mode="binder"` and `mode="want"`.
- In `binder` mode: Show "X Available" badge.
- In `want` mode: Show "X Needed" badge.
- Disable collection controls (increment/decrement) when not in owner management mode.
Add a component test to verify badge display in different modes.
</action>
<verify>
<automated>
npm test src/components/catalog/card-item.test.tsx
</automated>
</verify>
<done>Components are reusable across catalog and binder.</done>
</task>

<task id="10.8">
<files>
- src/app/binder/[username]/page.tsx
- tests/binder-public-render.test.ts
</files>
<action>
Integrate `CatalogClient` into the public binder page to provide standard filtering (Sets, Aspects, Rarity).
Ensure filters apply to both "Available" and "Looking For" sections.
Add a smoke test to ensure the public page renders correctly.
</action>
<verify>
<automated>
npm test tests/binder-public-render.test.ts
</automated>
</verify>
<done>Public binder is as powerful as the catalog for browsing.</done>
</task>
