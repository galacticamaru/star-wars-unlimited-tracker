---
depends_on: ["10-01"]
requirements: [TRADE-01, TRADE-02, TRADE-03]
must_haves:
  truths:
    - "Management actions are protected by session-based authentication."
    - "Users can update their trade offerings and manual wants via API."
  artifacts:
    - path: "src/app/api/trade/route.ts"
      provides: "Endpoints for trade quantity updates"
      min_lines: 30
    - path: "src/app/api/binder/wants/route.ts"
      provides: "Endpoints for manual wants"
      min_lines: 30
    - path: "src/app/api/binder/exclusions/route.ts"
      provides: "Endpoints for exclusions"
      min_lines: 30
    - path: "src/app/binder/manage/page.tsx"
      provides: "Protected management UI for trade binder"
      min_lines: 100
  key_links: []
---

# Phase 10-02: Trade Binder - Management API & UI

This plan implements the protected endpoints and management interface for users to curate their binder.

## Tasks

<task id="10.4">
<files>
- src/app/api/trade/route.ts
- src/app/api/binder/wants/route.ts
- src/app/api/binder/exclusions/route.ts
- tests/trade-api.test.ts
</files>
<action>
Create protected API endpoints for managing trade data.
- `PATCH /api/trade`: Update `tradeQuantity` for a card.
- `POST /api/binder/wants`: Add/update manual want.
- `POST /api/binder/exclusions`: Add/remove card from exclusion list.
Write integration tests to ensure session protection and correct DB updates.
</action>
<verify>
<automated>
npm test tests/trade-api.test.ts
</automated>
</verify>
<done>API layer is secure and functional.</done>
</task>

<task id="10.5">
<files>
- src/app/binder/manage/page.tsx
- src/components/binder/manage-trade-card.tsx
- src/components/binder/manage-wants-list.tsx
- tests/binder-manage-render.test.ts
</files>
<action>
Build the management interface for the trade binder.
1. Username setting/update section with validation.
2. Card search to add to trade offerings or manual wants.
3. Grid/List of current trade offerings with quantity controls.
4. List of exclusions and manual wants.
Add a smoke test to ensure the page renders correctly.
</action>
<verify>
<automated>
npm test tests/binder-manage-render.test.ts
</automated>
</verify>
<done>Management UI is intuitive and complete.</done>
</task>
