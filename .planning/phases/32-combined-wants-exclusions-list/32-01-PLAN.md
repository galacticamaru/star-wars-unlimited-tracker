---
phase: 32-combined-wants-exclusions-list
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/binder/manage-wants-list.tsx
  - src/app/binder/manage/page.tsx
autonomous: true
requirements: [BINDER-18, BINDER-19, BINDER-20]

must_haves:
  truths:
    - "ManageWantsList renders exactly two labeled sections in one list: Deck Wants (top) and Manual Wants (below); the former standalone Exclusions section no longer renders (D-01)"
    - "Section headers carry the auto-vs-manual distinction; each section shows its own count pill; no per-row Auto/Manual badge (D-02)"
    - "Excluding a Deck Want (Ban) keeps its row inline, dimmed (~opacity 50%), with an Excluded tag and a restore control, sorted below the active Deck Wants; restoring it returns the row to active — a full round trip (BINDER-19, D-03)"
    - "Manual Want rows keep the − n + quantity stepper and the [x] remove control wired to the existing handlers; a variant badge shows only for non-Normal printings; no owned/not-owned indicator (BINDER-20, D-04)"
    - "The manage/page.tsx invocation of ManageWantsList resolves against the updated props contract and the app typechecks and builds"
  artifacts:
    - "src/components/binder/manage-wants-list.tsx (restructured two-section list)"
    - "src/app/binder/manage/page.tsx (invocation aligned to updated props contract)"
  key_links:
    - "onToggleExclusion(cardDefinitionId, true|false) drives exclude and restore of Deck Wants"
    - "onUpdateWantQuantity(cardPrintingId, qty) and onRemoveWant (via onUpdateWantQuantity(id, 0)) drive manual want stepper and remove"
    - "autoWants[].isExcluded is the single source of excluded state after the standalone Exclusions section is removed"
---

<objective>
Redesign `ManageWantsList` from today's three disconnected sections (Manual Wants / standalone Exclusions / Automatic Wants) into **one "Looking For" list with two labeled sections** — **Deck Wants** (deck-driven auto-wants) on top, **Manual Wants** below — per locked decisions D-01..D-04. All controls are preserved; this is a pure client-side presentation rearrangement with no server/API/schema/data-flow change.

Purpose: Satisfy BINDER-18 (combined, visually distinguished list), BINDER-19 (preserved exclude/restore of auto-wants), and BINDER-20 (manual want quantity/remove from the list).
Output: A restructured `manage-wants-list.tsx` and an aligned invocation in `manage/page.tsx`.
</objective>

<execution_context>
@/Users/alan/Documents/Repos/Personal/star-wars-unlimited-tracker/.claude/gsd-core/workflows/execute-plan.md
@/Users/alan/Documents/Repos/Personal/star-wars-unlimited-tracker/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-combined-wants-exclusions-list/32-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@src/components/binder/manage-wants-list.tsx
@src/app/binder/manage/page.tsx
</context>

<artifacts_produced>
## Artifacts this phase produces

This is an in-place presentation refactor. No new exported components, functions, or public symbols are introduced.

Changed symbols:
- `ManageWantsListProps` (in `src/components/binder/manage-wants-list.tsx`) — the `exclusions` prop and the `onRemoveExclusion` handler prop are removed; excluded state is derived from `autoWants[].isExcluded` (D-03/D-04 discretion note in CONTEXT permits this simplification). Remaining contract: `wants`, `autoWants`, `onUpdateWantQuantity`, `onRemoveWant`, `onToggleExclusion`.
- `ManageWantsList` markup — three `<section>` blocks collapse to two (`Deck Wants`, `Manual Wants`); the standalone Exclusions `<section>` is deleted.

No new sub-row components or helper modules are added; the existing inline row markup and Tailwind/`lucide-react` patterns are reused.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Restructure ManageWantsList into two labeled sections (Deck Wants + Manual Wants)</name>
  <files>src/components/binder/manage-wants-list.tsx</files>
  <read_first>
    - src/components/binder/manage-wants-list.tsx (the component being redesigned — read fully; note the existing manual-want stepper/remove markup, the excluded-row dimmed markup at opacity-50, and the active auto-want row with the Ban control)
    - .planning/phases/32-combined-wants-exclusions-list/32-CONTEXT.md (locked decisions D-01..D-04 and the layout mockups)
    - .planning/codebase/CONVENTIONS.md (component/naming/styling conventions)
    - No Next.js framework API is touched by this task (pure `'use client'` component, no `next/*` imports, no routing/server APIs); AGENTS.md's node_modules/next/dist/docs read is therefore not triggered here.
  </read_first>
  <action>
    Rewrite the body of `ManageWantsList` so it renders **one list with two labeled sections** in this top-to-bottom order (D-01):

    1. **Deck Wants** section (deck-driven auto-wants, sourced from `autoWants`):
       - Header `<h3>` reads "Deck Wants" with a count pill styled like today's pills. Count semantics (Claude's discretion per CONTEXT): the pill reflects the number of **active (non-excluded)** auto-wants; excluded rows carry their own "Excluded" tag and are not counted in the active pill.
       - Sort rows so **active auto-wants render first and excluded auto-wants sink to the bottom** (D-03). Derive the ordering from `isExcluded` (e.g. a stable sort keying excluded rows last); do not mutate the incoming `autoWants` array in place — sort a shallow copy.
       - Active rows: reuse today's active auto-want row markup — name, optional subtitle, the quantity pill, and the `Ban` exclude control wired to `onToggleExclusion(cardDefinitionId, true)`.
       - Excluded rows: reuse today's dimmed markup — container at ~50% opacity, an "Excluded" tag, and a restore control wired to `onToggleExclusion(cardDefinitionId, false)`. Keep the row **inline within the Deck Wants section** (do not move it to a separate section).
       - Empty state: keep a dashed muted empty-state message when there are no auto-wants.

    2. **Manual Wants** section (per-printing, sourced from `wants`):
       - Header `<h3>` reads "Manual Wants" with a count pill of `wants.length`.
       - Rows: reuse today's manual-want markup — name, the variant badge **only when `variantType !== 'Normal'`** (D-04), optional subtitle, the `Minus`/quantity/`Plus` stepper wired to `onUpdateWantQuantity(cardPrintingId, ...)` (decrement floors at 0 via `Math.max(0, quantity - 1)`), and the `X` remove control wired to `onRemoveWant(cardPrintingId)`. **No owned/not-owned indicator** (D-04).
       - Empty state: keep a dashed muted empty-state message when there are no manual wants.

    Delete the standalone **Exclusions** `<section>` entirely (D-01). Update `ManageWantsListProps` to drop the now-unused `exclusions` and `onRemoveExclusion` props; keep `wants`, `autoWants`, `onUpdateWantQuantity`, `onRemoveWant`, `onToggleExclusion`. Keep the `'use client'` directive and the existing `lucide-react` icon imports actually used (`X`, `Plus`, `Minus`, `Ban`); remove any icon import left unused after the edit. Do not add per-row Auto/Manual badges (D-02) — the two section headers are the distinction.
  </action>
  <acceptance_criteria>
    - `grep -c '<section' src/components/binder/manage-wants-list.tsx` returns `2` (exactly two sections remain).
    - The file contains an `<h3>` whose text includes `Deck Wants` and an `<h3>` whose text includes `Manual Wants`; it no longer contains an `<h3>` header for a standalone `Automatic Wants` or standalone `Exclusions` section.
    - `grep -c 'onRemoveExclusion' src/components/binder/manage-wants-list.tsx` (excluding comment lines) returns `0` — the prop is fully removed from the interface and body.
    - The file still references `onToggleExclusion(` for both `true` (exclude via `Ban`) and `false` (restore) call sites, `onUpdateWantQuantity(`, and `onRemoveWant(`.
    - Excluded auto-want rows still use an opacity-reduced container (dimmed) and render an "Excluded" tag plus a restore control.
    - The variant badge is still guarded by a `variantType !== 'Normal'` condition.
    - `npx tsc --noEmit` reports no errors originating from `manage-wants-list.tsx`.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/alan/Documents/Repos/Personal/star-wars-unlimited-tracker && test "$(grep -c '<section' src/components/binder/manage-wants-list.tsx)" = "2" && grep -q 'Deck Wants' src/components/binder/manage-wants-list.tsx && grep -q 'Manual Wants' src/components/binder/manage-wants-list.tsx && [ "$(grep -v '^\s*//' src/components/binder/manage-wants-list.tsx | grep -c 'onRemoveExclusion')" = "0" ]</automated>
  </verify>
  <done>The component renders two sections (Deck Wants over Manual Wants), excluded auto-wants are dimmed and sorted to the bottom of Deck Wants with a restore control, manual rows keep stepper + remove + conditional variant badge, and the standalone Exclusions section and its props are gone.</done>
</task>

<task type="auto">
  <name>Task 2: Align the manage/page.tsx invocation to the updated props contract</name>
  <files>src/app/binder/manage/page.tsx</files>
  <read_first>
    - src/app/binder/manage/page.tsx (read the `ManageWantsList` invocation around line 483 and the surrounding optimistic-update handlers `updateWantQuantity` / `toggleExclusion` around lines 299–330 — these stay unchanged)
    - src/components/binder/manage-wants-list.tsx (the updated props contract from Task 1)
    - No Next.js framework API is added or changed by this task (only JSX props on an existing client component are removed); AGENTS.md's node_modules/next/dist/docs read is not triggered here.
  </read_first>
  <action>
    In the `<ManageWantsList ... />` invocation at `src/app/binder/manage/page.tsx` (around line 483), remove the two props that no longer exist on the component: `exclusions={...}` and `onRemoveExclusion={...}`. Keep `wants`, `autoWants`, `onUpdateWantQuantity`, `onRemoveWant`, and `onToggleExclusion` exactly as they are wired today. **Do not** modify the page's local `exclusions` state, the `toggleExclusion` optimistic handler, or any other page logic — CONTEXT states those handlers are already correct and must be preserved; only the two dropped props are removed from the JSX. Confirm the whole app still typechecks and builds after the change.
  </action>
  <acceptance_criteria>
    - The `ManageWantsList` invocation in `src/app/binder/manage/page.tsx` no longer passes `exclusions` or `onRemoveExclusion` props.
    - The page still passes `wants`, `autoWants`, `onUpdateWantQuantity`, `onRemoveWant`, and `onToggleExclusion`.
    - The page's `toggleExclusion` handler and its local `exclusions` state are unchanged (still present around lines 299–330).
    - `npx tsc --noEmit` reports no errors, and `npm run lint` reports no new errors for the two changed files.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/alan/Documents/Repos/Personal/star-wars-unlimited-tracker && npx tsc --noEmit && npm run lint 2>&1 | tail -20</automated>
  </verify>
  <done>The parent invocation matches the updated props contract, the page's optimistic handlers are untouched, and the project typechecks and lints cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → existing `/api/binder/wants`, `/api/binder/exclusions` | Untrusted client input crosses here. **Unchanged by this phase** — these endpoints, their auth, and their validation are reused exactly as-is; this phase adds no new request shape, field, or call site. |

## STRIDE Threat Register

This phase is a pure client-side presentation rearrangement of one React component. It introduces no new endpoints, request payloads, data flows, dependencies, or trust boundaries. The register reflects that reality honestly rather than inventing threats.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-01 | Tampering | Reused `/api/binder/wants` + `/api/binder/exclusions` (called via unchanged handlers) | low | accept | No change to endpoints, payloads, or call sites this phase; existing server-side auth/validation is out of scope and remains in force. No new attack surface. |
| T-32-02 | Information Disclosure | `ManageWantsList` render output | low | accept | Component renders the same want/exclusion data the page already fetched and displayed; no new data is exposed by regrouping existing rows. |
| T-32-SC | Tampering | npm/dependency supply chain | low | accept | No packages are installed or upgraded in this phase (reuses existing `lucide-react` + Tailwind). No Package Legitimacy Gate required. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes for the whole project.
- `npm run lint` reports no new errors for `manage-wants-list.tsx` or `manage/page.tsx`.
- Source assertions in Task 1 acceptance criteria hold (two sections, Deck Wants + Manual Wants headers, no standalone Exclusions section, removed props).
- `npm run build` (`next build`) completes successfully.
</verification>

<success_criteria>
- Deck-driven auto-wants and manual wants appear together in one list with two clearly labeled sections (BINDER-18).
- An auto-want can be excluded (dimmed, sunk to bottom, Excluded tag) and later restored — round trip preserved (BINDER-19).
- A manual want's quantity can be changed with the stepper or the row removed with [x], from the list, without leaving the page (BINDER-20).
- Project typechecks, lints, and builds.
</success_criteria>

<output>
Create `.planning/phases/32-combined-wants-exclusions-list/32-01-SUMMARY.md` when done.
</output>
