---
phase: 28-tech-debt-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/catalog/collection-controls.tsx
  - src/components/catalog/variant-collection-section.tsx
  - src/components/catalog/variant-filter.tsx
  - src/lib/catalog/select-best-variant.ts
  - src/db/schema.ts
autonomous: true
requirements: [DEBT-01, DEBT-03]
must_haves:
  truths:
    - "CollectionControls no longer exists anywhere in the codebase — no component file, no imports, no references (D-01, D-02)"
    - "Prestige Foil is a selectable option in the catalog variant filter (D-03)"
    - "Serialized is ranked above Prestige Foil in variant art precedence (D-04)"
    - "The schema variantType comment lists all 8 variant types (D-05)"
  artifacts:
    - path: "src/components/catalog/variant-filter.tsx"
      provides: "VARIANT_OPTIONS array including 'Prestige Foil'"
      contains: "'Prestige Foil'"
    - path: "src/lib/catalog/select-best-variant.ts"
      provides: "VARIANT_PRECEDENCE record including Serialized: 8"
      contains: "Serialized"
    - path: "src/db/schema.ts"
      provides: "variantType column comment listing all 8 types"
      contains: "Prestige Foil"
  key_links:
    - from: "src/lib/catalog/select-best-variant.ts"
      to: "selectBestVariantArtUrl"
      via: "VARIANT_PRECEDENCE lookup"
      pattern: "VARIANT_PRECEDENCE\\[artData.variantType\\]"
---

<objective>
Close DEBT-01 (dead code removal) and DEBT-03 (variant enum gaps). Delete the orphaned `CollectionControls` component and the two stale comments that reference it, then fill the two variant-enum gaps that cause silent data errors: add `'Prestige Foil'` to the catalog variant filter options and `Serialized: 8` to the variant art precedence record. Also update the stale `variantType` schema comment to list all 8 variant types.

Purpose: Remove a confusing dead file that points at a deleted API endpoint, and stop two known silent failures where `Prestige Foil` cannot be filtered and `Serialized` cards are ranked as lowest precedence (treated as unknown via the `?? 0` fallback).

Output: One deleted file, two cleaned comments, two enum additions, one corrected schema comment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-tech-debt-sweep/28-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete CollectionControls and clean its stale comment references (DEBT-01)</name>
  <files>src/components/catalog/collection-controls.tsx, src/components/catalog/variant-collection-section.tsx</files>
  <read_first>
    - src/components/catalog/collection-controls.tsx (file being deleted — confirm it is the orphaned component, not the active variant-collection-section.tsx)
    - src/components/catalog/variant-collection-section.tsx (active component with two stale comments at lines 42 and 67)
    - src/app/api/collection/route.ts (line 26 confirms POST /api/collection was removed — the endpoint CollectionControls called; this is the D-01 rationale)
  </read_first>
  <action>
    Delete the file `src/components/catalog/collection-controls.tsx` outright (per D-01 and the established dead-code pattern: delete the file, do not leave an empty shell or re-export). The component has zero active imports — verified via grep, only two comment references remain. Its POST `/api/collection` call target was removed (see route.ts line 26 "POST /api/collection has been REMOVED").

    Then edit `src/components/catalog/variant-collection-section.tsx` to remove the now-dangling references to `CollectionControls` in two comments (D-02):
    - Line 42 comment currently reads "Optimistic UI update (fire-and-update pattern from CollectionControls)". Rephrase so it no longer names the deleted component — e.g. describe it as the optimistic fire-and-update pattern without the "from CollectionControls" attribution.
    - Line 67 comment currently reads "Container — exact classes from UI-SPEC.md §Layout and existing CollectionControls wrapper". Rephrase to drop the "and existing CollectionControls wrapper" clause, keeping the UI-SPEC.md §Layout reference.

    Do NOT change any logic in variant-collection-section.tsx — comment text only. Exact rewording is at your discretion per CONTEXT.md, as long as the string "CollectionControls" no longer appears anywhere under src/.
  </action>
  <verify>
    <automated>Test -Path "src/components/catalog/collection-controls.tsx" returns False; grep for "CollectionControls" under src/ returns zero matches</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/catalog/collection-controls.tsx` does not exist
    - Searching `src/` for the literal string `CollectionControls` returns zero matches (component name removed from both comments)
    - `src/components/catalog/variant-collection-section.tsx` still exports `function VariantCollectionSection` and its logic (updateVariant, optimistic setCounts, rollback) is unchanged
    - No new import errors: any consumer of variant-collection-section.tsx still resolves
  </acceptance_criteria>
  <done>collection-controls.tsx deleted, no "CollectionControls" string anywhere in src/, variant-collection-section.tsx logic intact</done>
</task>

<task type="auto">
  <name>Task 2: Fill variant enum gaps in filter options, precedence, and schema comment (DEBT-03)</name>
  <files>src/components/catalog/variant-filter.tsx, src/lib/catalog/select-best-variant.ts, src/db/schema.ts</files>
  <read_first>
    - src/components/catalog/variant-filter.tsx (line 11 VARIANT_OPTIONS array — currently missing 'Prestige Foil')
    - src/lib/catalog/select-best-variant.ts (VARIANT_PRECEDENCE record + JSDoc header comment — currently missing Serialized; note the `?? 0` unknown-variant fallback that silently demotes Serialized today)
    - src/db/schema.ts (line 104 variantType column comment — currently lists only 5 types)
  </read_first>
  <action>
    Three concrete edits, one per file:

    1. `src/components/catalog/variant-filter.tsx`: insert `'Prestige Foil'` into the `VARIANT_OPTIONS` array immediately after `'Prestige'` (D-03). Final array exactly: `['Normal', 'Foil', 'Hyperspace', 'Hyperspace Foil', 'Showcase', 'Prestige', 'Prestige Foil', 'Serialized']`. This groups the two Prestige variants together and makes Prestige Foil filterable in the sidebar.

    2. `src/lib/catalog/select-best-variant.ts`: add the key `Serialized: 8` to the `VARIANT_PRECEDENCE` record (D-04). Place it as the new highest-precedence entry (above the existing `'Prestige Foil': 7`). Do NOT renumber the other entries — Prestige Foil stays 7, Prestige 6, Showcase 5, Hyperspace Foil 4, Hyperspace 3, Foil 2, Normal 1. Also update the JSDoc precedence line in the file's header comment (currently "Precedence: Prestige Foil(7) > Prestige(6) > ...") to read: `Serialized(8) > Prestige Foil(7) > Prestige(6) > Showcase(5) > Hyperspace Foil(4) > Hyperspace(3) > Foil(2) > Normal(1)`. Rationale: Serialized cards are ultra-rare numbered prints (e.g. 1/25), the most premium category; without this key they hit the `?? 0` fallback and are silently ranked lowest.

    3. `src/db/schema.ts` line 104: replace the `variantType` inline comment so it lists all 8 types: `// "Normal" | "Foil" | "Hyperspace" | "Hyperspace Foil" | "Showcase" | "Prestige" | "Prestige Foil" | "Serialized"` (D-05). Comment only — do NOT change the column definition itself.
  </action>
  <verify>
    <automated>npm test passes (existing suite stays green); grep confirms 'Prestige Foil' in variant-filter.tsx VARIANT_OPTIONS, "Serialized: 8" in select-best-variant.ts, and "Prestige Foil" in schema.ts variantType comment</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/catalog/variant-filter.tsx` VARIANT_OPTIONS equals `['Normal', 'Foil', 'Hyperspace', 'Hyperspace Foil', 'Showcase', 'Prestige', 'Prestige Foil', 'Serialized']` (8 entries, 'Prestige Foil' between 'Prestige' and 'Serialized')
    - `src/lib/catalog/select-best-variant.ts` VARIANT_PRECEDENCE contains `Serialized: 8` and still contains `'Prestige Foil': 7`, `Prestige: 6`, `Showcase: 5`, `'Hyperspace Foil': 4`, `Hyperspace: 3`, `Foil: 2`, `Normal: 1`
    - The JSDoc header in select-best-variant.ts contains the substring `Serialized(8) > Prestige Foil(7)`
    - `src/db/schema.ts` line ~104 variantType comment contains all 8 type names including `Prestige Foil` and `Serialized`; the `text('variant_type').notNull()` column definition is unchanged
    - `npm test` exits 0 (no existing tests broken)
  </acceptance_criteria>
  <done>Prestige Foil filterable, Serialized ranked 8 (highest), schema comment lists 8 types, full test suite green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan deletes a file and edits client-side constant arrays/records and a DB schema comment. No new trust boundary is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Tampering | schema.ts variantType comment edit | accept | Comment-only change with no runtime effect; the column definition is unchanged. No security impact. |
| T-28-02 | Information Disclosure | VARIANT_PRECEDENCE / VARIANT_OPTIONS edits | accept | Both are static client-side display/ranking constants over already-public card variant names. No PII, no auth surface. |
| T-28-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (verified: no new dependencies per phase scope). No supply-chain surface. |
</threat_model>

<verification>
- `Test-Path "src/components/catalog/collection-controls.tsx"` is False
- Grep for `CollectionControls` under `src/` returns zero matches
- Grep confirms `'Prestige Foil'` in variant-filter.tsx, `Serialized: 8` in select-best-variant.ts, all 8 types in schema.ts comment
- `npm test` exits 0
- `npm run build` succeeds (no broken imports from the deletion)
</verification>

<success_criteria>
- DEBT-01 closed: CollectionControls component file and all references removed
- DEBT-03 closed: Prestige Foil filterable, Serialized ranked correctly, schema comment accurate
- No existing test broken; build succeeds
</success_criteria>

<output>
Create `.planning/phases/28-tech-debt-sweep/28-01-SUMMARY.md` when done
</output>
