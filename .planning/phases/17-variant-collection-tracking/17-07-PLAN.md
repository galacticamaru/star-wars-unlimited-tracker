---
phase: 17-variant-collection-tracking
plan: "07"
type: execute
wave: 5
depends_on:
  - "17-02"
  - "17-03"
  - "17-04"
  - "17-05"
  - "17-06"
files_modified: []
autonomous: true
requirements:
  - REQ-COLLECT-06
  - REQ-COLLECT-07

must_haves:
  truths:
    - "user_printing_collections table exists in the live Neon database"
    - "drizzle-kit push exits 0 with no pending schema changes after push"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "Schema is the source of truth — already updated in Plan 02"
      contains: "user_printing_collections"
  key_links:
    - from: "src/db/schema.ts"
      to: "Neon live database"
      via: "npx drizzle-kit push"
      pattern: "user_printing_collections"
---

<objective>
[BLOCKING] Push the Drizzle schema to the live Neon database. This creates the `user_printing_collections` table in the production database.

Purpose: Without this step, all variant count API calls will fail at runtime with "relation does not exist" errors. The phase CANNOT pass verification without this push. TypeScript and unit tests pass regardless (schema types come from code, not the live DB), creating a false-positive verification state if this step is skipped.

Output: `user_printing_collections` table created in the live Neon database.
</objective>

<execution_context>
@C:\Users\alan_\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\alan_\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-variant-collection-tracking/17-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Push Drizzle schema to live database</name>
  <files><!-- DDL-only operation — no source files modified; src/db/schema.ts is the source of truth being pushed --></files>

  <read_first>
    - src/db/schema.ts — verify userPrintingCollections definition is present before pushing
  </read_first>

  <action>
Run the Drizzle schema push command to apply the new `user_printing_collections` table to the live Neon database:

```bash
npx drizzle-kit push
```

This command reads `drizzle.config.ts` (or `drizzle.config.js`) and applies schema changes to the connected database.

If the command prompts for confirmation (e.g., "Are you sure you want to push these changes?"), confirm with `yes` or the appropriate affirmation.

If the push fails with an auth/connection error, verify that the `DATABASE_URL` environment variable is set in `.env.local` or `.env`. Do not proceed without a successful push.

After a successful push, run the push command a SECOND time to confirm there are no remaining pending changes:
```bash
npx drizzle-kit push
```
The second run should report no pending changes (idempotent).
  </action>

  <verify>
    <automated>npx drizzle-kit push 2>&1 | tail -5</automated>
    Second run should output "No changes detected" or similar confirmation that the schema is up to date.
  </verify>

  <acceptance_criteria>
    - First `npx drizzle-kit push` exits 0
    - Second `npx drizzle-kit push` reports no pending changes (confirming idempotency)
    - `npx vitest run` still exits 0 after push (no regressions)
  </acceptance_criteria>

  <done>user_printing_collections table exists in the live Neon database. Schema push is idempotent on second run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| drizzle-kit → Neon production DB | Schema DDL (CREATE TABLE) applied directly to production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-07-01 | Tampering | Schema push to production | accept | DDL is additive only (new table, no modifications to existing tables). No data at risk. Drizzle push is the established migration workflow for this project. |
</threat_model>

<verification>
- `npx drizzle-kit push` exits 0 twice
- `npx vitest run` exits 0 after push
</verification>

<success_criteria>
- `user_printing_collections` table created in live Neon database
- Schema push idempotent (second run: no changes)
- Test suite still green
</success_criteria>

<output>
After completion, create `.planning/phases/17-variant-collection-tracking/17-07-SUMMARY.md`
</output>
