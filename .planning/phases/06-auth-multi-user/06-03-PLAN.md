---
phase: 06-auth-multi-user
plan: 03
type: execute
wave: 3
depends_on: [06-01, 06-02]
files_modified: [src/app/api/collection/import/route.ts, src/app/api/collection/route.ts, src/app/api/collection/sets/route.ts, src/app/api/decks/[id]/export/route.ts, src/app/api/decks/[id]/route.ts, src/app/api/decks/route.ts, src/app/api/want-list/route.ts]
autonomous: true
requirements: [AUTH-02, AUTH-04]
must_haves:
  truths:
    - "API routes return 401 if user is not authenticated"
    - "API routes use the authenticated userId for all database operations"
  artifacts:
    - path: "src/app/api/collection/route.ts"
      provides: "Protected collection API endpoint"
  key_links:
    - from: "src/app/api/collection/route.ts"
      to: "src/lib/auth.ts"
      via: "auth.api.getSession"
---

<objective>
Update all protected API routes to enforce session checks and pass the authenticated userId to database queries.

Purpose: Secure the API layer and ensure users only access their own data.
Output: Secure API routes returning 401 for unauthenticated requests and correctly filtering by session user.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-auth-multi-user/06-01-SUMMARY.md
@.planning/phases/06-auth-multi-user/06-02-SUMMARY.md
@src/lib/auth.ts
@src/db/queries/collection.ts
@src/db/queries/decks.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update API routes with session checks</name>
  <files>src/app/api/collection/route.ts, src/app/api/collection/import/route.ts, src/app/api/collection/sets/route.ts, src/app/api/decks/route.ts, src/app/api/decks/[id]/route.ts, src/app/api/decks/[id]/export/route.ts, src/app/api/want-list/route.ts</files>
  <action>
    1. Import `auth` from `src/lib/auth.ts` and `headers` from `next/headers`.
    2. At the top of each handler, add:
       `const session = await auth.api.getSession({ headers: await headers() });`
    3. If `!session`, return `new Response("Unauthorized", { status: 401 })`.
    4. Pass `Number(session.user.id)` as the `userId` argument to all query functions.
    5. Ensure `src/app/api/cron/sync-cards/route.ts` is NOT changed (it uses CRON_SECRET).
  </action>
  <verify>
    <automated>npm run build --no-lint</automated>
  </verify>
  <done>All protected API routes now require a session and use the session userId.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API Route → DB Query | userId parameter must be trusted (comes from session) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Elevation of Privilege | API Routes | mitigate | Session validation before every database operation; reject requests without valid session. |
</threat_model>

<verification>
1. Verify that unauthorized `curl` requests to `/api/collection` return 401.
2. Confirm the project builds without lint errors.
</verification>

<success_criteria>
All protected API routes are secured with session validation.
</success_criteria>

<output>
After completion, create `.planning/phases/06-auth-multi-user/06-03-SUMMARY.md`
</output>
