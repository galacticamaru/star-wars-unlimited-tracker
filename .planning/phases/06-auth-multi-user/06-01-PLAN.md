---
phase: 06-auth-multi-user
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, src/db/schema.ts, src/lib/auth.ts, src/lib/auth-client.ts, src/app/api/auth/[...all]/route.ts, src/app/(auth)/login/page.tsx, tests/auth-protection.test.ts, tests/data-isolation.test.ts, tests/migration-hook.test.ts]
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-05, AUTH-06]
user_setup:
  - service: google-console
    why: "Google OAuth"
    env_vars:
      - name: GOOGLE_CLIENT_ID
      - name: GOOGLE_CLIENT_SECRET
  - service: discord-portal
    why: "Discord OAuth"
    env_vars:
      - name: DISCORD_CLIENT_ID
      - name: DISCORD_CLIENT_SECRET
  - service: better-auth
    why: "Auth Secret"
    env_vars:
      - name: BETTER_AUTH_SECRET
        source: "Generate with `npx better-auth secret`"

must_haves:
  truths:
    - "User can visit /login and see Sign In / Sign Up tabs"
    - "Better Auth tables exist in the database with serial IDs"
    - "Auth API routes are responsive"
    - "Test scaffolding exists for auth protection, data isolation, and migration"
  artifacts:
    - path: "src/lib/auth.ts"
      provides: "Better Auth server instance"
    - path: "src/app/api/auth/[...all]/route.ts"
      provides: "Auth API handler"
    - path: "tests/auth-protection.test.ts"
      provides: "Test suite for route protection"
    - path: "tests/data-isolation.test.ts"
      provides: "Test suite for multi-user data isolation"
    - path: "tests/migration-hook.test.ts"
      provides: "Test suite for first-user migration"
  key_links:
    - from: "src/lib/auth.ts"
      to: "src/db/schema.ts"
      via: "Drizzle adapter"
---

<objective>
Setup the core authentication infrastructure using Better Auth and create the test scaffolding required for verifying auth protection, data isolation, and migration logic.

Purpose: Provide the foundation for multi-user support and establish a test-driven environment for the phase.
Output: Working authentication backend, login UI, and empty test suites for subsequent validation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-auth-multi-user/06-CONTEXT.md
@.planning/phases/06-auth-multi-user/06-RESEARCH.md
@src/db/schema.ts
@src/db/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and update schema</name>
  <files>package.json, src/db/schema.ts</files>
  <action>
    1. Install `better-auth`.
    2. Add Better Auth tables to `src/db/schema.ts`: `user`, `session`, `account`, `verification`.
    3. Use `integer('id').primaryKey()` for these tables to support `serial` IDs as per RESEARCH.md (Pattern 1).
    4. Ensure `userCollections` and `decks` still use `integer('user_id')`.
    5. Run `npx drizzle-kit generate` and `npx drizzle-kit push` (or migrate).
  </action>
  <verify>
    <automated>npx drizzle-kit check</automated>
  </verify>
  <done>Better Auth tables exist in schema.ts and database.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Better Auth server and client</name>
  <files>src/lib/auth.ts, src/lib/auth-client.ts, src/app/api/auth/[...all]/route.ts</files>
  <action>
    1. Create `src/lib/auth.ts` configuring `betterAuth` with `drizzleAdapter`.
    2. Set `advanced.database.generateId: "serial"` in `auth.ts`.
    3. Add Google and Discord providers (per D-07).
    4. Create `src/lib/auth-client.ts` using `createAuthClient`.
    5. Create `src/app/api/auth/[...all]/route.ts` exporting `GET` and `POST` from `auth.handler`.
  </action>
  <verify>
    <automated>npm run build --no-lint</automated>
  </verify>
  <done>Auth configuration files created and API route exported.</done>
</task>

<task type="auto">
  <name>Task 3: Create Test Scaffolding</name>
  <files>tests/auth-protection.test.ts, tests/data-isolation.test.ts, tests/migration-hook.test.ts</files>
  <action>
    Create empty test files with descriptive `describe` blocks and `it.todo` placeholders based on the Validation Architecture in RESEARCH.md.
    - `tests/auth-protection.test.ts`: Should cover AUTH-01 (redirects).
    - `tests/data-isolation.test.ts`: Should cover AUTH-04 (userId filters).
    - `tests/migration-hook.test.ts`: Should cover AUTH-07 (first-user migration).
  </action>
  <verify>
    <automated>ls tests/*.test.ts</automated>
  </verify>
  <done>Test scaffolding files exist.</done>
</task>

<task type="auto">
  <name>Task 4: Create Login/Register Page</name>
  <files>src/app/(auth)/login/page.tsx</files>
  <action>
    1. Create a unified `/login` page using `Tabs` (per D-05, D-06, D-07).
    2. Implement Sign In and Sign Up forms with Email and Password fields.
    3. Add Google and Discord login buttons using `authClient.signIn.social`.
    4. Use `src/components/ui/button.tsx` and `src/components/ui/input.tsx`.
    5. Follow shadcn Tab pattern from RESEARCH.md.
  </action>
  <verify>
    <automated>test -f src/app/\(auth\)/login/page.tsx</automated>
  </verify>
  <done>Login page created with functional tabs and social login buttons.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Auth API | User credentials and OAuth tokens cross here |
| Auth API → Database | Session and User data persisted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Spoofing | Auth API | mitigate | Better Auth handles secure password hashing (Argon2) and CSRF protection. |
| T-06-02 | Information Disclosure | Session Cookie | mitigate | Cookies are `httpOnly`, `secure`, and `sameSite: "Lax"` by default in Better Auth. |
| T-06-03 | Tampering | OAuth Callback | mitigate | Better Auth uses state/nonce to prevent authorization code injection. |
</threat_model>

<verification>
1. Check database for `user`, `session`, `account` tables.
2. Manually verify `/login` page renders with two tabs.
3. Confirm test files exist in the `tests/` directory.
</verification>

<success_criteria>
Better Auth is initialized, login page is accessible, and test scaffolding is ready.
</success_criteria>

<output>
After completion, create `.planning/phases/06-auth-multi-user/06-01-SUMMARY.md`
</output>
