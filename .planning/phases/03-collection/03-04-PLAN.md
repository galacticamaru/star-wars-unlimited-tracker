---
phase: 03-collection
plan: 04
type: execute
wave: 4
depends_on: [03-01]
files_modified: [src/app/collection/page.tsx, src/lib/collection/normalize.ts, src/app/api/collection/import/route.ts]
autonomous: true
requirements: [COLLECT-03, COLLECT-04]
---

<objective>
Implement the bulk CSV import tool, specifically supporting the Reddit community spreadsheet format.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: CSV Normalization Logic</name>
  <files>src/lib/collection/normalize.ts</files>
  <action>
    Create normalization function for Reddit CSV:
    Sum columns "Normal", "Foil", "Hyperspace", "Hyperspace Foil" into a single count per "Set-Number" key.
  </action>
  <verify>Unit tests with sample CSV row data</verify>
  <done>Normalization logic handles variant summing correctly</done>
</task>

<task type="auto">
  <name>Task 2: Import UI and Transactional API</name>
  <files>package.json, src/app/collection/page.tsx, src/app/api/collection/import/route.ts</files>
  <action>
    Install `papaparse` (npm install papaparse).
    Use `papaparse` for client-side parsing. Create POST /api/collection/import.
    Use `db.transaction` to ensure all-or-nothing upsert of collection data.
  </action>
  <verify>Upload test CSV, verify entire collection updates or fails cleanly</verify>
  <done>Bulk import functional</done>
</task>
</tasks>
