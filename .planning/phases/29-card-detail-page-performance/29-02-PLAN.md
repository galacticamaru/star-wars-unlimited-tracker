---
phase: 29-card-detail-page-performance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/collection/variants/route.ts
  - src/app/api/trade/route.ts
  - src/components/catalog/variant-collection-section.tsx
  - src/components/catalog/variant-trade-section.tsx
autonomous: true
requirements: [PERF-10]
must_haves:
  truths:
    - "Updating a variant owned count revalidates the per-user printings cache server-side and refreshes the router client-side"
    - "Updating a trade quantity revalidates the per-user printings cache server-side and refreshes the router client-side"
    - "Both mutation routes derive cardDefinitionId before calling revalidateTag"
  artifacts:
    - path: "src/app/api/collection/variants/route.ts"
      provides: "revalidateTag on card-printings tag after variant upsert"
      contains: "revalidateTag(`card-printings-"
    - path: "src/app/api/trade/route.ts"
      provides: "cardDefinitionId lookup + revalidateTag after trade upsert"
      contains: "revalidateTag(`card-printings-"
    - path: "src/components/catalog/variant-collection-section.tsx"
      provides: "router.refresh() on successful variant mutation"
      contains: "router.refresh()"
    - path: "src/components/catalog/variant-trade-section.tsx"
      provides: "router.refresh() on successful trade mutation"
      contains: "router.refresh()"
  key_links:
    - from: "src/app/api/trade/route.ts"
      to: "card-printings cache tag"
      via: "revalidateTag with derived cardDefinitionId"
      pattern: "revalidateTag\\(`card-printings-"
    - from: "src/components/catalog/variant-collection-section.tsx"
      to: "Router Cache"
      via: "router.refresh in fetch success branch"
      pattern: "router\\.refresh\\(\\)"
---

<objective>
Implement two-layer cache invalidation for the per-user printings cache introduced in plan 01 (D-04, D-05): `revalidateTag` in the variant and trade mutation route handlers (busts server Data Cache) plus `router.refresh()` in the variant collection and trade section components (busts client Router Cache). Mirrors the Phase 27 deck-mutation pattern.

Purpose: Without invalidation, the new per-user printings cache would serve stale owned/trade counts after a mutation. Both layers must fire for the next navigation to show fresh data.
Output: revalidateTag added to both API routes; router.refresh() added to both client components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-card-detail-page-performance/29-CONTEXT.md
@.planning/phases/29-card-detail-page-performance/29-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add revalidateTag to the variant and trade mutation route handlers</name>
  <files>src/app/api/collection/variants/route.ts, src/app/api/trade/route.ts</files>
  <read_first>
    - src/app/api/collection/variants/route.ts (POST handler — already looks up `printing.cardDefinitionId` at lines 40-48 and computes `userId` at line 31; revalidateTag goes after recomputeTotal on line 56)
    - src/app/api/trade/route.ts (PATCH handler — does NOT currently query cardPrintings; userId is inlined at line 21; both must be added)
    - src/app/api/decks/route.ts (reference: `import { revalidateTag } from 'next/cache'` line 5, call form `revalidateTag(\`decks-user-${userId}\`, 'max')` line 38 — note the required second argument 'max')
    - node_modules/next/dist/docs/01-app/03-api-reference/04-functions/revalidateTag.md (two-argument signature `revalidateTag(tag, 'max')` is required; single-argument form is deprecated)
  </read_first>
  <action>
    In `src/app/api/collection/variants/route.ts`: add `import { revalidateTag } from 'next/cache';` to the import block. After `await recomputeTotal(userId, printing.cardDefinitionId);` (line 56), add `revalidateTag(\`card-printings-${printing.cardDefinitionId}-user-${userId}\`, 'max');`. The `printing.cardDefinitionId` and `userId` values are already in scope — no new lookup or variable needed.

    In `src/app/api/trade/route.ts`: add four imports — `import { db } from '@/db';`, `import { cardPrintings } from '@/db/schema';`, `import { eq } from 'drizzle-orm';`, and `import { revalidateTag } from 'next/cache';`. Extract `const userId = Number(session.user.id);` to a variable before the `upsertTradeOffering` call (currently inlined at line 21) and pass `userId` to `upsertTradeOffering`. After the `await upsertTradeOffering(...)` call, add a cardDefinitionId lookup copied from the collection route pattern: select `cardDefinitionId` from `cardPrintings` where `eq(cardPrintings.id, cardPrintingId)` limit 1; if no `printing` row, return `new Response('cardPrintingId not found', { status: 404 })`. Then call `revalidateTag(\`card-printings-${printing.cardDefinitionId}-user-${userId}\`, 'max');`. Keep all calls inside the existing try/catch.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | Select-String -Pattern "trade/route|variants/route"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/api/collection/variants/route.ts contains `import { revalidateTag } from 'next/cache'`
    - src/app/api/collection/variants/route.ts contains `revalidateTag(\`card-printings-${printing.cardDefinitionId}-user-${userId}\`, 'max')`
    - src/app/api/trade/route.ts contains `import { revalidateTag } from 'next/cache'`, `import { db } from '@/db'`, `import { cardPrintings } from '@/db/schema'`, and `import { eq } from 'drizzle-orm'`
    - src/app/api/trade/route.ts contains a `const userId = Number(session.user.id)` variable and a select on `cardPrintings` filtered by `eq(cardPrintings.id, cardPrintingId)`
    - src/app/api/trade/route.ts returns status 404 with body `cardPrintingId not found` when the printing lookup is empty
    - src/app/api/trade/route.ts contains `revalidateTag(\`card-printings-${printing.cardDefinitionId}-user-${userId}\`, 'max')`
    - Both revalidateTag calls use the two-argument form with `'max'`
    - `npx tsc --noEmit` reports no errors in either route file
  </acceptance_criteria>
  <done>Both mutation routes invalidate the per-user printings Data Cache with a correctly-derived cardDefinitionId.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add router.refresh() to variant collection and trade section success branches</name>
  <files>src/components/catalog/variant-collection-section.tsx, src/components/catalog/variant-trade-section.tsx</files>
  <read_first>
    - src/components/catalog/variant-collection-section.tsx (useRouter imported line 8, `const router = useRouter()` line 27; the fetch success path is the `else` branch around lines 45-55 — router.refresh() is NOT currently called)
    - src/components/catalog/variant-trade-section.tsx (useRouter imported line 8, `const router = useRouter()` line 28; success path calls `onQuantityChange?.()` around line 56 but does NOT call router.refresh())
    - .planning/phases/29-card-detail-page-performance/29-CONTEXT.md (D-05: two-layer invalidation — router.refresh busts the client Router Cache; place it only in the fetch success branch, not the optimistic path)
  </read_first>
  <action>
    In `variant-collection-section.tsx`: in the `updateVariant` fetch handler, the current code has an `if (!res.ok) { ...rollback... }` block with no `else`. Add an `else { router.refresh(); }` branch so `router.refresh()` runs only on a successful (`res.ok`) response. Do not call it in the optimistic-update path or unconditionally. `useRouter` and `router` are already in scope — add no imports.

    In `variant-trade-section.tsx`: the success branch already exists as `else { onQuantityChange?.(cardPrintingId, val); }`. Add `router.refresh();` immediately after the `onQuantityChange?.(...)` call inside that same `else` branch. `useRouter` and `router` are already in scope — add no imports.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | Select-String -Pattern "variant-collection-section|variant-trade-section" ; npm run build 2>&1 | Select-String -Pattern "Compiled successfully|error" | Select-Object -First 5</automated>
  </verify>
  <acceptance_criteria>
    - variant-collection-section.tsx contains `router.refresh()` inside the `else` (res.ok success) branch of the variant fetch, not in the `!res.ok` rollback path
    - variant-trade-section.tsx contains `router.refresh()` immediately after `onQuantityChange?.(cardPrintingId, val)` in the success branch
    - Neither file adds a new `useRouter` import (both already present)
    - `npm run build` completes with "Compiled successfully" and exits 0
  </acceptance_criteria>
  <done>Both client components refresh the Router Cache on successful mutation, completing the two-layer invalidation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API route | cardPrintingId, count, tradeQuantity from request body are untrusted |
| session → userId | userId must come from session, never from request body |
| revalidateTag key | tag is built from server-derived cardDefinitionId + session userId only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-04 | Spoofing/Elevation | trade & variants route userId | mitigate | userId is derived from `session.user.id` (existing auth guard returns 401 when no session); never read from request body — preserved in this change |
| T-29-05 | Information Disclosure | revalidateTag cache key | mitigate | Tag interpolates server-side `printing.cardDefinitionId` (looked up by id) and session `userId`; client cannot influence which user's cache is busted |
| T-29-06 | Tampering | trade route cardPrintingId lookup | mitigate | cardPrintingId bound via drizzle `eq()` (parameterized); missing printing returns 404 before revalidateTag, preventing a malformed/undefined tag |
| T-29-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan — no dependency surface added |
</threat_model>

<verification>
- `npm run build` exits 0 with "Compiled successfully"
- Grep confirms `revalidateTag(\`card-printings-` appears in both route files
- Grep confirms `router.refresh()` appears in both variant section components
</verification>

<success_criteria>
- Both mutation routes bust the per-user printings Data Cache with `'max'` profile
- Both client components bust the Router Cache on success
- Build is green
</success_criteria>

<output>
Create `.planning/phases/29-card-detail-page-performance/29-02-SUMMARY.md` when done
</output>
