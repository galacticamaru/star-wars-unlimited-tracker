---
phase: 29-card-detail-page-performance
plan: 05
type: execute
wave: 3
depends_on: ["29-04"]
files_modified: []
autonomous: true
requirements: [PERF-10]
must_haves:
  truths:
    - "Each specific regression recorded in 29-04-SPEED-INSIGHTS.md has a corresponding fix applied, OR the plan is a documented no-op because the Wave 2a decision was insufficient-data"
    - "The card detail page still builds and the trade offer, collection controls, and variant tracking sections still function"
  artifacts:
    - path: ".planning/phases/29-card-detail-page-performance/29-05-SUMMARY.md"
      provides: "Record of targeted fixes applied or documented no-op closure"
      contains: "PERF-10"
  key_links:
    - from: ".planning/phases/29-card-detail-page-performance/29-04-SPEED-INSIGHTS.md"
      to: "targeted fixes in this plan"
      via: "each recorded regression maps to a fix"
      pattern: "Findings"
---

<objective>
Apply targeted fixes for each specific FCP/LCP/INP regression the user recorded in `29-04-SPEED-INSIGHTS.md` (D-11 Wave 2b). If the Wave 2a decision was `insufficient-data-close-after-wave-1`, this plan is a documented no-op that closes the phase — NO speculative fixes are applied (D-12). This plan is data-driven; the exact files modified depend on the recorded findings.

Purpose: PERF-10 requires each identified regression to have a corresponding fix. This plan converts the recorded findings into concrete code changes (or formally closes the phase when no data warranted fixes).
Output: Targeted fixes for each recorded regression, OR a no-op SUMMARY documenting the insufficient-data closure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-card-detail-page-performance/29-CONTEXT.md
@.planning/phases/29-card-detail-page-performance/29-PATTERNS.md
@.planning/phases/29-card-detail-page-performance/29-04-SPEED-INSIGHTS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Branch on the Wave 2a decision — apply targeted fixes or document closure</name>
  <files>(determined by 29-04-SPEED-INSIGHTS.md findings — e.g., src/components/catalog/variant-collection-section.tsx, src/components/catalog/variant-trade-section.tsx, src/app/cards/[set-code]/[card-number]/page.tsx)</files>
  <read_first>
    - .planning/phases/29-card-detail-page-performance/29-04-SPEED-INSIGHTS.md (REQUIRED — the recorded findings and the `Decision:` line that determines whether this plan does work or is a no-op)
    - .planning/phases/29-card-detail-page-performance/29-CONTEXT.md (D-12: do NOT apply speculative INP fixes if data is insufficient — proactive fixes are the full deliverable in that case)
    - .planning/phases/29-card-detail-page-performance/29-PATTERNS.md (analog patterns for whichever file a fix targets, e.g. startTransition/useDeferredValue precedent from Phase 27 for INP work)
    - src/app/cards/[set-code]/[card-number]/page.tsx (the page surface, if an LCP/FCP fix is needed)
    - node_modules/next/dist/docs/01-app (read the relevant doc for whatever Next.js API a fix uses — e.g. Image, Suspense, or caching — before writing the fix, per AGENTS.md)
  </read_first>
  <action>
    Read the `Decision:` line in 29-04-SPEED-INSIGHTS.md first.

    IF `Decision: insufficient-data-close-after-wave-1` — apply NO code changes. This is a documented no-op (D-12). Record in the SUMMARY that no Speed Insights regression data was available and the Wave 1 proactive fixes are the full PERF-10 deliverable. Leave files_modified empty. Stop.

    IF `Decision: targeted-fixes-needed` — for EACH regression in the `## Findings` list, apply the minimal corresponding fix using the established analog pattern:
    - INP driven by the variant +/- buttons (variant-collection-section.tsx / variant-trade-section.tsx): wrap the state-updating dispatch in `startTransition` and/or apply `useDeferredValue` to the displayed count, mirroring the Phase 27 deck-builder INP fix (27-02). Do not change the fetch/revalidate logic added in plan 02.
    - INP driven by the leader flip toggle (card-image-section.tsx): wrap the `setShowBack`/`setLoaded` updates in `startTransition` if the toggle is the recorded INP element.
    - LCP element is the card image and still slow: confirm `priority` from plan 03 is present; if the finding indicates an oversized fetch, tighten the `sizes` value to the recorded viewport breakpoint only — do not remove `priority`.
    - FCP slow despite the skeleton: verify loading.tsx from plan 03 is recognized for the route segment; if the finding points at a specific missing above-fold element in the skeleton, add that placeholder to loading.tsx.
    Apply only fixes that map to a recorded finding. Do NOT add fixes for metrics rated Good. Use the matching `node_modules/next/dist/docs/` reference for any Next.js API touched. After applying fixes, populate this plan's `files_modified` frontmatter with the actual files changed.
  </action>
  <verify>
    <automated>npm run build 2>&1 | Select-String -Pattern "Compiled successfully|error" | Select-Object -First 5 ; npm test 2>&1 | Select-String -Pattern "passing|failing|FAIL|PASS" | Select-Object -First 10</automated>
  </verify>
  <acceptance_criteria>
    - 29-04-SPEED-INSIGHTS.md was read and its `Decision:` line is reflected in the SUMMARY
    - If Decision was insufficient-data: NO source files were modified and the SUMMARY documents the close-after-Wave-1 outcome with a reference to D-12
    - If Decision was targeted-fixes-needed: every regression listed under `## Findings` has a named corresponding code change in the SUMMARY, and no fix was applied for a metric rated Good
    - `npm run build` completes with "Compiled successfully" and exits 0
    - The existing test suite still passes (no regression to trade offer, collection controls, or variant tracking behavior)
  </acceptance_criteria>
  <done>Each identified regression has a corresponding fix, or the phase is formally closed with a documented no-op when data was insufficient.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API route | If an INP fix touches a mutation component, the existing session-derived userId guard still applies — fixes are UI-state only |
| recorded findings → code | Findings are read from a planning doc and translated to minimal UI changes; no untrusted runtime input introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-10 | Tampering | INP fix (startTransition wrapping) | accept | startTransition/useDeferredValue are client-render scheduling only; they do not change the fetch payload, the revalidateTag key, or the session-derived userId from plan 02 |
| T-29-11 | Information Disclosure | targeted LCP/FCP fixes | accept | Image/skeleton tweaks render public card data only; no new user-data surface is introduced by tightening `sizes` or adding skeleton placeholders |
| T-29-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages are installed — INP/LCP fixes use React and Next.js APIs already in the project (startTransition, useDeferredValue, next/image) |
</threat_model>

<verification>
- `npm run build` exits 0 with "Compiled successfully"
- Full test suite passes (trade offer, collection controls, variant tracking unregressed)
- SUMMARY maps each recorded finding to a fix, or documents the insufficient-data no-op closure
</verification>

<success_criteria>
- Every Speed-Insights-identified regression has a corresponding fix applied (PERF-10 success criterion 2), OR the phase is closed with a documented no-op when data was insufficient (D-12)
- Card detail page builds and existing sections still function (PERF-10 success criterion 3)
</success_criteria>

<output>
Create `.planning/phases/29-card-detail-page-performance/29-05-SUMMARY.md` when done
</output>
