---
phase: 05.1-want-list-gap-fix
plan: 01
subsystem: database, ui
tags: [drizzle-orm, react, want-list, deck-builder, postgresql]

# Dependency graph
requires:
  - phase: 05-want-list
    provides: getDeckCardsForUser(), WantListTab, /api/want-list route — the base want list infrastructure this plan extends
  - phase: 04-deck-builder
    provides: deck-builder.tsx with leaderCardDefinitionId/baseCardDefinitionId state, decks schema with FK columns

provides:
  - getDeckCardsForUser() returns synthetic leader/base rows with quantity:1, isSideboard:false
  - WantListTab receives leader/base as additional deckCards entries when non-null
  - Per-deck and combined want lists now surface Leader/Base shortfalls

affects: [05-want-list, want-list, deck-builder]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "resolvePrinting() helper inside getDeckCardsForUser() — scoped helper reusing getDeckForExport's printingDetails join pattern"
    - "Spread-conditional null-guard for optional FK entries in React props: ...(state.leaderCardDefinitionId ? [...] : [])"
    - "Synthetic row emission: append typed rows matching Drizzle inferred select shape to DB query results"

key-files:
  created: []
  modified:
    - src/db/queries/decks.ts
    - src/components/decks/deck-builder.tsx

key-decisions:
  - "Synthetic rows appended after deckCardRows — shape must match Drizzle inferred type; tsc --noEmit enforces at build time (T-05.1-02)"
  - "N+1 resolvePrinting() calls per deck are acceptable for v1 small user deck counts; batch join deferred to v2 (T-05.1-03 accepted)"
  - "null leader/base FK values produce no synthetic row (D-01/D-02/D-08) — consistent in both per-deck WantListTab and combined getDeckCardsForUser()"

patterns-established:
  - "Scoped resolvePrinting() helper: define inline in the function that uses it, matching getDeckForExport's printingDetails pattern"
  - "Spread-conditional for optional deck slots in deckCards prop: ...(state.slotId ? [{ cardDefinitionId: state.slotId, quantity: 1 }] : [])"

requirements-completed: [WANT-01, WANT-02]

# Metrics
duration: 8min
completed: 2026-05-07
---

# Phase 5.1 Plan 01: Want List Gap Fix Summary

**Leader and Base card shortfalls now surface in both per-deck WantListTab and combined /api/want-list via synthetic rows in getDeckCardsForUser() and prop injection in deck-builder.tsx**

## Performance

- **Duration:** 8 min
- **Started:** 2026-05-07T05:16:48Z
- **Completed:** 2026-05-07T05:24:00Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Extended `getDeckCardsForUser()` to emit synthetic leader/base rows (quantity: 1, isSideboard: false) for every non-null leader/base FK on each user deck — combined want list at `/api/want-list` now aggregates Leader/Base shortfalls without any route changes
- Injected leader and base as additional `{cardDefinitionId, quantity: 1}` entries into the WantListTab `deckCards` prop at the call site in `deck-builder.tsx` — per-deck want list now shows missing Leaders/Bases grouped under the correct TYPE_ORDER headers
- Null leader/base slots silently produce no synthetic row (D-01/D-02/D-08), preserving clean UX for in-progress draft decks

## Task Commits

1. **Task 1: Extend getDeckCardsForUser() with synthetic leader/base rows** - `433acbb` (feat)
2. **Task 2: Inject leader/base into WantListTab deckCards prop** - `8756c56` (feat)

## Files Created/Modified

- `src/db/queries/decks.ts` - Extended `getDeckCardsForUser()`: Step 1 now selects `leaderCardDefinitionId`/`baseCardDefinitionId` from decks; added `resolvePrinting()` helper; Step 3 emits synthetic rows; returns `[...deckCardRows, ...syntheticRows]`
- `src/components/decks/deck-builder.tsx` - WantListTab call site: replaced single-line `deckCards` prop with spread array including leader/base conditional entries

## Decisions Made

None beyond what was specified in CONTEXT.md decisions D-01 through D-08. Plan executed exactly as specified.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None. TypeScript check (`npx tsc --noEmit`) passed after both tasks.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- WANT-01 and WANT-02 are now fully satisfied: per-deck and combined want lists include Leader and Base shortfalls
- Phase 5.1 is complete
- No blockers. Ready for any subsequent feature work (Phase 5.2 rarity filter fix is already planned separately)

## Self-Check: PASSED

Files modified confirmed in working tree:
- `src/db/queries/decks.ts` - modified and committed at 433acbb
- `src/components/decks/deck-builder.tsx` - modified and committed at 8756c56

Commits confirmed in git log:
- `433acbb` feat(05.1-01): extend getDeckCardsForUser() with synthetic leader/base rows
- `8756c56` feat(05.1-01): inject leader/base into WantListTab deckCards prop

---
*Phase: 05.1-want-list-gap-fix*
*Completed: 2026-05-07*
