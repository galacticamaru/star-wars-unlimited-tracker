---
phase: 04-deck-builder
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified: [src/components/deck-builder/deck-builder-client.tsx, src/components/deck-builder/deck-sidebar.tsx, src/app/decks/[id]/page.tsx]
autonomous: true
requirements: [DECK-01, DECK-02]
must_haves:
  truths:
    - "User can add/remove cards to the deck list"
    - "User can select exactly 1 Leader and 1 Base"
    - "Deck sidebar shows validation status and cost curve"
  artifacts:
    - path: "src/components/deck-builder/deck-builder-client.tsx"
      provides: "Main deck builder container and state management"
---

<objective>
Build the core Deck Builder UI container and sidebar. Manage the complex deck state using `useReducer` and integrate the validation utility from Plan 02.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-deck-builder/04-RESEARCH.md
@src/lib/deck-validation.ts
@src/components/ui/button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deck State & Container</name>
  <files>src/components/deck-builder/deck-builder-client.tsx</files>
  <action>
    Create `DeckBuilderClient` component.
    - Implement `useReducer` for `DeckState` (name, leaderId, baseId, cards[]).
    - Handle actions: SET_LEADER, SET_BASE, ADD_CARD, REMOVE_CARD, UPDATE_QUANTITY, SET_NAME.
    - Integrate `validateDeck` in a `useMemo` that updates whenever state changes.
    - Provide a "Save" button that calls `/api/decks` (POST/PATCH).
  </action>
  <verify>
    <automated>MISSING — Wave 0 must create src/components/deck-builder/deck-builder.test.tsx first</automated>
  </verify>
  <done>Deck builder container manages state and validation correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Sidebar UI</name>
  <files>src/components/deck-builder/deck-sidebar.tsx</files>
  <action>
    Create `DeckSidebar` component.
    - Display slots for Leader and Base (with "Select" placeholders if empty).
    - Render scrollable list of cards in main deck (Name, Subtitle, Aspect icons, Quantity controls).
    - Show Validation summary (isValid, error list).
    - Render Stats: Cost curve (simple bar chart using CSS), Aspect breakdown, Unit/Event/Upgrade counts.
  </action>
  <verify>
    <automated>npm test src/components/deck-builder/deck-sidebar.test.tsx</automated>
  </verify>
  <done>Sidebar displays deck contents and statistics accurately.</done>
</task>

<task type="auto">
  <name>Task 3: Deck Builder Route</name>
  <files>src/app/decks/[id]/page.tsx</files>
  <action>
    Create the deck builder page.
    - Fetch deck by ID (or initialize empty for "new").
    - Fetch all card definitions (for the catalog integration later).
    - Render `DeckBuilderClient`.
  </action>
  <verify>
    <automated>curl -I http://localhost:3000/decks/new</automated>
  </verify>
  <done>Route is accessible and initializes the builder.</done>
</task>

</tasks>

<success_criteria>
- User can build a deck via state-driven UI.
- Legality and stats update in real-time.
- Decks can be saved to the database.
</success_criteria>

<output>
After completion, create `.planning/phases/04-deck-builder/04-03-SUMMARY.md`
</output>
