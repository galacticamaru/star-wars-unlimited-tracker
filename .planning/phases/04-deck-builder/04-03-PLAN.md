---
phase: 04-deck-builder
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified: [src/app/decks/page.tsx, src/app/decks/[id]/page.tsx, src/components/decks/deck-builder.tsx, src/components/decks/deck-sidebar.tsx, __tests__/deck-builder.test.tsx]
autonomous: true
requirements: [DECK-01, DECK-02]

must_haves:
  truths:
    - "User can view a list of their decks on the dashboard"
    - "System provides a dedicated page for creating and editing decks"
    - "The builder page displays current deck contents and real-time stats"
  artifacts:
    - path: "src/app/decks/page.tsx"
      provides: "Deck Dashboard"
    - path: "src/components/decks/deck-builder.tsx"
      provides: "Main builder state and layout"
    - path: "src/components/decks/deck-sidebar.tsx"
      provides: "Real-time validation and stats display"
  key_links:
    - from: "src/components/decks/deck-builder.tsx"
      to: "src/lib/deck-validation.ts"
      via: "State updates"
---

<objective>
Build the core deck builder UI, including the dashboard and the builder container with state management.

Purpose: Provide the visual framework and state handling for building decks.
Output: Functional dashboard and a builder page with real-time stats.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/deck-validation.ts
</context>

<interfaces>
From src/lib/deck-validation.ts:
```typescript
export interface DeckValidationResult {
  isValid: boolean;
  errors: string[];
  warnings: string[];
  stats: {
    costCurve: Record<number, number>;
    typeCounts: Record<string, number>;
    aspectCounts: Record<string, number>;
    totalMain: number;
    totalSideboard: number;
  };
}
export function validateDeck(leader: any, base: any, mainDeck: any[], sideboard: any[]): DeckValidationResult;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create Deck Dashboard</name>
  <files>src/app/decks/page.tsx</files>
  <action>
    Implement the dashboard at `/decks`:
    - Fetch all decks via `GET /api/decks`.
    - Display list of decks with names and "Edit" / "Delete" buttons.
    - Implement "Create New Deck" button that prompts for a name, calls `POST /api/decks`, and redirects to `/decks/[id]`.
    - Use `Delete` button to call `DELETE /api/decks/[id]`.
  </action>
  <verify>
    <automated>npx vitest src/app/decks/page.test.tsx --run</automated>
  </verify>
  <done>Dashboard handles basic deck listing, creation, and deletion.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Builder Container with useReducer</name>
  <files>src/app/decks/[id]/page.tsx, src/components/decks/deck-builder.tsx, __tests__/deck-builder.test.tsx</files>
  <action>
    Create the builder page and component using `cardDefinitionId` consistently:
    - `page.tsx`: Fetch deck data and pass to `DeckBuilder`.
    - `DeckBuilder`: Manage deck state using `useReducer`.
      - State: `{ name, leaderCardDefinitionId, baseCardDefinitionId, isDraft, mainDeck: { cardDefinitionId: qty }, sideboard: { cardDefinitionId: qty } }`.
      - Actions: `SET_LEADER`, `SET_BASE`, `ADD_CARD`, `REMOVE_CARD`, `SET_QUANTITY`, `SAVE_DECK`.
    - Create `__tests__/deck-builder.test.tsx` to verify reducer logic and state updates.
  </action>
  <verify>
    <automated>npx vitest __tests__/deck-builder.test.tsx --run</automated>
  </verify>
  <done>Builder state is correctly managed and verified via unit tests.</done>
</task>

<task type="auto">
  <name>Task 3: Build Stats & Validation Sidebar</name>
  <files>src/components/decks/deck-sidebar.tsx</files>
  <action>
    Create a sidebar component for the builder:
    - Accepts the current deck state and full card objects for stats calculation.
    - Calls `validateDeck` on every render (or via useMemo).
    - Displays legality status, `errors`, `warnings`, and cost curve.
  </action>
  <verify>
    <automated>npx vitest src/components/decks/deck-sidebar.test.tsx --run</automated>
  </verify>
  <done>Stats and validation provide immediate feedback to the user.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Server | Save operations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | Save Deck | mitigate | Server-side re-validation of deck structure during PATCH |
</threat_model>

<verification>
Run `npx vitest src/app/decks/page.test.tsx __tests__/deck-builder.test.tsx src/components/decks/deck-sidebar.test.tsx --run`.
</verification>

<success_criteria>
- Dashboard lists decks and allows creation/deletion.
- Builder page loads deck state.
- Sidebar shows correct counts and validation messages.
</success_criteria>

<output>
After completion, create `.planning/phases/04-deck-builder/04-03-SUMMARY.md`
</output>
