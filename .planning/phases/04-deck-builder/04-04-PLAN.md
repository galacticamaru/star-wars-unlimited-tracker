---
phase: 04-deck-builder
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified: [src/components/catalog/catalog-client.tsx, src/components/catalog/card-item.tsx, src/components/catalog/card-grid.tsx, src/components/catalog/card-item.deck.test.tsx]
autonomous: true
requirements: [DECK-03, DECK-04]

must_haves:
  truths:
    - "Catalog can be switched to 'selector' mode for adding cards to a deck"
    - "Cards show both owned quantities and current deck quantities"
    - "Cards with insufficient owned copies are visually highlighted as shortfalls"
  artifacts:
    - path: "src/components/catalog/catalog-client.tsx"
      provides: "Selector mode support"
  key_links:
    - from: "src/components/catalog/card-item.tsx"
      to: "DeckBuilder reducer"
      via: "onDeckUpdate callback"
---

<objective>
Integrate the card catalog into the deck builder as a selector and add ownership highlights.

Purpose: Allow users to add cards to their deck while seeing their collection status.
Output: A reusable card selector with deck-specific controls and shortfall highlighting.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/catalog/catalog-client.tsx
@src/components/catalog/card-item.tsx
@src/components/catalog/card-grid.tsx
</context>

<interfaces>
From Plan 03:
```typescript
interface DeckCard {
  cardDefinitionId: number;
  quantity: number;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update CatalogClient for Selector Mode</name>
  <files>src/components/catalog/catalog-client.tsx, src/components/catalog/card-grid.tsx</files>
  <action>
    Modify `CatalogClientProps` to include:
    - `mode?: 'catalog' | 'selector'` (default 'catalog')
    - `deckCounts?: Record<number, number>` (cardDefinitionId -> quantity)
    - `onDeckUpdate?: (cardDefinitionId: number, count: number) => void`
    Pass these new props down through `CardGrid` to `CardItem`.
  </action>
  <verify>
    <automated>npx vitest src/components/catalog/catalog-client.browser.test.tsx --run</automated>
  </verify>
  <done>CatalogClient accepts deck-specific props and passes them to children.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Selector Controls and Shortfall Highlight in CardItem</name>
  <files>src/components/catalog/card-item.tsx, src/components/catalog/card-item.deck.test.tsx</files>
  <action>
    Update `CardItem` to handle selector mode using `cardDefinitionId`:
    - If `mode === 'selector'`:
      - Overlay shows "In Deck: X" with Plus/Minus buttons.
      - Buttons call `onDeckUpdate`.
      - Show `ownedCount` as a secondary badge.
    - If `deckCount > ownedCount`, add high-visibility "Shortfall" highlight (e.g., `border-red-500` and "Missing X" badge).
    - Handle Leader/Base: "Set as Leader" / "Set as Base" buttons if card type matches.
    - Create `src/components/catalog/card-item.deck.test.tsx` to verify shortfall logic.
  </action>
  <verify>
    <automated>npx vitest src/components/catalog/card-item.deck.test.tsx --run</automated>
  </verify>
  <done>CardItem provides deck-specific interaction and shortfall feedback.</done>
</task>

<task type="auto">
  <name>Task 3: Connect Selector to DeckBuilder</name>
  <files>src/components/decks/deck-builder.tsx</files>
  <action>
    In `DeckBuilder`, implement the catalog selector:
    - Render `CatalogClient` with `mode="selector"`.
    - Pass current deck quantities to `deckCounts`.
    - Provide a callback to `onDeckUpdate` that dispatches to the builder's reducer.
  </action>
  <verify>
    <automated>npx vitest __tests__/deck-builder.test.tsx --run</automated>
  </verify>
  <done>DeckBuilder is fully integrated with the catalog selector.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component Props | Interaction between builder and catalog |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Information Disclosure | Selector | accept | Collection counts are already local; showing them in selector mode is intended. |
</threat_model>

<verification>
Run `npx vitest src/components/catalog/card-item.deck.test.tsx src/components/catalog/catalog-client.browser.test.tsx --run`.
</verification>

<success_criteria>
- Catalog operates in both 'catalog' (collection) and 'selector' (deck) modes.
- `CardItem` shows deck quantity and shortfall in selector mode.
- Adding cards in the selector updates the `DeckBuilder` state immediately.
</success_criteria>

<output>
After completion, create `.planning/phases/04-deck-builder/04-04-SUMMARY.md`
</output>
