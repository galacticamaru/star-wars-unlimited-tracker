---
phase: 04-deck-builder
plan: 04
type: execute
wave: 3
depends_on: [04-03]
files_modified: [src/components/deck-builder/catalog-wrapper.tsx, src/components/catalog/card-item.tsx, src/components/catalog/card-grid.tsx]
autonomous: true
requirements: [DECK-03, DECK-04]
must_haves:
  truths:
    - "Every card in the catalog shows user's owned count inline"
    - "Cards where owned count < deck count are visually highlighted"
    - "Clicking 'Add' in catalog correctly updates deck state"
  artifacts:
    - path: "src/components/deck-builder/catalog-wrapper.tsx"
      provides: "Deck-builder specific catalog integration"
---

<objective>
Integrate the existing Card Catalog into the Deck Builder. Add "Add to Deck" functionality and visual highlights for collection status (owned counts and missing cards).
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/components/catalog/catalog-client.tsx
@src/components/catalog/card-item.tsx
@src/components/deck-builder/deck-builder-client.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Catalog Refactoring</name>
  <files>src/components/catalog/card-item.tsx, src/components/catalog/card-grid.tsx</files>
  <action>
    Update `CardItem` and `CardGrid` to support deck builder interactions:
    - Add `onAdd` and `onSetLeader`/`onSetBase` optional callbacks to `CardItem`.
    - Add `deckCount` prop to `CardItem` to show how many of this card are currently in the deck.
    - Implement visual highlight (e.g., red border or indicator) if `deckCount > ownedCount`.
    - Ensure `ownedCount` (from collection) is always displayed clearly.
  </action>
  <verify>
    <automated>npm test src/components/catalog/card-item.test.tsx</automated>
  </verify>
  <done>Catalog components support deck construction indicators and actions.</done>
</task>

<task type="auto">
  <name>Task 2: Catalog Wrapper for Builder</name>
  <files>src/components/deck-builder/catalog-wrapper.tsx</files>
  <action>
    Create `CatalogWrapper` that embeds `TopBar`, `CardGrid`, and `EmptyState`.
    - Wire `onAdd` to the `DeckBuilderClient` dispatch.
    - Pass down `deckCounts` from the active deck state.
    - Use `nuqs` for search/filters just like the main catalog, but ensure they don't conflict (or intentionally share state if desired).
  </action>
  <verify>
    <automated>MISSING — Wave 0 must create src/components/deck-builder/catalog-wrapper.test.tsx first</automated>
  </verify>
  <done>Deck builder includes a fully functional, filtered catalog for card selection.</done>
</task>

</tasks>

<success_criteria>
- Catalog cards show owned counts.
- Cards missing from collection are highlighted in the builder.
- Catalog interactions correctly modify the deck in progress.
</success_criteria>

<output>
After completion, create `.planning/phases/04-deck-builder/04-04-SUMMARY.md`
</output>
