---
phase: 04-deck-builder
plan: 02
type: tdd
wave: 2
depends_on: [04-01]
files_modified: [src/lib/deck-validation.ts, src/lib/deck-validation.test.ts]
autonomous: true
requirements: [DECK-01, DECK-05]
must_haves:
  truths:
    - "Deck validation correctly identifies 3-copy limit violations across reprints"
    - "Aspect penalty logic correctly identifies missing aspect icons"
    - "Stats calculation provides accurate cost curve and unit counts"
  artifacts:
    - path: "src/lib/deck-validation.ts"
      provides: "Deck legality and stats utility"
---

<objective>
Implement the core business logic for deck validation and statistics calculation using a TDD approach. This ensures the deck builder enforces SWU Premier rules correctly, especially the 3-copy limit across reprints (identity-based grouping).
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-deck-builder/04-RESEARCH.md
@src/db/schema.ts
</context>

<feature>
  <name>Deck Validation & Stats</name>
  <files>src/lib/deck-validation.ts, src/lib/deck-validation.test.ts</files>
  <behavior>
    - Rule: Exactly 1 Leader and 1 Base required.
    - Rule: Minimum 50 cards in main deck.
    - Rule: Maximum 10 cards in sideboard.
    - Rule: Maximum 3 copies of any card by identity (Name + Subtitle) across all printings.
    - Calculation: Extract provided aspects from Leader + Base.
    - Calculation: Identify "Aspect Penalty" (+2 cost) for cards missing required aspects.
    - Stats: Count Ground units, Space units, Events, Upgrades.
    - Stats: Generate cost curve data (counts for cost 0, 1, 2, 3, 4, 5, 6, 7+).
  </behavior>
  <implementation>
    1. Create `validateDeck(deckState, allCards)` function.
    2. Implement identity grouping: `const identity = card.name + (card.subtitle ?? '')`.
    3. Implement aspect pool matching for penalty calculation.
    4. Return `validationResult` with `isValid`, `errors`, `aspectsProvided`, and `stats`.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Validation Logic</name>
  <files>src/lib/deck-validation.ts, src/lib/deck-validation.test.ts</files>
  <behavior>
    - Validates 50-card minimum.
    - Validates 3-copy limit by Name + Subtitle (Pitfall 1 from RESEARCH.md).
    - Calculates aspect penalties using pool-based matching (Pitfall 2 from RESEARCH.md).
  </behavior>
  <action>
    Follow RED-GREEN-REFACTOR cycle to implement validation and stats logic.
    Ensure test cases cover reprints (different definition IDs, same Name+Subtitle).
  </action>
  <verify>
    <automated>npm test src/lib/deck-validation.test.ts</automated>
  </verify>
  <done>Validation logic is robust and passes all test cases.</done>
</task>

</tasks>

<success_criteria>
- Validation utility correctly identifies illegal decks.
- Aspect penalty logic handles single, double, and missing aspects.
- Stats calculation provides all data needed for the sidebar UI.
</success_criteria>

<output>
After completion, create `.planning/phases/04-deck-builder/04-02-SUMMARY.md`
</output>
