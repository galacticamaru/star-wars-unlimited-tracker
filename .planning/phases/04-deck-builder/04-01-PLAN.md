---
phase: 04-deck-builder
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/db/schema.ts, src/db/queries/decks.ts, src/app/api/decks/route.ts, src/app/api/decks/[id]/route.ts]
autonomous: true
requirements: [DECK-01, DECK-02]
must_haves:
  truths:
    - "Database schema supports deck metadata and card lists"
    - "API supports CRUD operations for decks"
  artifacts:
    - path: "src/db/queries/decks.ts"
      provides: "CRUD query logic for decks"
    - path: "src/app/api/decks/route.ts"
      provides: "List and Create endpoints"
---

<objective>
Set up the database schema and API endpoints for deck persistence. This follows the single-user pattern (userId: 1) established in previous phases.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@src/db/schema.ts
@src/app/api/collection/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update DB Schema</name>
  <files>src/db/schema.ts</files>
  <action>
    Add `decks` and `deckCards` tables to `src/db/schema.ts` as defined in RESEARCH.md.
    - `decks`: id, userId (default 1), name, leaderCardDefinitionId, baseCardDefinitionId, timestamps.
    - `deckCards`: deckId (cascade delete), cardDefinitionId, quantity, isSideboard.
    - Composite primary key for `deckCards` on [deckId, cardDefinitionId, isSideboard].
    Run `npx drizzle-kit push` to update the database.
  </action>
  <verify>
    <automated>npx drizzle-kit check</automated>
  </verify>
  <done>Schema updated and pushed to Neon.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Deck Queries</name>
  <files>src/db/queries/decks.ts</files>
  <action>
    Implement CRUD queries using Drizzle:
    - `getDecks(userId)`: Returns list of decks with basic metadata and card counts.
    - `getDeckById(id)`: Returns full deck details including card list (definition joins).
    - `saveDeck(deck)`: Transactional save (upsert deck, delete existing deckCards, insert new deckCards).
    - `deleteDeck(id)`: Deletes deck (cascade will handle cards).
  </action>
  <verify>
    <automated>MISSING — Wave 0 must create src/db/queries/decks.test.ts first</automated>
  </verify>
  <done>Queries implemented and verified with basic unit test.</done>
</task>

<task type="auto">
  <name>Task 3: Create API Routes</name>
  <files>src/app/api/decks/route.ts, src/app/api/decks/[id]/route.ts</files>
  <action>
    Implement Next.js App Router API handlers:
    - `GET /api/decks`: Returns `getDecks(1)`.
    - `POST /api/decks`: Accepts deck data, calls `saveDeck`.
    - `GET /api/decks/[id]`: Returns `getDeckById(id)`.
    - `PATCH /api/decks/[id]`: Updates existing deck via `saveDeck`.
    - `DELETE /api/decks/[id]`: Calls `deleteDeck(id)`.
  </action>
  <verify>
    <automated>curl -X GET http://localhost:3000/api/decks</automated>
  </verify>
  <done>API endpoints functional and returning correct structures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client→API | Deck data (IDs and counts) sent to server for persistence |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | /api/decks | mitigate | Use Drizzle for parameterized queries; validate card counts are > 0. |
| T-04-02 | Information Disclosure | /api/decks | accept | No PII; single-user mode for v1 makes all decks accessible to ID 1. |
</threat_model>

<success_criteria>
- Decks can be created, read, updated, and deleted via API.
- Database correctly stores deck contents and metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/04-deck-builder/04-01-SUMMARY.md`
</output>
