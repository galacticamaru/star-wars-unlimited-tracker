---
phase: 04-deck-builder
plan: 05
type: execute
wave: 4
depends_on: [04-03, 04-04]
files_modified: [src/app/decks/page.tsx, src/lib/deck-export.ts, src/components/deck-builder/export-buttons.tsx]
autonomous: true
requirements: [DECK-02, Export]
must_haves:
  truths:
    - "User can view a list of all saved decks"
    - "User can delete a deck from the list"
    - "User can export a deck in Melee (.txt) and JSON formats"
  artifacts:
    - path: "src/app/decks/page.tsx"
      provides: "Deck management dashboard"
    - path: "src/lib/deck-export.ts"
      provides: "Export formatters"
---

<objective>
Build the deck management dashboard (list and delete) and implement export functionality for Melee and JSON formats.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-deck-builder/04-RESEARCH.md
@src/db/queries/decks.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deck Management Page</name>
  <files>src/app/decks/page.tsx</files>
  <action>
    Create a dashboard for deck management.
    - Fetch all decks for user 1.
    - Display cards for each deck with name, createdAt, and simple counts.
    - Provide "Edit" link (to `/decks/[id]`) and "Delete" button.
    - Implement a "Create New Deck" button that redirects to `/decks/new`.
  </action>
  <verify>
    <automated>curl -I http://localhost:3000/decks</automated>
  </verify>
  <done>User can manage their list of decks from a central page.</done>
</task>

<task type="auto">
  <name>Task 2: Export Logic</name>
  <files>src/lib/deck-export.ts</files>
  <action>
    Implement export formatters:
    - `exportToMelee(deck)`: Returns string in Melee format (Leader, Base, Main, Sideboard sections).
    - `exportToJSON(deck)`: Returns string in SwuDB compatible JSON format.
  </action>
  <verify>
    <automated>npm test src/lib/deck-export.test.ts</automated>
  </verify>
  <done>Export logic accurately formats deck data for external tools.</done>
</task>

<task type="auto">
  <name>Task 3: Export Buttons UI</name>
  <files>src/components/deck-builder/export-buttons.tsx</files>
  <action>
    Create a component with "Export Melee" and "Export JSON" buttons.
    - Trigger file downloads with correct filenames (e.g., `deck-name.txt`, `deck-name.json`).
    - Integrate this into the Deck Builder sidebar and the Deck List page.
  </action>
  <verify>
    <checkpoint:human-verify gate="blocking">
      <what-built>Deck management page and export functionality</what-built>
      <how-to-verify>
        1. Navigate to /decks.
        2. Create a new deck, save it.
        3. Back in /decks, verify it appears in the list.
        4. Click 'Export Melee' and verify the downloaded file format.
        5. Delete the deck and verify it disappears.
      </how-to-verify>
      <resume-signal>approved</resume-signal>
    </checkpoint:human-verify>
  </verify>
  <done>Users can export their decks for use in tournaments or other online builders.</done>
</task>

</tasks>

<success_criteria>
- All saved decks are listed and manageable.
- Exports are valid and downloadable.
</success_criteria>

<output>
After completion, create `.planning/phases/04-deck-builder/04-05-SUMMARY.md`
</output>
