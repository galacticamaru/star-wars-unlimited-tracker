---
phase: 11-new-home-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/queries/catalog.ts
  - src/db/queries/catalog.test.ts
  - src/components/home/hero-section.test.tsx
  - src/components/home/high-value-grid.test.tsx
  - src/app/cards/page.tsx
  - src/app/cards/[set-code]/[card-number]/page.tsx
autonomous: true
requirements:
  - REQ-HOME-01

must_haves:
  truths:
    - "Navigating to /cards loads the existing catalog with full filter and search functionality"
    - "Clicking 'Back to catalog' from a card detail page goes to /cards, not /"
    - "getTopCardsByPrice(10) returns at most 10 cards ordered by priceUsd DESC with no nulls and no duplicates"
    - "Test files exist for all three new components with Vitest stubs"
  artifacts:
    - path: "src/app/cards/page.tsx"
      provides: "CatalogPage server component at /cards route"
      contains: "export default async function CatalogPage"
    - path: "src/db/queries/catalog.ts"
      provides: "getTopCardsByPrice query"
      exports: ["getTopCardsByPrice"]
    - path: "src/db/queries/catalog.test.ts"
      provides: "getTopCardsByPrice test stubs"
      contains: "describe('getTopCardsByPrice()"
    - path: "src/components/home/hero-section.test.tsx"
      provides: "HeroSection test stub"
      contains: "@vitest-environment jsdom"
    - path: "src/components/home/high-value-grid.test.tsx"
      provides: "HighValueGrid and CardPriceTile test stubs"
      contains: "@vitest-environment jsdom"
  key_links:
    - from: "src/app/cards/page.tsx"
      to: "src/db/queries/catalog.ts"
      via: "import { getAllCards, getFilterOptions }"
      pattern: "getAllCards|getFilterOptions"
    - from: "src/app/cards/[set-code]/[card-number]/page.tsx"
      to: "/cards"
      via: "Back to catalog href"
      pattern: "href=\"/cards\""
---

<objective>
Lay the data and routing foundation for the new home page.

Purpose: Three things must exist before any UI can be built: (1) the CatalogPage must live at `/cards` so the root `/` can be repurposed, (2) the `getTopCardsByPrice` Drizzle query must exist to feed the high-value grid, and (3) test stubs for all new components must be in place (Wave 0 contract from VALIDATION.md).

Output:
- `src/app/cards/page.tsx` — CatalogPage moved from root route to `/cards`
- `src/app/cards/[set-code]/[card-number]/page.tsx` — "Back to catalog" href fixed to `/cards`
- `src/db/queries/catalog.ts` — extended with `getTopCardsByPrice(limit)`
- `src/db/queries/catalog.test.ts` — new `describe('getTopCardsByPrice()')` block
- `src/components/home/hero-section.test.tsx` — new Wave 0 test stub
- `src/components/home/high-value-grid.test.tsx` — new Wave 0 test stub
</objective>

<execution_context>
@C:\Users\alan_\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\alan_\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-new-home-page/11-CONTEXT.md
@.planning/phases/11-new-home-page/11-RESEARCH.md
@.planning/phases/11-new-home-page/11-PATTERNS.md
@.planning/phases/11-new-home-page/11-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/db/queries/catalog.ts (current state — will be extended):
```typescript
import { db } from '@/db';
import { cardDefinitions, cardPrintings, userCollections } from '@/db/schema';
import { eq, and, notIlike, asc, sql } from 'drizzle-orm';

export async function getAllCards(userId?: number) { ... }
export async function getFilterOptions() { ... }
```

From src/app/page.tsx (current CatalogPage — its content moves to src/app/cards/page.tsx):
```typescript
import { getAllCards, getFilterOptions } from '@/db/queries/catalog';
import { CatalogClient } from '@/components/catalog/catalog-client';
import { auth } from '@/lib/auth';
import { headers } from 'next/headers';

export const dynamic = 'force-dynamic';

export default async function CatalogPage() {
  const session = await auth.api.getSession({ headers: await headers() });
  const [cards, filterOptions] = await Promise.all([
    getAllCards(session?.user.id ? Number(session.user.id) : undefined),
    getFilterOptions(),
  ]);
  const plainCards = cards.map(c => ({
    id: c.id, swudbId: c.swudbId, name: c.name, subtitle: c.subtitle,
    type: c.type, aspects: c.aspects ?? [], arenas: c.arenas ?? [],
    traits: c.traits ?? [], keywords: c.keywords ?? [], cost: c.cost,
    power: c.power, hp: c.hp, rarity: c.rarity, setCode: c.setCode,
    collectorNumber: c.collectorNumber, frontArtUrl: c.frontArtUrl,
    backArtUrl: c.backArtUrl, frontText: c.frontText, backText: c.backText,
    epicAction: c.epicAction, doubleSided: c.doubleSided, unique: c.unique,
    priceEur: c.priceEur, priceUsd: c.priceUsd,
  }));
  return <CatalogClient cards={plainCards} filterOptions={filterOptions} />;
}
```

From src/app/cards/[set-code]/[card-number]/page.tsx (line 39–49 area):
```tsx
// Line 41 — THIS IS THE LINE TO CHANGE:
href="/"   // change to: href="/cards"
// Context: <Link href="/" ...><ChevronLeft />Back to catalog</Link>
```

From src/db/queries/catalog.test.ts (current stub — extend this file):
```typescript
// @vitest-environment node
import { describe, it } from 'vitest';

describe('getAllCards()', () => {
  it.todo('returns an array of card objects');
  // ... 4 more .todo tests
});

describe('getFilterOptions()', () => {
  it.todo('returns { sets: string[], types: string[] }');
  // ... 2 more .todo tests
});
```

From src/components/catalog/card-item.test.tsx (jsdom test pattern to replicate):
```typescript
/**
 * @vitest-environment jsdom
 */
import { expect, test, vi } from 'vitest';
import { render, screen } from '@testing-library/react';
import React from 'react';

vi.mock('next/image', () => ({
  default: (props: any) => <img {...props} />,
}));
vi.mock('next/link', () => ({
  default: ({ children, href }: any) => <a href={href}>{children}</a>,
}));
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend catalog query with getTopCardsByPrice + add test stubs</name>
  <files>
    src/db/queries/catalog.ts,
    src/db/queries/catalog.test.ts,
    src/components/home/hero-section.test.tsx,
    src/components/home/high-value-grid.test.tsx
  </files>

  <read_first>
    - src/db/queries/catalog.ts — read the full file to see current imports and query patterns before extending
    - src/db/queries/catalog.test.ts — read the full stub so the new describe block appends correctly after existing blocks
    - src/components/catalog/card-item.test.tsx — read the jsdom test file header (lines 1–17) to replicate the exact mock pattern for new test stubs
    - .planning/phases/11-new-home-page/11-VALIDATION.md — read Wave 0 requirements section for the exact test case list
  </read_first>

  <behavior>
    - getTopCardsByPrice(10) returns exactly 10 results when ≥10 qualifying cards exist
    - getTopCardsByPrice excludes cards with null priceUsd
    - getTopCardsByPrice excludes cards whose type contains 'token' (case-insensitive)
    - getTopCardsByPrice excludes non-Normal variant cards (no Foil/Hyperspace duplicates)
    - getTopCardsByPrice returns cards ordered by priceUsd DESC (highest first)
  </behavior>

  <action>
**Step 1 — Extend src/db/queries/catalog.ts:**

Add `desc` and `isNotNull` to the existing drizzle-orm import line. The current import is:
```typescript
import { eq, and, notIlike, asc, sql } from 'drizzle-orm';
```
Change it to:
```typescript
import { eq, and, notIlike, asc, sql, desc, isNotNull } from 'drizzle-orm';
```

Append the following function at the end of the file (after `getFilterOptions`):
```typescript
export async function getTopCardsByPrice(limit: number) {
  return db
    .select({
      id: cardDefinitions.id,
      name: cardDefinitions.name,
      type: cardDefinitions.type,
      setCode: cardPrintings.setCode,
      collectorNumber: cardPrintings.collectorNumber,
      frontArtUrl: cardPrintings.frontArtUrl,
      backArtUrl: cardPrintings.backArtUrl,
      priceUsd: cardDefinitions.priceUsd,
      // CRITICAL: no timestamp columns — Date objects cannot cross RSC→client boundary
    })
    .from(cardDefinitions)
    .innerJoin(cardPrintings, eq(cardDefinitions.id, cardPrintings.cardDefinitionId))
    .where(
      and(
        isNotNull(cardDefinitions.priceUsd),
        notIlike(cardDefinitions.type, '%token%'),
        eq(cardPrintings.variantType, 'Normal'),
      )
    )
    .orderBy(desc(cardDefinitions.priceUsd))
    .limit(limit);
}
```

**Step 2 — Extend src/db/queries/catalog.test.ts:**

Append the following describe block after the existing `getFilterOptions()` describe block (keep all existing content):
```typescript
describe('getTopCardsByPrice()', () => {
  it.todo('returns at most `limit` cards');
  it.todo('excludes cards with null priceUsd');
  it.todo('excludes token type cards (type contains "token", case-insensitive)');
  it.todo('excludes non-Normal variant cards (no Foil/Hyperspace duplicates)');
  it.todo('returns cards ordered by priceUsd DESC (highest first)');
});
```

**Step 3 — Create src/components/home/hero-section.test.tsx:**

Create the `src/components/home/` directory if it does not exist, then create `hero-section.test.tsx` with this exact content:
```typescript
/**
 * @vitest-environment jsdom
 */
import { expect, test, vi } from 'vitest';
import { render, screen } from '@testing-library/react';
import React from 'react';

vi.mock('next/link', () => ({
  default: ({ children, href }: { children: React.ReactNode; href: string }) => (
    <a href={href}>{children}</a>
  ),
}));

// Mock shadcn Button — render as button element
vi.mock('@/components/ui/button', () => ({
  Button: ({ children, asChild, ...props }: any) => {
    if (asChild) return <>{children}</>;
    return <button {...props}>{children}</button>;
  },
}));

// Wave 0 stubs — implementations pending Plan 02
test.todo('HeroSection renders h1 with exact title: "Star Wars Unlimited Card Database and Deck Builder"');
test.todo('HeroSection renders subtitle: "Track your collection, build your decks, and begin trading with up to date market prices"');
test.todo('HeroSection renders CTA link to /collection with text "Import Collection"');
test.todo('HeroSection renders CTA link to /decks with text "Build a Deck"');
test.todo('HeroSection renders CTA link to /binder/manage with text "Trade"');
```

**Step 4 — Create src/components/home/high-value-grid.test.tsx:**

Create `high-value-grid.test.tsx` in the same `src/components/home/` directory:
```typescript
/**
 * @vitest-environment jsdom
 */
import { expect, test, vi } from 'vitest';
import { render, screen } from '@testing-library/react';
import React from 'react';

vi.mock('next/image', () => ({
  default: (props: { src: string; alt: string; [key: string]: any }) => (
    <img src={props.src} alt={props.alt} />
  ),
}));

vi.mock('next/link', () => ({
  default: ({ children, href }: { children: React.ReactNode; href: string }) => (
    <a href={href}>{children}</a>
  ),
}));

const makeCard = (overrides?: Partial<{
  id: number;
  name: string;
  type: string;
  setCode: string;
  collectorNumber: string;
  frontArtUrl: string | null;
  backArtUrl: string | null;
  priceUsd: number | null;
}>) => ({
  id: 1,
  name: 'Darth Vader',
  type: 'Unit',
  setCode: 'SOR',
  collectorNumber: 'SOR-059',
  frontArtUrl: '/vader.jpg',
  backArtUrl: null,
  priceUsd: 4500,
  ...overrides,
});

// Wave 0 stubs — implementations pending Plan 02
test.todo('HighValueGrid renders one CardPriceTile per card in the array (10 tiles for 10 cards)');
test.todo('CardPriceTile displays price as "$X.XX" — e.g. priceUsd=4500 renders "$45.00"');
test.todo('CardPriceTile displays "—" when priceUsd is null');
test.todo('CardPriceTile renders a link to /cards/{setCode}/{cardNumber} — e.g. "/cards/SOR/059" for collectorNumber "SOR-059"');
test.todo('CardPriceTile renders img with alt equal to card name');
```
  </action>

  <verify>
    <automated>npx vitest run src/db/queries/catalog.test.ts src/components/home/hero-section.test.tsx src/components/home/high-value-grid.test.tsx --reporter=verbose</automated>
  </verify>

  <acceptance_criteria>
    - src/db/queries/catalog.ts contains the string `export async function getTopCardsByPrice`
    - src/db/queries/catalog.ts contains `isNotNull(cardDefinitions.priceUsd)`
    - src/db/queries/catalog.ts contains `eq(cardPrintings.variantType, 'Normal')`
    - src/db/queries/catalog.ts contains `notIlike(cardDefinitions.type, '%token%')`
    - src/db/queries/catalog.ts contains `.orderBy(desc(cardDefinitions.priceUsd))`
    - src/db/queries/catalog.ts import line contains `desc, isNotNull`
    - src/db/queries/catalog.test.ts contains `describe('getTopCardsByPrice()')`
    - src/db/queries/catalog.test.ts contains 5 `it.todo` entries inside the getTopCardsByPrice describe block
    - src/components/home/hero-section.test.tsx exists and contains `@vitest-environment jsdom`
    - src/components/home/hero-section.test.tsx contains 5 `test.todo` entries
    - src/components/home/high-value-grid.test.tsx exists and contains `@vitest-environment jsdom`
    - src/components/home/high-value-grid.test.tsx contains 4 `test.todo` entries
    - `npx vitest run src/db/queries/catalog.test.ts src/components/home/hero-section.test.tsx src/components/home/high-value-grid.test.tsx` exits with code 0 (all todo tests are skipped, none fail)
  </acceptance_criteria>

  <done>All three test files pass (todo tests are skipped). getTopCardsByPrice is exported from catalog.ts with the full WHERE clause, orderBy, and limit. TypeScript compiles without errors (`npx tsc --noEmit` exits 0).</done>
</task>

<task type="auto">
  <name>Task 2: Route migration — move CatalogPage to /cards and fix back-link</name>
  <files>
    src/app/cards/page.tsx,
    src/app/cards/[set-code]/[card-number]/page.tsx
  </files>

  <read_first>
    - src/app/page.tsx — read the full file; this is the EXACT content to copy verbatim into src/app/cards/page.tsx (function name stays CatalogPage, all imports stay the same)
    - src/app/cards/[set-code]/[card-number]/page.tsx — read lines 35–55 to see the full back-link context before making the single href change
  </read_first>

  <action>
**Step 1 — Create src/app/cards/page.tsx:**

Copy the ENTIRE content of `src/app/page.tsx` verbatim into `src/app/cards/page.tsx`. No changes to imports, logic, function name, or serialization. The function stays named `CatalogPage`. The `export const dynamic = 'force-dynamic'` stays. This is a pure file copy.

The resulting file must be byte-for-byte identical to the current `src/app/page.tsx`.

**Step 2 — Fix back-link in src/app/cards/[set-code]/[card-number]/page.tsx:**

Find the line containing `href="/"` that is adjacent to the text "Back to catalog". This is at approximately line 41 in the file. Change ONLY the href attribute value:

```tsx
// Before:
href="/"
// After:
href="/cards"
```

Do not change the label text "Back to catalog", the ChevronLeft icon, the className, or any other attribute. Only the href value changes.

**Important:** Do NOT modify or delete `src/app/page.tsx` in this task. It remains as-is. Plan 03 will replace it with the new HomePage.
  </action>

  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - src/app/cards/page.tsx exists
    - src/app/cards/page.tsx contains `export default async function CatalogPage`
    - src/app/cards/page.tsx contains `export const dynamic = 'force-dynamic'`
    - src/app/cards/page.tsx contains `import { getAllCards, getFilterOptions } from '@/db/queries/catalog'`
    - src/app/cards/page.tsx contains `import { CatalogClient } from '@/components/catalog/catalog-client'`
    - src/app/cards/[set-code]/[card-number]/page.tsx does NOT contain `href="/"` adjacent to "Back to catalog" (the old back-link is gone)
    - src/app/cards/[set-code]/[card-number]/page.tsx contains `href="/cards"` (the new back-link)
    - `npx tsc --noEmit` exits with code 0
    - src/app/page.tsx still exists and is unchanged (Plan 03 replaces it)
  </acceptance_criteria>

  <done>CatalogPage is available at /cards route. The back-link from card detail pages points to /cards. TypeScript is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB → RSC | getTopCardsByPrice returns data from PostgreSQL; no user input involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information Disclosure | getTopCardsByPrice query | accept | Query returns only public card data (name, art URL, price). No user PII. No auth context. Accept. |
| T-11-02 | Tampering | `limit` parameter in getTopCardsByPrice | accept | `limit` is a hardcoded literal (10) at the call site in the RSC — never user-supplied. Accept. |
</threat_model>

<verification>
After Plan 01 completes:
- `npx vitest run src/db/queries/catalog.test.ts src/components/home/hero-section.test.tsx src/components/home/high-value-grid.test.tsx` — all pass (todos skip)
- `npx tsc --noEmit` — exits 0
- `src/app/cards/page.tsx` exists with CatalogPage content
- `src/app/cards/[set-code]/[card-number]/page.tsx` back-link points to `/cards`
- `getTopCardsByPrice` exported from `src/db/queries/catalog.ts`
</verification>

<success_criteria>
- Catalog route accessible at /cards (content identical to current / route)
- Card detail "Back to catalog" link targets /cards
- getTopCardsByPrice Drizzle query exported and type-correct
- All Wave 0 test stubs in place; Vitest suite green
</success_criteria>

<output>
After completion, create `.planning/phases/11-new-home-page/11-01-SUMMARY.md`
</output>
