---
phase: 23-binder-variant-completeness
plan: 02
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - src/components/catalog/card-item.tsx
autonomous: false
requirements:
  - BINDER-07
tags:
  - card-item
  - variant-badge
  - public-binder
  - looking-for
  - ui

must_haves:
  truths:
    - "On the public binder Looking For section, each tile for a non-Normal manual-want printing displays a variant type badge in the top-left corner"
    - "Normal manual wants and all auto-wants display NO variant badge (matching the Available for Trade Normal tile behavior)"
    - "Looking For badge markup is byte-identical to the badge rendered on Available for Trade tiles (same classes, same content, same z-index, same position)"
    - "The `want` mode badge condition is gated on `variantType && variantType !== 'Normal'` (truthy check covers both null and the literal 'Normal')"
    - "No regression on Available for Trade tiles or catalog browse tiles — only the `want` mode branch is changed"
  artifacts:
    - path: "src/components/catalog/card-item.tsx"
      provides: "Variant badge render block extended to also fire in `want` mode"
      contains: "isWant"
      min_lines: 1
  key_links:
    - from: "src/components/catalog/card-item.tsx"
      to: "Looking For tile DOM"
      via: "absolute-positioned div inside CardItem when mode='want' and variantType !== 'Normal'"
      pattern: "(isBinder \\|\\| isWant)\\s*&&\\s*variantType\\s*&&\\s*variantType\\s*!==\\s*'Normal'"
    - from: "Plan 01 getPublicBinderData lookingFor entry variantType"
      to: "CardItem prop"
      via: "lookingFor[i].variantType flows through PublicBinderClient mapToFilterable into CardGrid item props"
      pattern: "variantType"
---

<objective>
Complete BINDER-07 by extending the existing variant badge in `CardItem` so it also renders when the component is in `want` mode. After Plan 01, the public binder's Looking For section now receives per-printing entries with `variantType`. This plan flips a single render condition so the badge actually shows on those tiles, mirroring the Available for Trade tiles exactly.

Purpose: One-line behavior change (plus careful verification). The badge markup, styling, copy, and position are already established by Phase 21 — this plan ensures consistency rather than reinventing badge UI.

Output:
- Single edit to `src/components/catalog/card-item.tsx` extending the variant badge gate from `isBinder` to `isBinder || isWant`
- Human-verify checkpoint confirming both Looking For tiles (manual wants with non-Normal variant) display the badge and Looking For tiles for auto-wants (Normal default) do NOT display a badge
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/23-binder-variant-completeness/23-CONTEXT.md
@.planning/phases/23-binder-variant-completeness/23-RESEARCH.md
@.planning/phases/23-binder-variant-completeness/23-PATTERNS.md
@.planning/phases/23-binder-variant-completeness/23-UI-SPEC.md
@AGENTS.md

<interfaces>
<!-- The exact existing badge block in CardItem (binder mode). The change is to extend the condition. -->

From src/components/catalog/card-item.tsx (lines ~111-115 — current state, binder mode only):
```typescript
{isBinder && variantType && variantType !== 'Normal' && (
  <div className="absolute top-1 left-1 bg-black/70 text-white text-[9px] px-1.5 py-0.5 rounded-sm font-bold shadow-md z-20 pointer-events-none uppercase">
    {variantType}
  </div>
)}
```

UI-SPEC contract for Looking For variant badge (Surface 1):
- Apply same badge as ManageTradeCard: `absolute top-1 left-1 bg-black/70 text-white text-[10px] px-2 py-0.5 rounded-sm font-bold shadow-md z-20 uppercase`
- Show when `variantType !== 'Normal'`
- Auto-wants get no badge (treated as Normal)
- One tile per printing — Normal + Foil → two tiles intentionally
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend CardItem variant badge to also render in `want` mode</name>
  <files>src/components/catalog/card-item.tsx</files>
  <read_first>
    - src/components/catalog/card-item.tsx (full file — locate the existing variant badge block around lines 111-115; confirm the `isBinder` and `isWant` mode booleans are derived from the `mode` prop; understand prop flow for `variantType`)
    - src/components/binder/manage-trade-card.tsx (analog — the badge classNames/markup pattern that CardItem already mirrors; confirm UI-SPEC parity if there is any drift)
    - .planning/phases/23-binder-variant-completeness/23-UI-SPEC.md (Surface 1 — Looking For variant badges section, lines 93-101)
    - .planning/phases/23-binder-variant-completeness/23-PATTERNS.md (Variant badge markup section, lines 849-860)
  </read_first>
  <action>
Locate the existing variant badge render block in `CardItem` (currently gated on `isBinder && variantType && variantType !== 'Normal'`). Extend the condition so the SAME badge also renders when the component is in `want` mode.

Concrete change: replace the gate `isBinder && variantType && variantType !== 'Normal'` with `(isBinder || isWant) && variantType && variantType !== 'Normal'`. Do NOT duplicate the JSX block — extend the boolean only. The badge classes, content (`{variantType}`), and DOM structure must remain byte-identical.

If the `isWant` boolean is not already derived from the `mode` prop (for example, only `isBinder` is destructured), add a local boolean `const isWant = mode === 'want';` adjacent to the existing `isBinder` derivation. The `mode` prop already accepts `'want'` as documented in 23-RESEARCH.md and the existing `want` mode is the binder Looking For grid render path.

The `variantType` prop on CardItem already exists in the type signature (used by the binder branch). It is passed through from `PublicBinderClient` → `CardGrid` → `CardItem` for the Looking For section after Plan 01 reshaped `getPublicBinderData`. No prop type changes needed in this file.

Do NOT touch:
- Any other branch of CardItem (catalog browse mode, binder mode quantity badge, etc.)
- The CSS classes of the badge — they match UI-SPEC and ManageTradeCard exactly
- The quantity badge (top-right) — that is a separate block
- Any other component file

The check that auto-wants do not render a badge is enforced by data shape, not by this code: Plan 01 sets auto-wants `variantType: 'Normal'`, so the `variantType !== 'Normal'` clause skips them. Manual wants for Normal printings also correctly suppress the badge (consistent with UI-SPEC Surface 1).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/catalog/card-item.tsx` contains the substring `(isBinder || isWant)` within the variant badge gate condition
    - File contains `variantType !== 'Normal'` adjacent to that gate (verify the truthy + inequality combination remains intact)
    - File contains exactly ONE block rendering `<div className="absolute top-1 left-1 ...">{variantType}</div>` (the gate is extended, not duplicated)
    - File contains `const isWant = mode === 'want'` OR the `isWant` boolean is otherwise visible in scope adjacent to `isBinder`
    - `npm run build` exits 0
    - `grep -c "absolute top-1 left-1" src/components/catalog/card-item.tsx` (filtering out `^#` lines) is 1 — no duplicate badge markup
  </acceptance_criteria>
  <done>The variant badge gate now reads `(isBinder || isWant) && variantType && variantType !== 'Normal'`; markup unchanged; build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification on public binder Looking For section</name>
  <files>(human-only — no file changes; verification of Task 1 output)</files>
  <action>Run the verification steps below. This task has no Claude-side action; the human operator follows the protocol in <how-to-verify> and types the resume signal.</action>
  <verify><human-check>See <how-to-verify> below — checkpoint passes when the human operator types "approved".</human-check></verify>
  <done>Human operator has confirmed Looking For tiles render variant badges per UI-SPEC Surface 1 and types "approved".</done>
  <what-built>
    BINDER-07 is now end-to-end functional:
    - Manual wants are per-printing (Plan 01)
    - `getPublicBinderData` returns Looking For entries with `variantType` per printing (Plan 01)
    - `CardItem` in `want` mode renders a variant badge top-left when `variantType !== 'Normal'` (this plan)
  </what-built>
  <how-to-verify>
    Prerequisites:
    1. Plan 01 migration has been applied (DB has `trade_manual_wants.card_printing_id`)
    2. Plan 04 has NOT shipped yet — manual wants UX in Manage Binder is still the legacy flow, but you can manually insert per-printing rows via SQL or via the legacy POST after temporarily toggling the request body to send `cardPrintingId`
    3. Dev server running (`npm run dev`)

    Steps:
    1. Pick a test account with a public binder URL. If you do not have one with a non-Normal manual want, run the following SQL on the dev DB to insert two manual wants for the same card with different variants:
       ```sql
       -- Find a card with Normal + Foil printings:
       SELECT cp.id, cp.variant_type, cd.name
       FROM card_printings cp
       JOIN card_definitions cd ON cd.id = cp.card_definition_id
       WHERE cd.id IN (SELECT card_definition_id FROM card_printings GROUP BY card_definition_id HAVING COUNT(*) > 1)
       LIMIT 10;
       -- Insert two manual wants:
       INSERT INTO trade_manual_wants (user_id, card_printing_id, quantity) VALUES (<your_user_id>, <normal_printing_id>, 1);
       INSERT INTO trade_manual_wants (user_id, card_printing_id, quantity) VALUES (<your_user_id>, <foil_printing_id>, 1);
       ```
    2. Navigate to `/binder/<your_username>` (public binder URL — log out first or use an incognito window)
    3. Scroll to the Looking For section
    4. Inspect the two tiles for the same card:
       - Normal printing tile: NO variant badge in the top-left corner — passes
       - Foil printing tile: variant badge "FOIL" in the top-left, black/70 background, white text, uppercase — passes
       - Both tiles present (one per printing) — passes D-05
    5. Check an auto-want tile (a card you have a deck shortfall for but no manual want): NO variant badge — passes D-03

    Expected DOM (inspect element on the Foil tile):
    ```html
    <div class="absolute top-1 left-1 bg-black/70 text-white text-[9px] px-1.5 py-0.5 rounded-sm font-bold shadow-md z-20 pointer-events-none uppercase">
      Foil
    </div>
    ```

    If a Looking For tile shows NO badge when it should, the data layer is fine but `isWant` is not derived correctly — re-check Task 1 edit.
    If a Looking For tile shows TWO badges, the gate was duplicated — Task 1 was implemented incorrectly.
    If the badge classes differ from the spec, UI-SPEC drift — fix className.
  </how-to-verify>
  <resume-signal>Type "approved" if Looking For tiles show variant badges correctly per the steps above, or describe any visual issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC → CardItem props | Server-rendered `lookingFor` entries flow into client `CardItem` rendering; values are pre-validated by Plan 01 query layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-02-01 | Tampering | CardItem `variantType` prop | accept | Public binder data is intentionally public; variantType is a controlled enum (`'Normal' | 'Foil' | 'Showcase' | 'Hyperspace' | 'Hyperspace Foil'`) sourced from `card_printings.variant_type`. Rendering it inside a `<div>` with className is XSS-safe via React text node escaping. |
| T-23-02-02 | Information Disclosure | Looking For badge | accept | Per CONTEXT.md, Looking For with variant badges is the intended public exposure. No new disclosure surface. |
| T-23-02-SC | Tampering | npm install | n/a | No new packages. Skip slopcheck. |
</threat_model>

<verification>
- `npm run build` exits 0
- Manual smoke (checkpoint): public binder Looking For section renders variant badges on non-Normal manual-want tiles and no badge on Normal/auto-want tiles
</verification>

<success_criteria>
- BINDER-07 is fully functional end-to-end: data layer (Plan 01) + UI badge (this plan) wired correctly
- Visual verification passed: badge appears on non-Normal Looking For tiles, no badge on Normal/auto-want tiles
- No regression on Available for Trade tiles (which already had the badge in `binder` mode)
</success_criteria>

<output>
Create `.planning/phases/23-binder-variant-completeness/23-02-SUMMARY.md` when done. Record the single line change applied and confirm visual checkpoint passed.
</output>
