---
phase: 23
slug: binder-variant-completeness
status: approved
shadcn_initialized: true
preset: base-nova / zinc / cssVariables
created: 2026-05-25
reviewed_at: 2026-05-25
---

# Phase 23 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn |
| Preset | base-nova, baseColor: zinc, cssVariables: true |
| Component library | @base-ui/react ^1.4.1 (via shadcn Sheet, Dialog primitives) |
| Icon library | lucide-react ^1.14.0 |
| Font | --font-sans (Geist Sans), --font-heading (Oxanium), --font-mono (Geist Mono) |

Source: `components.json` + `src/app/globals.css`

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, variant badge inner padding (`px-1`), inline badge padding |
| sm | 8px | Row internal gaps (+/- control to count input), badge-to-edge margin, chip container gap (`gap-2`), badge inner horizontal padding (`px-2`) |
| md | 16px | Section container padding, card tile gap in grid, Sheet header padding |
| lg | 24px | Between distinct UI sections (e.g. trade section below collection section) |
| xl | 32px | Page column gap (image column ↔ metadata column on md+) |
| 2xl | 48px | Major section breaks on Manage Binder page |
| 3xl | 64px | Page-level vertical padding (py-16 on full-bleed containers) |

Exceptions:
- Touch targets for +/- icon buttons: minimum 40px (existing `size="icon"` Button = 40px — maintain, do not reduce)
- Variant badge top-left on card tile: `top-1 left-1` (4px) matching `ManageTradeCard` existing pattern
- Quantity badge top-right on card tile: `-top-2 -right-2` (−8px offset) matching `ManageTradeCard` existing pattern
- Sheet content bottom padding on mobile: 32px to clear the browser chrome

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px (text-sm) | 400 (normal) | 1.5 |
| Label | 12px (text-xs) | 700 (bold) | 1.4 — used for section headings, badges, variant badge text |
| Heading | 20px (text-xl) | 700 (bold), font-heading | 1.25 — section heading inside Sheet, page h2 |
| Micro | 10px (text-[10px]) | 700 (bold) | 1.2 — card name under tile, quantity badge, "Stop Trading" inline action, owned count chip |

Two weights only: 400 (normal) and 700 (bold). No intermediate weights (500 medium or 600 semibold) are used in this phase.

Source: inferred from `VariantCollectionSection`, `ManageTradeCard`, `ManageWantsList` patterns in codebase.

Note: Font heading (Oxanium) used only for card detail h1 (existing) and Sheet panel card name heading. All other text uses --font-sans.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `--background` oklch(1 0 0) light / oklch(0.141 …) dark | Page background, Sheet backdrop |
| Secondary (30%) | `--muted` oklch(0.967 …) light / oklch(0.274 …) dark | Section containers (`bg-muted/50`), row hover states, chip default state |
| Accent (10%) | `--primary` oklch(0.5 0.134 242.749) light / oklch(0.443 …) dark | See reserved list below |
| Destructive | `--destructive` oklch(0.577 0.245 27.325) light / oklch(0.704 …) dark | "Stop Trading" hover state on card tile overlay; remove/exclude icon hover |

Accent (`--primary`) reserved for:
1. Quantity badge background on card tiles (`bg-primary text-primary-foreground`)
2. Selected variant chip background in manual wants chip selector
3. "Owned" status label in VariantCollectionSection rows (`text-primary`)
4. Trade quantity row: "Trading" status indicator when `tradeQuantity > 0` (mirrors "Owned" pattern)

Never apply accent to: borders, hover backgrounds, section labels, or neutral row backgrounds.

Source: `src/app/globals.css` CSS variables; `manage-trade-card.tsx` badge pattern; `variant-collection-section.tsx` owned status.

---

## Surface & Component Contracts

### Surface 1 — Looking For variant badges (BINDER-07)

The public binder's Looking For `CardGrid` renders tiles via the existing binder card grid pattern. No changes to the grid component itself; the data shape changes to include `variantType` per printing.

**Variant badge on Looking For tiles:**
- Apply the same badge as `ManageTradeCard`: `absolute top-1 left-1 bg-black/70 text-white text-[10px] px-2 py-0.5 rounded-sm font-bold shadow-md z-20 uppercase`
- Show badge when `variantType !== 'Normal'`. Normal tiles receive no badge (matches Available for Trade behavior).
- Auto-want tiles: show no variant badge (they represent "any version") — treat as Normal display.
- One tile per printing. Two tiles for the same card name is intentional and correct.

### Surface 2 — Card Detail "Available for Trade" section (BINDER-08)

Container: matches `VariantCollectionSection` exactly — `flex flex-col gap-2 p-4 bg-muted/50 rounded-lg border border-border`

Section label: `text-xs font-bold uppercase tracking-wider text-muted-foreground` — copy: **"Available for Trade"**

Per-printing row anatomy (one row per printing, same as collection rows):
- Variant type label: `text-sm text-muted-foreground w-28 min-w-[7rem]`
- Minus Button: `variant="outline" size="icon"`, disabled when `tradeQuantity === 0`, `aria-label="Decrease {variantType} trade quantity"`
- Count Input: `w-16 text-center font-bold type="number"`, aria-label `"{variantType} trade quantity"`
- Plus Button: `variant="outline" size="icon"`, `aria-label="Increase {variantType} trade quantity"`
- Status indicator: when `tradeQuantity > 0` show `text-sm font-bold text-primary` "Trading"; when 0 show `text-sm font-normal text-muted-foreground` "Not trading"

Placement: directly below `VariantCollectionSection` in the image column, gap-6 between the two sections.

Auth gate: entire section hidden when `!userId`. No sign-in prompt inline (card detail already has other auth-gated UI).

Zero quantity rows: rows with `tradeQuantity === 0` remain visible — the user can set a trade quantity from zero. Do not hide zero rows.

Optimistic update: fire-and-update pattern matching `VariantCollectionSection` — update local state immediately, roll back on API error.

### Surface 3 — Manage Binder owned-card browse grid (BINDER-09)

**Browse grid (replaces full-catalog search):**
- Grid: `grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-4` — same as current Trade Offerings grid
- Card tiles: reuse `ManageTradeCard` component. Art uses highest-owned-variant precedence (Showcase > Hyperspace Foil > Hyperspace > Foil > Normal).
- Search input: same as current, `pl-9` with Search icon, placeholder **"Search your collection..."**
- Clicking a card tile (not the +/- overlay) opens the per-printing Sheet panel.
- The +/- hover overlay on each tile remains functional for quick single-printing cards.

**Side panel — per-printing trade controls (Sheet from shadcn/ui):**
- Use existing `Sheet` component (`src/components/ui/sheet.tsx`) backed by `@base-ui/react/dialog`.
- Sheet opens from the right (`side="right"`), width `w-full sm:max-w-sm`.
- Sheet header: card name in `text-xl font-bold font-heading`, subtitle in `text-sm text-muted-foreground italic`.
- Sheet content: list of owned printings. One row per printing.
- Per-printing row in Sheet:
  - Variant label: `text-sm font-normal` — e.g. "Normal", "Foil", "Showcase"
  - Owned count chip: `text-[10px] bg-muted px-2 py-0.5 rounded-full font-normal` — e.g. "Owned: 3"
  - Current trade quantity with +/- controls: same pill pattern as `ManageWantsList` — `bg-background rounded-full px-2 py-0.5 border shadow-sm`
  - Minus Button: `variant="outline" size="icon"`, `aria-label="Decrease {variantType} trade quantity"`
  - Plus Button: `variant="outline" size="icon"`, `aria-label="Increase {variantType} trade quantity"`
  - Show all printings the user owns (count > 0). Do not show unowned printings.
- Sheet footer: no explicit footer needed; Sheet close via the built-in X button in sheet header.

**Manual wants variant chip selector (BINDER-07 add-flow, D-07):**
The existing search box in the "Add Cards" flow (now searching owned cards) is for trade offerings. The manual wants add-flow is separate. After searching by card name and selecting a card, a variant chip selector appears.

Chip selector anatomy:
- Container: `flex flex-wrap gap-2 mt-2`
- Each chip: `px-2 py-1 rounded-full text-xs font-bold border transition-colors`
- Unselected chip: `bg-background border-input text-foreground hover:bg-muted`
- Selected chip: `bg-primary border-primary text-primary-foreground`
- Available chips derived from the card's printings (Normal, Foil, etc.) — not all variants, only those that exist for this card.
- Single selection only (one variant per want row). Clicking a selected chip deselects it (clears selection).
- After chip selection, a confirm button appears: `size="sm" variant="secondary"` — copy: **"Add Want"**

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Card Detail trade section label | "AVAILABLE FOR TRADE" (uppercase via className, matches existing section label pattern) |
| Trade status — active | "Trading" (mirrors "Owned" in collection section) |
| Trade status — zero | "Not trading" (mirrors "Not owned" in collection section) |
| Manage Binder browse search placeholder | "Search your collection..." |
| Manage Binder browse empty state heading | "No cards found" |
| Manage Binder browse empty state body | "Try a different search term, or add cards to your collection first." |
| Manage Binder browse zero-owned empty state | "Your collection is empty. Add cards to your collection to offer them for trade." |
| Sheet panel no printings fallback | "No owned printings to display." |
| Manual wants chip selector label | "Select variant" (text-xs text-muted-foreground above chips) |
| Manual wants add button | "Add Want" |
| Card tile overlay "stop trading" action | "Stop Trading" (existing — do not change) |
| Looking For section heading (public binder) | "Looking For" (existing — do not change) |
| Manual Wants section heading (Manage Binder) | "Manual Wants" (existing — do not change) |
| Error state — trade quantity update failed | "Failed to update — please try again." (inline, `text-xs text-destructive`, appears below the row for 3 seconds then clears) |
| Error state — Sheet data load failed | "Could not load printings. Please close and reopen." |

Destructive actions in this phase:

| Action | Confirmation approach |
|--------|----------------------|
| Setting trade quantity to 0 via Sheet | No confirmation — quantity 0 simply means "not trading"; row stays visible. No modal. |
| "Stop Trading" on card tile hover overlay | No confirmation — immediate optimistic update (existing pattern). |
| Removing a manual want | No confirmation — X button removes immediately (existing pattern). |

No hard-delete destructive actions in this phase require a confirmation modal.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | Sheet (already installed — `src/components/ui/sheet.tsx` exists), Button, Input, Card, Badge, Tabs, Label, Switch, Tooltip, DropdownMenu | not required |

No third-party registries declared for this phase. The Sheet component is already installed and backed by `@base-ui/react` — no new registry additions needed.

Source: `src/components/ui/` glob confirms Sheet present; `components.json` registries field is `{}`.

---

## Interaction Contract Summary

| Interaction | Behavior |
|-------------|----------|
| +/- buttons on trade rows (Card Detail section) | Optimistic update; floor 0; rollback on error |
| +/- buttons on Sheet trade rows | Optimistic update; floor 0; rollback on error |
| Card tile click (owned-card browse grid) | Opens Sheet for that card — does not trigger trade quantity change |
| Card tile hover overlay (owned-card browse grid) | Shows existing +/- overlay and "Stop Trading" — for single-printing quick update without opening Sheet |
| Variant chip select (manual wants) | Single-select toggle; confirms "Add Want" button appears |
| Sheet close | Via built-in X in SheetHeader — no unsaved-changes guard (all mutations are already persisted optimistically) |
| Quantity input direct edit | Accept numeric input; floor 0; trigger same update path as +/- buttons |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
