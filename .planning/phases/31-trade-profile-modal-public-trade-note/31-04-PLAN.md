---
phase: 31-trade-profile-modal-public-trade-note
plan: 04
type: execute
wave: 2
depends_on: [31-01]
files_modified:
  - src/db/queries/binder.ts
  - src/db/queries/binder.test.ts
  - src/components/binder/public-binder-client.tsx
  - src/components/binder/public-binder-client.test.tsx
  - src/app/binder/[username]/page.tsx
autonomous: true
requirements: [BINDER-17]
must_haves:
  truths:
    - "`getUserIdByUsername` returns `{ id, tradeNote }` (or null) — the public read carries the note (BINDER-17 data path)."
    - "Visiting `/binder/[username]` shows the trade note as a callout under the username header when one is set (BINDER-17, D-06)."
    - "When the note is null or empty, the public page renders NO callout DOM node — no box, no empty line, no reserved space (D-07)."
  artifacts:
    - "src/db/queries/binder.ts — `getUserIdByUsername` returns `{ id: number; tradeNote: string | null } | null`"
    - "src/db/queries/binder.test.ts — query-shape test"
    - "src/components/binder/public-binder-client.tsx — `tradeNote` prop + conditional callout"
    - "src/components/binder/public-binder-client.test.tsx — callout render/absent + XSS-safe tests"
    - "src/app/binder/[username]/page.tsx — passes `tradeNote` from the query to the client"
  key_links:
    - "The RSC page reads `tradeNote` via the raw-Drizzle `getUserIdByUsername` (no Better Auth session API on the public path) and passes it as a prop — no client fetch (RESEARCH Pattern 4)."
    - "The callout guard wraps the WHOLE element (`{tradeNote && <div>...}`) so an empty note reserves zero layout (D-07)."
  prohibitions:
    - "MUST NOT render the note via `dangerouslySetInnerHTML` — plain JSX text interpolation only (T-31-01, XSS)."
    - "MUST NOT reserve space / render an empty box when the note is falsy — use a conditional wrapper, not `hidden`/`opacity-0` (D-07)."
    - "MUST NOT call the Better Auth session API on the public read path (it is anonymous; RESEARCH Pattern 4)."
    - "MUST NOT introduce a sanitization library — React auto-escaping is sufficient (RESEARCH Don't Hand-Roll)."
---

<objective>
Surface the trade note on the public binder. Extend `getUserIdByUsername` to also select `user.tradeNote` (changing its return from `number | null` to `{ id, tradeNote } | null`, RESEARCH Pattern 4), pass that value from the `/binder/[username]` RSC into `PublicBinderClient`, and render it as a callout directly under the username header — but only when set. When the note is null/empty, render nothing at all (D-07, satisfying success criterion #3).

Purpose: Delivers BINDER-17. Depends on Plan 01 only (the `trade_note` column must exist in `schema.ts` and the live DB before this query can select it). Runs in Wave 2, parallel with Plan 03 (no file overlap).

Output: extended query + test, callout in the client + test, and the RSC wiring.
</objective>

<execution_context>
@/Users/alan/Documents/Repos/Personal/star-wars-unlimited-tracker/.claude/gsd-core/workflows/execute-plan.md
@/Users/alan/Documents/Repos/Personal/star-wars-unlimited-tracker/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/31-trade-profile-modal-public-trade-note/31-CONTEXT.md
@.planning/phases/31-trade-profile-modal-public-trade-note/31-RESEARCH.md
@.planning/phases/31-trade-profile-modal-public-trade-note/31-UI-SPEC.md

@src/db/queries/binder.ts
@src/app/binder/[username]/page.tsx
@src/components/binder/public-binder-client.tsx
@AGENTS.md
</context>

<artifacts>
Symbols this plan produces:
- `getUserIdByUsername(username): Promise<{ id: number; tradeNote: string | null } | null>` (changed return shape).
- `PublicBinderClient` gains a `tradeNote?: string | null` prop and a conditional callout block.
- `[username]/page.tsx` passes `tradeNote` to the client.
</artifacts>

<!-- planner-discipline-allow: dangerouslySetInnerHTML -->

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend getUserIdByUsername to return tradeNote</name>
  <files>src/db/queries/binder.ts, src/db/queries/binder.test.ts</files>
  <read_first>
    - src/db/queries/binder.ts (`getUserIdByUsername`, lines ~6-13)
    - src/db/queries/collection.test.ts (the narrow `vi.mock('@/db', ...)` pattern to mirror)
    - 31-RESEARCH.md → Pattern 4 and the Validation Architecture note about keeping the DB mock narrow (21-REVIEW.md fragility caution)
  </read_first>
  <behavior>
    - `getUserIdByUsername` selects `{ id: user.id, tradeNote: user.tradeNote }` and returns the row object `{ id, tradeNote }` when a user matches.
    - Returns `null` when no user matches (preserving the existing null contract).
    - The username is still lowercased in the `where` clause.
  </behavior>
  <action>
    Change `getUserIdByUsername` to select both `id` and `tradeNote` and return `u ?? null` (the row object), so the return type becomes `{ id: number; tradeNote: string | null } | null` (RESEARCH Pattern 4). This changes its one call site (`[username]/page.tsx`, handled in Task 3). Leave `getPublicBinderData` untouched.

    Write `binder.test.ts` first, mocking `@/db` narrowly (mirror `collection.test.ts`): stub the `db.select(...).from(...).where(...).limit(...)` chain to resolve a single-row array, assert the returned `{ id, tradeNote }`; and stub an empty-array resolution, assert `null`. Do not attempt to mock the whole `getPublicBinderData` query chain (fragile per 21-REVIEW.md) — test only `getUserIdByUsername`.
  </action>
  <verify>
    <automated>npx vitest run src/db/queries/binder.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `getUserIdByUsername` returns `{ id, tradeNote }` for a match, `null` otherwise.
    - Test mocks only this query's chain; `tsc --noEmit` clean.
  </acceptance_criteria>
  <done>The public read carries the trade note alongside the user id.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the conditional trade-note callout to PublicBinderClient</name>
  <files>src/components/binder/public-binder-client.tsx, src/components/binder/public-binder-client.test.tsx</files>
  <read_first>
    - src/components/binder/public-binder-client.tsx (props interface ~L17-22; username header block ~L123-126)
    - 31-UI-SPEC.md → Layout Contract Surface 3 and Interaction Contract (XSS-safe rendering)
    - 31-RESEARCH.md → "Public callout" Code Example and Anti-Patterns (render nothing when empty; never `dangerouslySetInnerHTML`)
  </read_first>
  <behavior>
    - Given a non-empty `tradeNote` prop, the rendered output contains the note text inside a callout element beneath the username header.
    - Given `tradeNote={null}` (and `tradeNote={''}`), NO callout element is rendered — querying for the callout returns nothing and no empty wrapper exists.
    - The note is rendered as text content (asserting a value containing markup characters appears as literal text, not parsed HTML).
  </behavior>
  <action>
    Add `tradeNote?: string | null` to `PublicBinderClientProps` and the destructured params. Directly beneath the username header `<div>` (the `bg-muted/30 border-b` block ending ~L126) and above the `flex flex-1` results row, insert a conditional callout guarded on the whole element: `{tradeNote && (<div className="px-4 lg:px-8 py-2 bg-muted/20 border-b border-border text-sm text-muted-foreground">...</div>)}` (UI-SPEC Surface 3, D-06). Inside, precede the text with a `MessageSquareText` lucide icon (`size-3.5 inline-block mr-1.5 align-text-bottom`) and interpolate the note as PLAIN JSX text `{tradeNote}` — never `dangerouslySetInnerHTML` (T-31-01; React auto-escapes). When `tradeNote` is null/empty the guard renders nothing — no wrapper, no reserved space (D-07). Import `MessageSquareText` from `lucide-react`.

    Write `public-binder-client.test.tsx` first (`@vitest-environment jsdom`), covering all `<behavior>` bullets. Stub the child catalog components (`CardGrid`, `SidebarFilters`, etc.) or provide minimal props so the component renders; focus assertions on the callout.
  </action>
  <verify>
    <automated>npx vitest run src/components/binder/public-binder-client.test.tsx</automated>
    <automated>test $(grep -v '^\s*//' src/components/binder/public-binder-client.tsx | grep -c 'dangerouslySetInnerHTML') -eq 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Callout with note text renders when `tradeNote` is set; nothing renders when null/empty.
    - Note is interpolated as text; no `dangerouslySetInnerHTML` anywhere in the file.
  </acceptance_criteria>
  <done>The public binder shows the note as a safe callout, or nothing when empty (BINDER-17, D-06/D-07).</done>
</task>

<task type="auto">
  <name>Task 3: Wire tradeNote through the public RSC page</name>
  <files>src/app/binder/[username]/page.tsx</files>
  <read_first>
    - src/app/binder/[username]/page.tsx (current `getUserIdByUsername` usage ~L14-22 and the `PublicBinderClient` render ~L57-64)
    - 31-RESEARCH.md → Pattern 4 (and the Project Constraints note: the existing async-`params` pattern is already correct for this Next.js version — no change needed)
    - AGENTS.md — read node_modules/next/dist/docs/ before changing any Next.js data-fetching/RSC API (this task only adjusts local variable handling + a prop, not the params contract)
  </read_first>
  <action>
    `getUserIdByUsername` now returns an object. Rename the local from `userId` to `profile`; guard `if (!profile) notFound();`. Pass `profile.id` into `getPublicBinderData(...)` (replacing the old numeric `userId`). Add `tradeNote={profile.tradeNote}` to the `<PublicBinderClient .../>` props. Do not change the `params: Promise<...>` / `await params` handling — RESEARCH confirms it is already the current, correct convention for this installed Next.js version.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>grep -q 'tradeNote={profile.tradeNote}' src/app/binder/[username]/page.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `getPublicBinderData` receives `profile.id`; `notFound()` still fires for unknown usernames.
    - `PublicBinderClient` receives `tradeNote`; `tsc --noEmit` clean.
  </acceptance_criteria>
  <done>The RSC passes the server-read note straight to the client — no client-side fetch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated author → anonymous visitor | Any authenticated user can set a `tradeNote`; anonymous visitors render it on the public `/binder/[username]` page |
| Neon DB → RSC (SSR) | `getUserIdByUsername` reads the note directly via Drizzle for server rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-01 | Tampering / Elevation of Privilege | public trade-note rendering in `PublicBinderClient` | high | mitigate | Render via plain JSX text interpolation (`{tradeNote}`) — React auto-escapes. Never `dangerouslySetInnerHTML`; no HTML is parsed from the note, so stored-XSS against anonymous visitors is prevented (RESEARCH Anti-Patterns + Security Domain). Verified by a file-scoped negative grep for `dangerouslySetInnerHTML`. |
| T-31-06 | Information Disclosure | public read path | low | accept | The note is intentionally public (D-06). Only `id` + `tradeNote` are selected — no session, email, or other user fields leak through `getUserIdByUsername`. |
| T-31-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages installed. |
</threat_model>

<verification>
- `npx vitest run src/db/queries/binder.test.ts src/components/binder/public-binder-client.test.tsx` green.
- `npx tsc --noEmit` clean.
- File-scoped grep confirms no `dangerouslySetInnerHTML` in `public-binder-client.tsx`.
- Manual: set a note in the modal (Plan 03), visit `/binder/[username]` → callout shows under the header; clear the note, save, reload → callout is gone with no reserved gap.
</verification>

<success_criteria>
BINDER-17 met: the public binder displays the trade note under the header when set, renders nothing (no broken UI, no reserved space) when empty, and renders the note XSS-safely as plain text.
</success_criteria>

<output>
Create `.planning/phases/31-trade-profile-modal-public-trade-note/31-04-SUMMARY.md` when done.
</output>
