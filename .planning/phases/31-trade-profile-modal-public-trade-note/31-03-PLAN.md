---
phase: 31-trade-profile-modal-public-trade-note
plan: 03
type: execute
wave: 2
depends_on: [31-01, 31-02]
files_modified:
  - src/components/binder/profile-modal.tsx
  - src/components/binder/profile-modal.test.tsx
  - src/app/binder/manage/page.tsx
autonomous: true
requirements: [BINDER-15, BINDER-16]
must_haves:
  truths:
    - "The Manage Binder page no longer shows the inline username/binder-URL Card; a header 'Trade Profile' button opens a modal containing that content (BINDER-15)."
    - "The modal contains a username field, the public-URL helper text, and a trade-note textarea (≤140 chars, live counter); a single Save persists username + note together and closes on success (BINDER-16, D-08/D-09)."
    - "Saving an empty textarea sends `tradeNote: ''` (clears the note); Save is disabled when neither field changed (D-05)."
  artifacts:
    - "src/components/binder/profile-modal.tsx — `ProfileModal`"
    - "src/components/binder/profile-modal.test.tsx — behavior tests"
    - "src/app/binder/manage/page.tsx — header 'Trade Profile' button + mounted `ProfileModal`, inline Card removed"
  key_links:
    - "The modal's Save calls `authClient.updateUser({ username, displayUsername, tradeNote })` in ONE request (D-09) — the same write path the old inline Update button used, extended with `tradeNote`."
    - "Modal fields re-seed from `session.user` each time it opens, not once on mount (UI-SPEC Interaction Contract)."
  prohibitions:
    - "MUST NOT leave the inline 'Trade Profile' Card (or any placeholder/empty column) on the manage page (D-01)."
    - "MUST NOT add a second write path or `/api` route for the note (D-10) — reuse `authClient.updateUser`."
    - "MUST NOT reserve layout space when the modal is closed; it mounts via Portal only when open."
    - "MUST NOT remove the 'View Public Binder' header link (D-03) — the profile button is additive."
---

<objective>
Compose the profile modal and wire it into the Manage Binder page. Build `ProfileModal` from the Plan 02 `Dialog`/`Textarea` primitives — username `Input` (relocated from the deleted inline Card), the public-URL helper text, and the trade-note `Textarea` with a 140-char cap and live counter — with one Save button that persists both fields via `authClient.updateUser` (D-08/D-09). On the manage page, delete the inline "Trade Profile" Card entirely (D-01) and add a header "Trade Profile" button (before the existing "View Public Binder" link, D-03) that opens the modal.

Purpose: Delivers BINDER-15 (profile behind a modal) and the write half of BINDER-16 (setting the note). Depends on Plan 01 (typed `updateUser({ tradeNote })` + live column) and Plan 02 (Dialog/Textarea).

Output: `profile-modal.tsx` + its test, and the reworked manage page header.
</objective>

<execution_context>
@/Users/alan/Documents/Repos/Personal/star-wars-unlimited-tracker/.claude/gsd-core/workflows/execute-plan.md
@/Users/alan/Documents/Repos/Personal/star-wars-unlimited-tracker/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/31-trade-profile-modal-public-trade-note/31-CONTEXT.md
@.planning/phases/31-trade-profile-modal-public-trade-note/31-RESEARCH.md
@.planning/phases/31-trade-profile-modal-public-trade-note/31-UI-SPEC.md

@src/app/binder/manage/page.tsx
@src/lib/auth-client.ts
@src/components/ui/sheet.tsx
@AGENTS.md
</context>

<artifacts>
Symbols this plan produces:
- `ProfileModal` component — props `{ open: boolean; onOpenChange: (open: boolean) => void; currentUsername: string; currentTradeNote: string }` (src/components/binder/profile-modal.tsx).
- Manage page gains `profileOpen` state and a header "Trade Profile" button (`UserCog` icon).
</artifacts>

<!-- planner-discipline-allow: handleUpdateUsername -->

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the ProfileModal with tests</name>
  <files>src/components/binder/profile-modal.tsx, src/components/binder/profile-modal.test.tsx</files>
  <read_first>
    - src/app/binder/manage/page.tsx (the existing `handleUpdateUsername` + inline Card markup being relocated, lines ~184-191 and ~372-398)
    - src/components/ui/dialog.tsx and src/components/ui/textarea.tsx (Plan 02 outputs)
    - 31-UI-SPEC.md → Copywriting Contract, Layout Contract Surface 2, Interaction Contract
    - 31-RESEARCH.md → "Full modal save handler" and "Textarea with character counter" Code Examples
    - src/components/home/hero-section.test.tsx (existing component-test + `@vitest-environment jsdom` pattern to mirror)
  </read_first>
  <behavior>
    - Renders a username `Input` seeded from `currentUsername`, the public-URL helper paragraph, and a trade-note `Textarea` seeded from `currentTradeNote`.
    - Typing into the textarea clamps to 140 characters; the counter reads `{length}/140` and updates per keystroke.
    - Clicking Save calls a mocked `authClient.updateUser` with `{ username: <lowercased+trimmed>, displayUsername: <trimmed>, tradeNote: <trimmed> }`.
    - Clearing the textarea and saving sends `tradeNote: ''`.
    - Save is disabled when both fields equal their seeded values, and while a save is in flight.
    - When the mocked `updateUser` resolves with an `{ error }`, the modal stays open and the inline error copy is shown; on success (`!error`) `onOpenChange(false)` is called.
  </behavior>
  <action>
    Create `ProfileModal` (`'use client'`). Compose `Dialog`/`DialogContent`/`DialogTitle`/`DialogDescription`/`DialogFooter` (Plan 02). Content order (UI-SPEC Surface 2): title "Trade Profile" → description "Set your public username and trade note for your binder." → username `Input` (placeholder "Choose a username...") + public-URL helper `<p>` ("Your binder will be at: swu-tracker.com/binder/{username}") → trade-note `Textarea` (rows=3, `maxLength={140}`, `onChange` clamps via `.slice(0,140)`, placeholder "e.g. EU only, will ship") + right-aligned counter `text-xs text-muted-foreground` → inline error text (`text-sm text-destructive`, only when a save failed) → `DialogFooter` with one right-aligned "Save Profile" button.

    Local state: `username`, `tradeNote`, `isSaving`, `error`. Re-seed `username`/`tradeNote` from `currentUsername`/`currentTradeNote` via a `useEffect` keyed on `open` (re-seed each open, UI-SPEC). Save handler (D-09): set `isSaving`, call `authClient.updateUser({ username: username.toLowerCase().trim(), displayUsername: username.trim(), tradeNote: tradeNote.trim() })`; on `!error` call `onOpenChange(false)`; on `error` keep open and set the inline error message. Disable Save when `isSaving` OR (`username === currentUsername && tradeNote === currentTradeNote`). Show a `Loader2` spinner on the button while saving. Field label "Trade Note" carries a muted `(optional)` suffix. Do NOT add any `/api` route — the single `updateUser` call is the entire write path (D-10).

    Write `profile-modal.test.tsx` first (`@vitest-environment jsdom`), mocking `@/lib/auth-client` so `authClient.updateUser` is a spy, covering every bullet in `<behavior>`.
  </action>
  <verify>
    <automated>npx vitest run src/components/binder/profile-modal.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All `<behavior>` cases pass.
    - Save sends all three fields in one `updateUser` call; empty note sends `''`.
    - Failure keeps the modal open with error copy; success closes it.
  </acceptance_criteria>
  <done>ProfileModal implements the locked D-04/D-05/D-08/D-09 behavior with green tests.</done>
</task>

<task type="auto">
  <name>Task 2: Rework the Manage Binder header and mount the modal</name>
  <files>src/app/binder/manage/page.tsx</files>
  <read_first>
    - src/app/binder/manage/page.tsx (header row ~L356-368; inline Trade Profile Card ~L372-398; `username` state/effect ~L79, L100-104; `handleUpdateUsername` ~L184-191)
    - 31-CONTEXT.md → D-01, D-02, D-03
    - 31-UI-SPEC.md → Layout Contract Surface 1
    - AGENTS.md — read the relevant node_modules/next/dist/docs/ guide before any Next.js API change (this task changes no routing/RSC/server-action API, only client JSX)
  </read_first>
  <action>
    Delete the inline "Trade Profile" `Card` block entirely (D-01) — no placeholder, no empty column; the remaining left-column content shifts up via the existing `space-y-8`. Remove the now-unused `handleUpdateUsername` function, the `isUpdatingUsername` state, the page-level `username` state, and the `useEffect` that seeded `username` from the session (the modal owns its own field state and seeds from props). Keep the `Card`/`CardHeader`/`CardTitle`/`CardContent`/`CardDescription` imports — the "Add Cards & Wants" Card still uses them.

    In the header, wrap the right side in a `flex items-center gap-2` group with two elements in this order (D-03): FIRST a new "Trade Profile" button (`buttonVariants({ variant: "outline" })` styling to match the sibling link, `UserCog` lucide icon + label "Trade Profile", `onClick` sets `profileOpen` to true) — render it unconditionally so a user with no username yet can still open the modal to set one; THEN the existing "View Public Binder" `Link` (unchanged, still conditional on `publicUrl`). Add a `profileOpen` state and mount `<ProfileModal open={profileOpen} onOpenChange={setProfileOpen} currentUsername={session.user.username ?? ''} currentTradeNote={session.user.tradeNote ?? ''} />` (session type carries `tradeNote` via Plan 01). Import `ProfileModal` and `UserCog`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>test $(grep -c 'handleUpdateUsername' src/app/binder/manage/page.tsx) -eq 0 && grep -q 'ProfileModal' src/app/binder/manage/page.tsx && grep -q 'UserCog' src/app/binder/manage/page.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The inline Trade Profile Card and `handleUpdateUsername` are gone; no empty column remains.
    - Header shows a "Trade Profile" button before the "View Public Binder" link.
    - `ProfileModal` is mounted and receives `currentUsername`/`currentTradeNote`; `tsc --noEmit` is clean.
  </acceptance_criteria>
  <done>The profile is reachable only via the header button + modal; the inline card is removed (BINDER-15).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Better Auth API | The modal's Save sends username + tradeNote to `/api/auth/update-user` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-02 | Elevation of Privilege | `ProfileModal` Save → `authClient.updateUser` | medium | mitigate | Better Auth scopes the update to the current session's user — the client sends no `userId`, so a user can only edit their own profile/note (RESEARCH Security Domain V4). |
| T-31-03 | Denial of Service | trade-note textarea input | low | accept | Client caps at 140 chars via native `maxLength` + `.slice(0,140)` (D-04). Server-side length enforcement is an optional hardening (RESEARCH Open Question) — not blocking for this single-user-writes-own-profile threat model. |
| T-31-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages installed. |
</threat_model>

<verification>
- `npx vitest run src/components/binder/profile-modal.test.tsx` green.
- `npx tsc --noEmit` clean.
- `grep -c handleUpdateUsername` on the manage page is 0; `ProfileModal` + `UserCog` present.
- Manual: header shows "Trade Profile" then "View Public Binder"; opening the modal shows username + helper text + note textarea + counter; Save persists both and closes.
</verification>

<success_criteria>
BINDER-15 met: the profile lives behind a header button + modal, no inline card. BINDER-16 write path met: a ≤140-char note is set and saved (with username) in one call, and an empty save clears it.
</success_criteria>

<output>
Create `.planning/phases/31-trade-profile-modal-public-trade-note/31-03-SUMMARY.md` when done.
</output>
