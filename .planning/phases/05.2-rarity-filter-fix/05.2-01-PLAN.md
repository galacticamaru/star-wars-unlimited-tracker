---
phase: 05.2-rarity-filter-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/filter-cards.ts, src/lib/filter-cards.test.ts]
autonomous: true
requirements: [COLLECT-05]
must_haves:
  truths:
    - "Selecting 'Common' filters cards to only show those with rarity 'Common'"
    - "Selecting '(C) Common' (UI format) filters cards to only show those with rarity 'Common'"
    - "Selecting multiple rarities works as an OR filter"
    - "Selecting no rarities returns all cards"
  artifacts:
    - path: "src/lib/filter-cards.ts"
      provides: "Rarity filtering logic"
    - path: "src/lib/filter-cards.test.ts"
      provides: "Verification of rarity filtering"
  key_links:
    - from: "src/lib/filter-cards.ts"
      to: "card.rarity"
      via: "comparison"
---

<objective>
Implement rarity filtering in `filterCards` to resolve the hardcoded `true` bypass and handle the discrepancy between UI labels and database values.

Purpose: Enables the rarity filter in the card catalog, fulfilling a Phase 3 requirement.
Output: Updated `filter-cards.ts` and passing tests in `filter-cards.test.ts`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.2-rarity-filter-fix/05.2-CONTEXT.md
@src/lib/filter-cards.ts
@src/lib/filter-cards.test.ts

<interfaces>
From src/lib/filter-cards.ts:
```typescript
export interface FilterState {
  // ...
  selectedRarities: string[];
  // ...
}

export interface CardForFilter {
  // ...
  rarity: string;
  // ...
}

export function filterCards(cards: CardForFilter[], filters: FilterState): CardForFilter[];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update tests for rarity filtering</name>
  <files>src/lib/filter-cards.test.ts</files>
  <behavior>
    - Update 'rarity filter is bypassed' test to expect actual filtering:
      - Given cards ['Common', 'Rare']
      - When filter is ['Common']
      - Then result should have 1 card (Common)
    - Add test for UI format handling:
      - Given cards ['Common', 'Rare']
      - When filter is ['(C) Common']
      - Then result should have 1 card (Common)
    - Add test for multiple selection:
      - Given cards ['Common', 'Uncommon', 'Rare']
      - When filter is ['Common', 'Rare']
      - Then result should have 2 cards
  </behavior>
  <action>
    Modify `src/lib/filter-cards.test.ts`:
    1. Rename/Update the test "rarity filter is bypassed" to "filters by rarity (handles simple and UI-prefixed values)".
    2. Add assertions for simple values, prefixed values, and multiple values.
  </action>
  <verify>
    <automated>npx vitest run src/lib/filter-cards.test.ts</automated>
  </verify>
  <done>Tests are updated and failing (RED state)</done>
</task>

<task type="auto">
  <name>Task 2: Implement rarity filtering logic</name>
  <files>src/lib/filter-cards.ts</files>
  <action>
    Update `filterCards` in `src/lib/filter-cards.ts`:
    Replace `const matchesRarity = true;` with implementation that:
    1. Returns true if `selectedRarities` is empty.
    2. Maps `selectedRarities` to strip prefixes (e.g., "(C) Common" -> "Common") by checking for a space and splitting.
    3. Checks if `card.rarity` is included in the mapped selected rarities.
    
    Per D-05, filtering within a category is an OR match.
  </action>
  <verify>
    <automated>npx vitest run src/lib/filter-cards.test.ts</automated>
  </verify>
  <done>Tests in src/lib/filter-cards.test.ts pass (GREEN state)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client-side logic | Input parameters for filtering are processed on the client. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.2-01 | Tampering | filter-cards.ts | accept | Logic is for local filtering of cached data; no security risk if bypassed. |
</threat_model>

<verification>
Run the updated unit tests to ensure all filtering logic (including the fix and existing filters) remains intact.
</verification>

<success_criteria>
- Rarity filter correctly identifies cards by rarity.
- Rarity filter correctly handles "(X) Rarity" format from UI.
- No regression in other filters.
- `npm test src/lib/filter-cards.test.ts` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-rarity-filter-fix/05.2-01-SUMMARY.md`
</output>
