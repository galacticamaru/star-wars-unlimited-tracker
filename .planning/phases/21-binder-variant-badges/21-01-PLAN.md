---
phase: 21-binder-variant-badges
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/components/catalog/card-item.test.tsx
autonomous: true
requirements:
  - REQ-BINDER-06
must_haves:
  truths:
    - "card-item.test.tsx contains a failing test that asserts a variant badge is shown in binder mode when variantType is 'Foil'"
    - "card-item.test.tsx contains a failing test that asserts no variant badge is shown in binder mode when variantType is 'Normal'"
    - "npx vitest run exits non-zero (RED) after Wave 0 — the 2 new tests fail because variantType prop does not exist yet"
  artifacts:
    - path: src/components/catalog/card-item.test.tsx
      provides: "Failing Nyquist tests for variant badge behavior in binder mode"
      contains: "shows variant badge in binder mode when variantType is 'Foil'"
  key_links:
    - from: src/components/catalog/card-item.test.tsx
      to: src/components/catalog/card-item.tsx
      via: "import CardItem from card-item.tsx"
      pattern: "import.*CardItem.*from.*card-item"
---

<objective>
Write the two failing tests for the variant badge before any implementation exists. This is the Wave 0 Nyquist gate — the tests must be RED before Phase 21 implementation starts.

Purpose: Nyquist validation requires tests to pre-exist the implementation. These tests define the acceptance contract for the badge behavior in `CardItem` binder mode.
Output: `src/components/catalog/card-item.test.tsx` extended with 2 new failing test cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-binder-variant-badges/21-CONTEXT.md
@.planning/phases/21-binder-variant-badges/21-VALIDATION.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Extend card-item.test.tsx with 2 failing badge tests</name>
  <files>src/components/catalog/card-item.test.tsx</files>
  <read_first>
    - src/components/catalog/card-item.test.tsx (read current content before modifying — understand existing test structure, mock setup, defaultProps shape)
    - src/components/catalog/card-item.tsx (read to understand current CardItemProps — confirm variantType prop does NOT yet exist, so tests will fail RED)
  </read_first>
  <behavior>
    - Test case 1: `shows variant badge in binder mode when variantType is 'Foil'`
      - Renders `<CardItem {...defaultProps} mode="binder" variantType="Foil" />`
      - Expects `screen.getByText('FOIL')` to be defined (badge renders uppercase via CSS class, but the text node content is the raw prop value; check both 'Foil' and 'FOIL' — prefer checking for the raw value 'Foil' since `uppercase` is a CSS transform, not a JS transform; jsdom does not apply CSS text-transform)
      - NOTE: jsdom does NOT apply CSS `uppercase`. The text node content will be `Foil` (not `FOIL`). Test should use `screen.getByText('Foil')`.
    - Test case 2: `does not show variant badge in binder mode when variantType is 'Normal'`
      - Renders `<CardItem {...defaultProps} mode="binder" variantType="Normal" />`
      - Expects `screen.queryByText('Normal')` to be null (badge must not appear for Normal variant)
      - NOTE: 'Normal' text must not appear as a standalone badge element. The card name 'Vader' will still render. Only check that 'Normal' is absent.
  </behavior>
  <action>
Append two new test cases to `src/components/catalog/card-item.test.tsx` after the existing 5 tests (after line 57, before the end of the file). Do NOT modify the existing tests or imports.

Add exactly:

```typescript
test("shows variant badge in binder mode when variantType is 'Foil'", () => {
  render(<CardItem {...defaultProps} mode="binder" variantType="Foil" />);
  // variantType prop does not exist yet — this test is RED until Wave 1
  expect(screen.getByText('Foil')).toBeDefined();
});

test("does not show variant badge in binder mode when variantType is 'Normal'", () => {
  render(<CardItem {...defaultProps} mode="binder" variantType="Normal" />);
  // Badge must not render for Normal variant
  expect(screen.queryByText('Normal')).toBeNull();
});
```

After writing, run `npx vitest run src/components/catalog/card-item.test.tsx`. Confirm it exits with a non-zero code (RED) because `variantType` prop does not exist in `CardItemProps` yet and the text 'Foil' will not be present in the render output. If it exits 0, the implementation already exists — stop and report.
  </action>
  <verify>
    <automated>npx vitest run src/components/catalog/card-item.test.tsx 2>&1; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/catalog/card-item.test.tsx contains the line: `test("shows variant badge in binder mode when variantType is 'Foil'",`
    - src/components/catalog/card-item.test.tsx contains the line: `test("does not show variant badge in binder mode when variantType is 'Normal'",`
    - src/components/catalog/card-item.test.tsx contains: `screen.getByText('Foil')`
    - src/components/catalog/card-item.test.tsx contains: `screen.queryByText('Normal')`
    - `npx vitest run src/components/catalog/card-item.test.tsx` exits non-zero (the 2 new tests FAIL — this is the expected RED state)
    - The 5 pre-existing tests still PASS (only the 2 new tests fail)
  </acceptance_criteria>
  <done>card-item.test.tsx has 7 tests total; 2 new tests are RED; 5 existing tests are GREEN. Test file committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test file → component | Unit test boundary — no trust concerns |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | card-item.test.tsx | accept | Test file is not a security surface; threat is low-value. No production trust boundary crossed. |
</threat_model>

<verification>
- `npx vitest run src/components/catalog/card-item.test.tsx` exits non-zero
- File contains both new test names as exact strings
- Existing 5 tests remain passing
</verification>

<success_criteria>
Wave 0 Nyquist gate is set: 2 failing tests exist that define the badge contract. Implementation in Wave 1 will turn these GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/21-binder-variant-badges/21-01-SUMMARY.md`
</output>
