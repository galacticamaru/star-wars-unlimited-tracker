---
phase: 17.1-card-sync-variant-grouping
verified: 2026-05-20T00:00:00Z
status: human_needed
score: 4/5 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Run npm run db:seed a second time and confirm idempotent output"
    expected: "Seed exits 0 with same counts (34 sets, 7451 cards or equivalent) and no errors"
    why_human: "Cannot run a live DB-writing script during verification; requires human to execute and confirm"
---

# Phase 17.1: Card Sync Variant Grouping Verification Report

**Phase Goal:** Replace the fragile two-pass seeding logic in `upsertCards` with in-memory variant grouping by (Name, Subtitle), so all variants of a card reliably share the same `card_definition_id` regardless of set numbering conventions
**Verified:** 2026-05-20
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `upsertCards` groups all API-returned variants by (Name, Subtitle) in memory before any DB operations — no cross-DB name lookup inside the loop | VERIFIED | Lines 72-78 of `src/lib/sync/upsert-cards.ts`: `new Map<string, SWUCard[]>()` built from `nonTokenCards`; key is `Name.trim()|Subtitle.trim()`; DB insert begins only after all groups are built. No `db.select` or `db.query` anywhere inside the per-card loop. |
| 2 | All variant types (Normal, Foil, Hyperspace, Showcase, Prestige, etc.) for the same card share a single `card_definition_id` in `card_printings` | VERIFIED | Lines 82-176: every variant in a group receives `cardDefinitionId: def.id` from the single `card_definitions` upsert done once per group. ORPHAN-CHECK.md confirms `orphaned_variants = 0` after re-seed. |
| 3 | `npm run db:seed` completes without errors (verified via ORPHAN-CHECK.md) | VERIFIED | ORPHAN-CHECK.md records "Seed complete: 34 sets processed, 7451 cards upserted / Exit code: 0". Commit `5a8b87f` matches. |
| 4 | Re-seeding is idempotent (confirmed via human checkpoint) | UNCERTAIN | The 17.1-02-SUMMARY.md states "Human verified: idempotency confirmed" and the checkpoint task was marked approved. However the SUMMARY was authored by Claude; no independent human-written confirmation record (e.g. a typed "approved" log) is readable as a codebase artifact. Requires human re-confirmation. |
| 5 | Orphan-check query confirms 0 orphaned variant rows after re-seed | VERIFIED | ORPHAN-CHECK.md explicitly states `orphaned_variants = 0` with conclusion PASS. `scripts/orphan-check.ts` exists and contains the exact SQL query. Commit `5a8b87f` records the run. |

**Score:** 4/5 truths verified (1 UNCERTAIN — idempotency requires human confirmation)

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/lib/sync/upsert-cards.ts` | Refactored upsertCards with in-memory Map grouping | VERIFIED | File exists; contains `new Map<string, SWUCard[]>()`; no Pass 1/Pass 2 comments; no `fallbackDef`; no DB read inside loop. 177 lines (substantially reduced from ~314 lines in old version per commit `c117261` diff: +51/-188). |
| `.planning/phases/17.1-card-sync-variant-grouping/ORPHAN-CHECK.md` | Query output + row counts from orphan check after re-seed | VERIFIED | File exists; contains `orphaned_variants = 0`, `unreferenced_definitions = 53`, seed output, and PASS conclusion. |
| `scripts/orphan-check.ts` | One-shot Node.js script for variant orphan SQL queries | VERIFIED | File exists at `scripts/orphan-check.ts`; contains both orphan-variant and unreferenced-definitions queries via Drizzle `db.execute`. |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| groupBy key | anchor selection | `find(v => v.VariantType === 'Normal') ?? min(collectorNumber)` | WIRED | Line 88: `variants.find((v) => v.VariantType === 'Normal') ?? variants.slice().sort(...)` |
| anchor | card_definitions upsert | `swudbId = anchorCollectorNumber` | WIRED | Lines 93, 99: `anchorCollectorNumber` constructed from `anchor.Set-anchor.Number`; used as `swudbId` in `.values()` |
| card_printings onConflictDoUpdate | self-healing re-seed | `cardDefinitionId: sql\`excluded.card_definition_id\`` | WIRED | Line 162: `cardDefinitionId: sql\`excluded.card_definition_id\`` confirmed present in conflict update set |
| npm run db:seed | upsertCards (new) | syncAllCards -> upsertCards per set | WIRED | `syncAllCards` at lines 185-213 calls `upsertCards(set.setId, cards)` per set; seed script invokes `syncAllCards` |

---

## Data-Flow Trace (Level 4)

`upsertCards` is a sync/write function, not a data-rendering component. Level 4 data-flow trace applies to the grouping → DB write path rather than a render pipeline.

| Stage | Data Variable | Source | Produces Real Data | Status |
|-------|---------------|--------|--------------------|--------|
| Grouping | `groups` (Map) | `nonTokenCards` filtered from caller-supplied `cards` array | Yes — populated from API response via `syncAllCards` | FLOWING |
| Anchor selection | `anchor` | `variants.find(Normal) ?? min(collectorNumber)` | Yes — deterministic selection from real group members | FLOWING |
| card_definitions upsert | `def.id` | Drizzle `.insert(...).returning({ id })` | Yes — real DB write + returning ID | FLOWING |
| card_printings upsert | `cardDefinitionId: def.id` | `def.id` from above returning clause | Yes — real DB write; `cardDefinitionId` in conflict update set | FLOWING |

---

## Behavioral Spot-Checks

| Behavior | Evidence | Status |
|----------|----------|--------|
| No Pass 1 / Pass 2 comments in upsertCards | `grep "Pass 1"` → 0 matches; `grep "Pass 2"` → 0 matches | PASS |
| No fallbackDef block | `grep "fallbackDef"` → 0 matches | PASS |
| Exactly one Map instantiation | `grep "new Map"` → 1 match (line 72) | PASS |
| card_definition_id in conflict update set | `grep "card_definition_id"` → 2 matches (comment line 143, update set line 162) | PASS |
| TypeScript compiles clean | `npx tsc --noEmit` → no output, exit 0 | PASS |
| CardGrid React key uses collectorNumber | `card-grid.tsx` line 32: `key={\`${card.collectorNumber}-${mode}\`}` | PASS |
| Commits match claimed content | `c117261` modifies only `upsert-cards.ts` (+51/-188); `5a8b87f` adds `orphan-check.ts` + ORPHAN-CHECK.md | PASS |

---

## Requirements Coverage

| Requirement ID | Source Plan | Description | Status | Evidence |
|----------------|-------------|-------------|--------|----------|
| `data-integrity-variant-grouping` | 17.1-01-PLAN.md, 17.1-02-PLAN.md | In-memory variant grouping so all variants share one card_definition_id | SATISFIED | `upsertCards` implements Map grouping; ORPHAN-CHECK.md confirms 0 orphaned rows; both plans declare this ID |

**Note on REQUIREMENTS.md coverage:** The requirement ID `data-integrity-variant-grouping` does not appear in `.planning/REQUIREMENTS.md`. That file tracks v4.0 user-facing requirements (REQ-DECK-*, REQ-COLLECT-*, REQ-CAT-*). Phase 17.1 is a data-integrity sub-phase introduced after the REQUIREMENTS.md baseline was established, and its requirement ID is a plan-internal identifier. There are no orphaned REQUIREMENTS.md entries pointing to Phase 17.1. This is not a gap — the ID naming convention differs from the v4 REQ-* scheme.

---

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `src/lib/sync/upsert-cards.ts` | 54-59 | JSDoc comment still describes the old "two-pass strategy" and "look up existing card_definitions by name+subtitle" — directly contradicts the actual implementation | Warning | Documentation misleading; the function body is correct and does not use two-pass logic. No functional impact. |

The JSDoc stale comment is a documentation inconsistency, not a logic bug. The actual implementation on lines 60-177 is correct and consistent with the plan specification. Severity: Warning only.

---

## Human Verification Required

### 1. Idempotency Confirmation

**Test:** Run `npm run db:seed` a second time (or third time) against the live database.
**Expected:** Seed exits 0 with the same set/card counts as the previous run (34 sets, no runtime errors). No new orphaned variants created.
**Why human:** Cannot execute a live DB-writing seed script during automated verification. The SUMMARY claims this was human-verified at the Plan 02 checkpoint, but the checkpoint transcript is not independently available as a codebase artifact.

---

## Gaps Summary

No BLOCKER gaps. One WARNING:

- **Stale JSDoc comment** on `upsertCards` (lines 54-59) still describes the removed two-pass strategy. This is documentation drift — the implementation is correct. Recommend updating the JSDoc in a follow-up commit, but it does not block phase goal achievement.

One UNCERTAIN must-have (idempotency) requires human confirmation before status can be elevated to `passed`. If the developer confirms idempotency was verified at the Plan 02 checkpoint, no further action is required and status is effectively `passed`.

---

_Verified: 2026-05-20_
_Verifier: Claude (gsd-verifier)_
